import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
  purpose: "The current user's job applications, ordered by most recently updated; rows are inserted/updated by the Gmail webhook when inbox mail classifies as application/interview correspondence.",
  external: [],
};
