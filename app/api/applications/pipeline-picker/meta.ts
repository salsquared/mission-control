import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Read-only list of the user's INTERESTED-status applications that carry a usable posting URL, used as the source list for the resume-generate auto-fill flow.",
    external: [],
};
