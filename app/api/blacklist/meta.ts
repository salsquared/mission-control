import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Lists and adds companies to the user's job-discovery blacklist so they're excluded from suggestions.",
    external: [],
};
