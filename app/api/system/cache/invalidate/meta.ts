import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
  purpose: 'Invalidates entries in the in-memory withCache store by exact key or by key prefix, returning how many entries were evicted.',
  external: [],
};
