import type { ApiMeta } from "@/lib/api-docs/meta";

export const apiMeta: ApiMeta = {
    purpose: "Render a Canon's saved manual selection as a standalone HTML resume preview (opened in a new tab so links work) — verbatim, no AI rewrite.",
    external: [],
};
