import type { ApiMeta } from "@/lib/api-docs/meta";

export const apiMeta: ApiMeta = {
    purpose: "Get, update, or delete a single Canon (a keyword edit marks its resume stale).",
    external: [],
};
