import type { ApiMeta } from "@/lib/api-docs/meta";

export const apiMeta: ApiMeta = {
    purpose: "Load (GET) and save (PUT) a Canon's manual resume selection — the hand-curated entity/bullet set the builder edits.",
    external: [],
};
