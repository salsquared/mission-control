import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: 'Fetches daily papers directly from Hugging Face without arXiv fallback or Semantic Scholar enrichment.',
    external: ['Hugging Face Daily Papers API'],
};
