import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
  purpose: 'Retrieves the latest spaceflight and exploration news articles, capped per news site.',
  external: ['Spaceflight News API'],
};
