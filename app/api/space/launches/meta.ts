import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
  purpose: 'Fetches the next upcoming rocket launches worldwide, or launches within a year/month window when those query params are supplied.',
  external: ['The Space Devs Launch Library 2'],
};
