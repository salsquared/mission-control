import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
  purpose: "Returns the single owner's identity (id + email) and whether their Google account is connected with the required Gmail/Calendar scopes — the frontend's edge-trusted replacement for useSession.",
  external: [],
};
