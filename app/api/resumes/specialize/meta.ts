import type { ApiMeta } from "@/lib/api-docs/meta";

export const apiMeta: ApiMeta = {
    purpose: "Opt-in: re-word a canon's resume for a specific job (same selection, new wording) and save it as a per-job child.",
    external: ["Gemini"],
};
