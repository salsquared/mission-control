import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Computes the diff between two of the session user's generated resumes (passed as ?a and ?b), comparing their bullet selections, keywords, and skills gaps.",
    external: [],
};
