import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Returns one generated resume row with its bullet selections, posting input, and skills gap for the traceability UI; the heavy profile snapshot is included only with ?includeSnapshot=1.",
    external: [],
};
