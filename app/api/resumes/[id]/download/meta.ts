import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Streams the archived PDF/DOCX artifact bytes for one generated resume, owner-scoped, with a derived download filename.",
    external: [],
};
