import type { ApiMeta } from '@/lib/api-docs/meta';

export const apiMeta: ApiMeta = {
    purpose: "Lists the user's tracked job postings with server-side status/track/company/location/employment-type filtering and negative-filter culling.",
    external: [],
};
