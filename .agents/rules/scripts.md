---
trigger: always_on
---

Always place new one-off test scripts, database checkers, or experimental files in the `scripts/tests/` directory instead of the project root or /tmp.

Use TypeScript (`.ts`) as the language of choice for all new scripts.
