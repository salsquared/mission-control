# next_steps.md — Living session context

**Purpose.** Cross-session memory for Claude Code: where the last session left off, what's in flight, the umbrella goal, and the immediate next actions. Code-derivable facts live in CLAUDE.md; *state-derivable* facts (uncommitted work, decisions deferred to "next time", critical-path ordering) live here.

**Companion docs:** [`docs/user-stories.md`](./user-stories.md) (what + why) · [`docs/implementation.md`](./implementation.md) (how + in what order, with milestone status + concrete schema / API / file shapes). This file points at the next concrete thing to do; the others are the canonical references.

**Session protocol** (referenced from CLAUDE.md):
- **At session start** — read this file in full before doing anything else. If "In-progress work" conflicts with what's on disk now (file deleted, commit landed, etc.), update this file before continuing.
- **At session end** — update the sections below: move finished items into "Recently completed" (keep last 3–5), refresh "Critical path" and "Immediate next actions", note anything the user deferred.
- **Date format** — absolute ISO dates (`2026-05-14`), never relative phrasings.

---

## Last session

- **Date:** 2026-05-25 (Close-detection probe gate — fixed false-close across all 11 ATS kinds, recovered 921 false-closed postings on dev + 629 on prod).
- **Branch:** `main`, **12 commits ahead** of `origin/main` (4 new today on top of 8 prior). Working tree clean except for an unrelated `docs/user-stories.md` modification (user-added stories S8.9–S8.13) that's outside this fix.
- **Commits landed this session (in chronological order):**
  - **`9fcd6df` `feat(postings): probePostingLiveness + per-kind probe profiles`** — new `lib/postings/liveness.ts` module + `scripts/tests/hermetic/liveness-probe-smoke.ts` (70 assertions) + `scripts/pre-push.sh` wiring. `PROBE_PROFILES` const with 11 per-ATS budgets (LinkedIn 1×serial @ 1.5 s; Workday 6×parallel cap 500; etc.). Greenhouse/Lever hit their public REST APIs via slug+id parsed from `sourceUrl`. Defense-in-depth: post-fetch `assertSafeResponseUrl` rejects redirects to internal hosts. 429 from any probe in the batch sets a shared abort flag — remaining probes short-circuit to "unknown". `MC_LIVENESS_BYPASS` env (hermetic-test only) short-circuits the whole module.
  - **`0deef8e` `fix(job-watcher): probe-gate close detection across all 11 ATS kinds`** — `scheduler/jobs/job-watcher.ts` close-detection rewritten as probe-gated. Stale candidates → `probeBatch` → bucket into `confirmedClosed` / `refreshedAlive` / `unknown`. **Race-guard**: both `updateMany` WHEREs re-assert `status: notIn [closed, hidden]` so a manual user "Hide" during a long probe window (LinkedIn ~45 s) wins. `WatchlistRunResponseSchema.refreshedAlive` added with `.default(0)` for back-compat. `scripts/tests/hermetic/job-watcher-scale-regression-smoke.ts` sets `MC_LIVENESS_BYPASS=closed` so the bug-C 1100-row scale regression still exercises the close UPDATE path.
  - **`2626760` `chore(recovery): one-shot script for false-closed postings`** — `scripts/tests/debug/recover-false-closed.ts`. Dry-run default; `--apply` mutates; `--kind` / `--watchlist` / `--limit` / `--cap-multiplier` filters. Race-guarded: `status="closed"` required in `updateMany` WHERE.
  - **`9e29040` `docs: close-detection probe gate`** — `docs/close-detection-probe.md` (~360 lines, slated for `docs/archive/` after 7 days clean) + `CLAUDE.md` race-safety bullet + `docs/implementation.md` MB-2.4 paragraph + `docs/next_steps.md`.
- **Rollout completed both tiers:**
  - **Dev:** `pm2 restart mission-control-scheduler-dev`. Recovery dry-run + apply per kind: linkedin (201 → 15 closed, 186 reopened incl. the user-tracked Mattel listing), workday (677 → 22 closed, 655 alive across multiple sweeps), greenhouse (369 → 304 closed, 65 alive), small kinds (16 → 0 closed, all reopened). Total dev: 1,263 closed → 342 closed; 921 reopened.
  - **Prod:** `npm run build` + `pm2 restart mission-control mission-control-scheduler-prod --update-env`. Recovery applied: linkedin (162 → 6, 157 reopened including 7 user-tracked-as-application listings preserved), workday (414 → 0, all 414 alive — the most dramatic case), greenhouse (240 → 195, 45 alive), small kinds (15 → 1, 14 alive). Total prod: 831 closed → 202 closed; 629 reopened.
- **Self-review caught + fixed before committing:** (a) race with manual user "Hide" during probe window (H1, added `status: notIn` guards); (b) missing post-fetch `assertSafeResponseUrl` allowing redirect-to-internal-host (H2); (c) no 429-abort plumbing despite the design committing to it (H4, added `RateLimitCallback` thread-through); (d) stale comment block in `job-watcher.ts` describing pre-probe logic alongside new (consolidated); (e) `parseInt` `--limit` had no NaN check (added); (f) redundant `if (perHitDelayMs > 0)` + trailing-sleep-after-last-probe inefficiency. Added 4 new smoke tests covering the abort + redirect paths.
- **Final gate state:** `bash scripts/pre-push.sh` 51/51 hermetic suites green (the +1 from `liveness-probe-smoke.ts`). `tsc --noEmit` clean. Both schedulers running new code; `[liveness] kind=… alive/closed/unknown` lines visible in `pm2 logs mission-control-scheduler-{dev,prod}`. Mattel posting (the user's original symptom report) confirmed reopened: `status="tracked"`, `removedAt=null`, CLOSED badge gone.

---

### Previous session (kept for one cycle, then prune)

- **Date:** 2026-05-24 (LOP-6 cutover — all 9 callsites moved to the Lunary prompt registry).
- **Branch:** `main`, 2 commits ahead of `origin/main` (commits `0581845` + `d9ecd07` from earlier in the day still local-only), plus uncommitted LOP-6 work in the tree.
- **What landed this turn (uncommitted):**
  - **`scripts/sync-lunary-templates.ts`** — idempotent bulk-upload of `docs/llm-prompts/<slug>.md` → Lunary's prompt registry via REST. Authenticated by `LUNARY_SECRET_KEY` (private/secret API key from Lunary dashboard → Settings; distinct from `LUNARY_PUBLIC_KEY` for tracing). Key-sorted JSON compare so version churn only fires on real content diffs. All 9 templates created in Lunary (IDs 6272–6280).
  - **`lib/ai/prompts.ts`** — `loadPrompt(slug, vars)` façade. Prefers `lunary.renderTemplate` when configured; falls back to parsing `docs/llm-prompts/<slug>.md` from disk (what makes hermetic smokes + dev runs without a Lunary account work, and what protects production through transient Lunary API blips). Caches disk reads.
  - **8 callsite cutovers** — `posting-parse`, `profile-import`, `profile-synthesize`, `discovery-suggest`, `employment-type-classifier`, `email-parser`, `resume-rewrite`, and `bullet-assist-{fill,rewrite}`. All inline `SYSTEM_PROMPT` constants removed; each call routes through `loadPrompt`. `buildBulletAssistPrompt` became async (loops the registry renderer for the 8 KB overflow contract); `await` added in smoke + `/api/profile/bullets/assist/route.ts` + `eval/provider.ts`.
  - **Docs** — `docs/llm-calls.md` "Migrated to registry?" column flipped to ✅ for all 9 + Observability + Change log entries updated. `docs/implementation.md` LOP-6 expanded with the actual mechanism + flipped table to ✅. `CLAUDE.md` LOP-6 invariant rewritten with the loadPrompt flow + dashboard model-dropdown caveat.
  - **Debug aids in `scripts/tests/debug/`** — `lunary-probe.ts` (verify `renderTemplate` return shape) + `loadprompt-smoke.ts` (verify both Lunary + disk paths return identical content for `posting-parse`). Both kept; cheap smokes for future Lunary debugging.
- **Final gate state:** pre-push 45/45 hermetic suites green after the cutover. Typecheck clean (`tsc --noEmit` passed after async-cutover ripple fixes). PM2 dev tier restarted with `--update-env` to pick up `LUNARY_PUBLIC_KEY` + `LUNARY_SECRET_KEY`; `[LUNARY] tracing enabled` confirmed in logs.
- **Lunary dashboard caveat noted in docs:** the UI's model dropdown only knows OpenAI/Anthropic SKUs, so our `gemini-3.x` strings display as a fallback like "gpt-5.5". The actual stored `extra.model` is correct; playground / built-in evals don't work for our model fleet (use `npm run test:prompts` instead).
- **Working tree:** dirty with the LOP-6 cutover. Not yet committed — user to review + bundle.

## Umbrella goal

**Apply to jobs.** The `docs/user-stories.md` roadmap is functionally closed — every 🔴 + 🟡 + 🔵 story is shipped, declined, or deferred-by-design. Forward motion now runs through real-world use:

1. Send applications.
2. When the LLM rewrites are off, capture the failure mode in `docs/implementation.md` §Prompt tuning and revise prompts.
3. When something in the pipeline misfires (wrong status classification, missed posting, calendar sync glitch), file it as a follow-up and patch.

Track A (Pipeline UX), Track B (Discovery + notifications), Track C (Profile + resume + GitHub) are all in production. Backups encrypted (RAH-13). Rate limits in place (RAH-12). The MA/MB/M7/M8/M9 milestone scaffolding referenced by older sessions is all behind us; consult `docs/implementation.md` for the per-milestone shipped detail.

## Critical path — current

**Apply, observe, iterate — now backed by structured LLM tracing.** The "apply ASAP" loop is fully closed (capture, kanban + side-track kanban, drill-in, contacts, watchlists, notifications with negative-filter + quiet hours, profile + multi-resume import + snapshots, tailored PDF + DOCX + skills-gap + resume-diff, GitHub metrics + README ingestion). 2026-05-24 added the LLM observability rail on top: every Gemini call lands in Lunary with `name` + tokens + latency when `LUNARY_PUBLIC_KEY` is set; prompt regressions catch in `npm run test:prompts` via Promptfoo. §Prompt tuning's free-text observations fold into Promptfoo fixtures going forward — see `eval/suites/<callsite>.yaml` and `docs/llm-prompts/<slug>.md`.

## Immediate next actions (in order)

1. **Commit the close-detection probe gate (today's work).** Suggested commit split per the doc plan: (1) `feat(postings): probePostingLiveness + per-kind profiles` for `lib/postings/liveness.ts` + smoke + pre-push wiring, (2) `fix(job-watcher): probe-gate close detection across all 11 ATS kinds` for the scheduler integration, (3) `chore(recovery): one-shot script + run against dev/prod` for the debug script (data already mutated in-tier), (4) `docs: close-detection probe gate` for the doc + CLAUDE.md + implementation.md updates.
2. **Push the now ~13 local commits ahead of `origin/main`.** Earlier LOP-6 cutover + 2026-05-24 audit fix-stack + today's probe-gate work. Nothing else gates the push.
2. **Triage the "Audit-found soft bugs" list below** (next §). These are the 10 issues caught during the audit that weren't fixed in-session — most are bounded in impact but worth deciding on each. Pick the ones to fix next; some are 5-line edits.
3. **End-to-end Lunary trace check.** Trigger any LLM path through the running dev process (e.g. fill some bullets on the Profile dash, paste a posting URL) and confirm runs appear in Lunary's dashboard tagged by their `name`. PM2 was restarted twice this session; should be live.
4. **Run `npm run test:prompts` once end-to-end.** Validates the Promptfoo harness against all 9 cut-over callsites with synthetic seed fixtures. Expected ~$0.01–0.05 in Gemini spend. First-run failures are the cue to capture real fixtures (LOP-9) and tighten assertions.
5. **LOP-9 real fixture captures.** Add a `CAPTURE_FIXTURES=1` env gate to the start of `chatJSON` that emits `console.info('[FIXTURE]', JSON.stringify({ name, system, user }))`. Then `CAPTURE_FIXTURES=1 pm2 restart mission-control-dev --update-env`, use the app for ~30 min, grep the pm2 logs for `[FIXTURE]`, and replace the synthetic-but-realistic seed entries in `eval/suites/*.yaml`.
6. **Story S7.6 rollback UX (🔵, deferred-by-design).** Capture side shipped; restore-from-snapshot is the deferred half. Build a destructive-overwrite confirm + transactional bulk-replace of `WorkRole` / `Project` / `Education` from the stored payload. Intentionally not shipped — destructive UI without a real trigger is more risk than safety; pick it up when you've actually made an edit you want to undo.

## Audit-found soft bugs (deferred — pick next)

From the 2026-05-24 system audit (commits `4a609df`, `4f1854d`, `dde9151`, `86f5aec`). Each was caught + flagged but not fixed in-session because impact was bounded or the fix required a product call. **Regression tests for the 7 fixed bugs are now in pre-push** (`job-watcher-classifier-regression-smoke`, `job-watcher-scale-regression-smoke`, `fetcher-partial-regression-smoke`, `events-broadcast-regression-smoke` — covers A/B/C/D/E/G; F is wrap-and-test-resistant). The list below is what to fix next.

| # | Issue | Where | Impact | Fix shape |
|---|---|---|---|---|
| 1 | **`nextSteps` wipe on email update.** `lib/applications/ingest.ts:208` does `nextSteps: parsed.nextSteps ?? null` — a newer email that omits nextSteps wipes the prior value (e.g. "Reply by Friday" hint disappears when the followup "we received it" email lands). | `lib/applications/ingest.ts:208` + similar in race-loser branch ~264. | Soft UX. User may lose actionable hints. | Either `nextSteps: parsed.nextSteps ?? existingApp.nextSteps` (preserve), or product call: "latest email is source of truth even when blank?" |
| 2 | **No Notification retention.** WebhookDelivery has 30d retention; Notification has none. 784 rows in 4 days currently; grows unbounded. | `prisma/schema.prisma:Notification` + `scheduler/jobs/`. | Cosmetic table bloat. Bell stays usable but DB grows. | New `scheduler/jobs/notification-prune.ts` mirroring `webhook-delivery-prune.ts`, 90-day default; wire into `scheduler/index.ts:JOBS`. |
| 3 | **Gmail webhook doesn't paginate `gmail.users.history.list`.** For active users (sparse delta per webhook) this never matters. For backfill bursts (fresh user with many historical inbox events), page 2+ records silently drop — `lastSyncedHistoryId` advances past them. | `app/api/gmail/webhook/route.ts:95-100`. | Backfill-only. Unfair to new users on signup. | Loop on `nextPageToken` from `historyRes.data.nextPageToken`, append messages, stop when no token. |
| 4 | **email-parser hardcodes `"gemini-3.1-flash-lite"` in two places.** `safeTrack` extra.model (line ~166) AND `getProvider()("gemini-3.1-flash-lite")` (line ~177). Future model bump means editing both or the trace lies. | `lib/email-parser.ts`. | Maintenance gotcha (latent — currently consistent). | Pull to a single constant at the top of the file, or read from `loaded.model` returned by `loadPrompt("email-parser", …)`. |
| 5 | **`bulkMoveApplicationsTrack` `NOT: { id: { in: ids } }` could P2029.** Same 999-param notIn limit as Bug C. Currently bounded by UI selection size (kanban). | `lib/repositories/applications.ts:192`. | Latent — only fires if a script bulk-moves >1000 apps. | Same pattern as Bug C fix: SELECT conflicting rows, diff in JS, UPDATE by id. |
| 6 | **Scheduler ↔ Next.js cross-process race.** `processOne`'s in-process Map mutex doesn't span processes. A user clicking "Run now" while the scheduler ticks the same watchlist → both processes fetch externally + write. P2002 fallback handles per-posting collisions; the external fetch is wasted. | `scheduler/jobs/job-watcher.ts:73-82` + `app/api/watchlists/[id]/run/route.ts`. | Wasteful (duplicate fetch, potential 429 on the external source) but not corrupting. | Move the mutex to DB (`Watchlist.inFlightAt` timestamp + 60s lease), or accept it. |
| 7 | **Resume gen orphan rows on crash mid-write.** `app/api/resumes/route.ts:219-282` creates a row with `status="ready"` then writes the artifact and updates `artifactPath`. Server crash between the two leaves a "ready" row with null path — `/download` 404s while the row sits in the list. | `app/api/resumes/route.ts:219-282`. | Low probability (~100ms window). User-visible: orphan row in resume history. | Either create row with `status="generating"` and only flip to `"ready"` after artifact write succeeds, or add a startup sweep that errors-out any `status="ready" + artifactPath=null` rows. |
| 8 | **4659 legacy null-`employmentType` rows in prod.** Most pre-date the classifier or were wiped by the (now-fixed) Bug A. Not recoverable without paid backfill. | `prisma/prod.db`. | UI shows "Unspecified" for these rows. Going-forward fix is in place; backfill is one-time. | Optional: `scripts/archive/migrations/backfill-employment-types.ts` that batches the 4659 nulls through `classifyEmploymentTypes`. ~$0.50–$2 in Gemini spend. |
| 9 | **`cache.ts` L1 pruner has the same setInterval startup-skip pattern as Bug F.** But the 5-min interval makes it irrelevant in practice (first prune is at most 5min late). | `lib/cache.ts:62`. | Negligible — bounded by interval. | Optional consistency fix mirroring scheduler/index.ts kick pattern. |
| 10 | **Module-init `LUNARY_ENABLED` reads in `lib/email-parser.ts` + `lib/ai/gemini.ts`.** Same race I fixed in `prompts.ts`. In Next.js/PM2 production env loads before module load → correct. In ad-hoc tsx scripts the import order matters — could silently miss Lunary traces. | `lib/email-parser.ts:10` + `lib/ai/gemini.ts:9`. | Production-correct. Observability-degraded for ad-hoc tsx scripts only. | Wrap each `LUNARY_ENABLED` in a `function lunaryEnabled()` call-time check (same shape as `lib/ai/prompts.ts:42`). |

Recommended fix order if working through the list:
- **Quick wins (≤ 15 min each):** #4 (single-constant refactor), #9, #10 (call-time wrappers).
- **High-value (~30 min):** #2 (notification-prune job), #5 (bulkMoveApplicationsTrack notIn rewrite).
- **Real product calls:** #1 (nextSteps wipe semantics), #7 (orphan-row recovery strategy).
- **Bigger lifts:** #3 (Gmail pagination — needs a backfill smoke), #6 (cross-process mutex — moves state to DB), #8 (paid backfill — needs user opt-in for spend).

**Note: every other shippable 🔴/🟡/🔵 story (and both open RAH-N items) is now ✅ or ⛔.** The backlog moves to "real-world use → first applied posting → iterate on prompts." Failure modes captured via the Lunary trace and folded into Promptfoo fixtures rather than free-text in §Prompt tuning. Genuine MVP-followup TODOs from prior sessions (LLM fuzzy bullet dedup, LinkedIn export ZIP, legacy `.doc`, per-file SSE progress, ProjectCard portfolio toggle UI) are still around but none of them block applying.

**User-side follow-ups for RAH-13 (just shipped):**
- **Save the secret key to 1Password.** The file `~/.config/mission-control/backup.key` was generated this session. It's chmod-600 locally, but losing the Mac without an offsite copy means every encrypted backup is unrecoverable. Copy the file's full contents (including the `# created:` / `# public key:` header lines) into a 1Password secure-note titled `mission-control backup secret`.
- **Decrypt-smoke an encrypted backup** to be sure the round-trip works on a real artifact: `./scripts/backup-decrypt.sh ~/backups/mission-control/mc-20260522-201632.db.age` should produce a readable `.db` next to it. (This session already verified the round-trip end-to-end against the live keypair — same 37 Application rows in encrypted+decrypted vs `prisma/prod.db`.)
- **Purge the plaintext history** once decrypt-smoke passes: `rm -f ~/backups/mission-control/mc-*.db ~/backups/mission-control/mc-resumes-*.tar.gz` locally; if rclone is installed, also `rclone delete gdrive:backups/mission-control/ --include "mc-*.db" --include "mc-resumes-*.tar.gz"`. Until then, the security gap stays open on the historical snapshots even though new ones are encrypted.

**Genuine MVP-followup TODOs (cross-cutting, not story-numbered):**
- LLM-judged fuzzy bullet dedup (current dedup is exact-text only — "Built a TS API" and "Built a TypeScript API" both survive).
- LinkedIn export ZIP import support (separate unzip path that reads `Positions.csv` / `Education.csv`).
- Legacy `.doc` import format (mammoth handles `.docx` only).
- Per-file progress streaming via SSE so the UI shows "extract → analyze → merge" stages live instead of one long spinner.
- M9 Phase 2 UX: portfolio-toggle + `githubRepo` input on `ProjectCard` (currently DB-only).

## In-progress work

**LOP-6 cutover is in the working tree — needs commit + push.** Files touched:
- New: `scripts/sync-lunary-templates.ts`, `lib/ai/prompts.ts`, `scripts/tests/debug/lunary-probe.ts`, `scripts/tests/debug/loadprompt-smoke.ts`.
- Modified callsites (8): `lib/resumes/posting.ts`, `lib/profile/import-llm.ts`, `lib/profile/synthesize.ts`, `lib/discovery/suggest.ts`, `lib/ai/classify-employment-type.ts`, `lib/email-parser.ts`, `lib/resumes/rewrite.ts`, `lib/profile/bullet-assist.ts`.
- Async-cutover ripple: `scripts/tests/hermetic/bullet-assist-smoke.ts`, `app/api/profile/bullets/assist/route.ts`, `eval/provider.ts`.
- Docs: `CLAUDE.md`, `docs/llm-calls.md`, `docs/implementation.md`, `docs/next_steps.md` (this file).

Pre-push 45/45 hermetic green. Typecheck clean. Lunary dashboard has all 9 templates (verified via `scripts/tests/debug/lunary-probe.ts`).

**Unattended verifications waiting on user (LOP-6 cutover):**
- End-to-end trace: trigger any LLM path in the running dev process (Profile dash bullet fill / posting paste / email ingest) and confirm the run shows up in the Lunary dashboard tagged by its `name`. Confirm `renderTemplate` returns the right template version (compare against `docs/llm-prompts/<slug>.md`).
- Run `npm run test:prompts` once end-to-end to validate the eval harness against the now-registry-backed prompts. Expected: ~30–60 Gemini calls, ~$0.01–0.05 spend, structured pass/fail. First-run rubric failures expected on synthetic fixtures — that's the cue for LOP-9.

**Unattended verifications still open from prior sessions:**

*Backups / infrastructure (no UI):*
- Confirm `~/.config/mission-control/backup.key` exists and is chmod-600. Run `./scripts/backup-decrypt.sh ~/backups/mission-control/$(ls -t ~/backups/mission-control/*.age | head -1)` once to verify the round-trip works against a real artifact.
- **Save the secret key to 1Password.** Losing the Mac without an offsite copy means every encrypted backup is unrecoverable.

*From 2026-05-22 — UI surfaces probably already verified by now (sweep if anything looks off):*
- Profile dash: "History" section (ProfileSnapshotsCard) + cross-tab SSE delete refresh.
- Applications dash: "Contacts" expander between Timeline and Resumes; ≥2 resume rows show checkboxes → "Compare selected" → inline ResumeDiffPanel; kanban header `CheckSquare` → select-mode → bulk-track footer bar with same-employer-both-tracks conflict toast.
- Postings: emerald comp chips on cleanly-parsed `NewPostingsCard` rows.
- Side-track UI: kanban sections, AddApplicationModal `defaultTrack="side"`, AddWatchlistModal hides career-curated tabs, per-track filter slices independent.
- `/api/system` `cacheStats.hits` actually moves (was pinned at 0 pre-`ae424e8`).

## Recently completed

- **2026-05-25 (resume-link filter)** — **Fixed two GitHub-link entries rendering side-by-side in generated resumes.** Commit `decb309`. Root cause: `lib/profile/import-llm.ts` and `lib/profile/synthesize.ts` schemas declare link `url` as `z.string()` (not `.url()`), so the LLM occasionally emitted section-header text like `{label:"Github", url:"Github"}` alongside the real `{label:"GitHub", url:"https://github.com/u"}` — both rendered in the resume header. Fix: new `lib/profile/links.ts:isValidUrl()` (parseable URL with scheme OR scheme-less `host.tld[/path]`); `lib/profile/merge.ts:mergeHeader` filters incoming + existing through it so future imports stop accumulating bad data AND legacy corruption self-heals on next import; `lib/resumes/templates/ats-plain.tsx` filters at render time as final defense; two new hermetic test cases in `profile-merge-smoke.ts`. Tightened `docs/llm-prompts/profile-import.md` rule 7 to instruct the model to omit entries when only a section header is present (run `npx tsx scripts/sync-lunary-templates.ts` to push to Lunary). Cleaned corrupt rows out of dev.db (kept the real GitHub URL) and prod.db (all entries were bogus — now `links: null`, user can re-add manually via Profile UI which uses the strict `.url()` schema). 50/50 hermetic green; lint clean on changed files.
- **2026-05-24 (LOP-6 cutover)** — **All 9 LLM callsites moved off inline `SYSTEM_PROMPT` constants and onto the Lunary prompt registry.** Uncommitted, in working tree. New `scripts/sync-lunary-templates.ts` idempotently uploads `docs/llm-prompts/*.md` to Lunary's REST API (auth via `LUNARY_SECRET_KEY` private key, separate from `LUNARY_PUBLIC_KEY` for tracing). All 9 templates created in Lunary (IDs 6272–6280). New `lib/ai/prompts.ts:loadPrompt(slug, vars)` façade prefers Lunary's `renderTemplate`, falls back to disk-parsing `docs/llm-prompts/<slug>.md` when `LUNARY_PUBLIC_KEY` unset or Lunary errors (hermetic smokes / fresh clones / API blips). 8 callsite cutovers: `posting-parse`, `profile-import`, `profile-synthesize`, `discovery-suggest`, `employment-type-classifier`, `email-parser`, `resume-rewrite`, `bullet-assist-{fill,rewrite}`. `buildBulletAssistPrompt` became async (loops the registry renderer to enforce 8 KB overflow against the live template); smoke + `/api/profile/bullets/assist` route + Promptfoo provider all gained `await`. 45/45 hermetic green post-cutover, typecheck clean. Dashboard caveat documented: Lunary's UI model dropdown only recognizes OpenAI/Anthropic SKUs, so our `gemini-3.x` strings show as fallback "gpt-5.5"; the actual `extra.model` value is stored correctly and returned by `renderTemplate`. Playground/built-in evals unusable for our model fleet — `npm run test:prompts` (Promptfoo) is the eval path.
- **2026-05-24 (LOP-1 → LOP-5 + LOP-7 + LOP-8 + LOP-10 + LOP-11)** — **LLM observability infra + prompt-registry scaffold.** Commit `d9ecd07`. Lunary `wrapModel` integration at module-init in `lib/ai/gemini.ts`, gated on `LUNARY_PUBLIC_KEY` so dev / CI / no-key runs are a true no-op. Required `name: string` field on `ChatJSONOptions` — TypeScript flags every callsite. All 7 chatJSON callsites tagged with stable kebab-case slugs (`bullet-assist-fill`, `bullet-assist-rewrite`, `resume-rewrite`, `posting-parse`, `profile-import`, `profile-synthesize`, `discovery-suggest`, `employment-type-classifier`). `lib/email-parser.ts` bypasses chatJSON (Vercel AI SDK), so it traces manually via `lunary.trackEvent` inside a defensive `safeTrack` helper. New `docs/llm-prompts/` (10 files, one per slug) with verbatim system + user template text + `{{var}}` markers ready to paste into Lunary's template UI for LOP-6 cutover. New `eval/` directory (14 files): TS custom provider `eval/provider.ts` dispatches per-fixture callsite to the real chatJSON-wrapped lib functions; `eval/suites/<slug>.yaml` × 9 with 13 seed fixtures total; `npm run test:prompts` script (NOT in pre-push — burns real Gemini tokens, ~$0.01–0.05/run). `docs/llm-calls.md` gains an Observability section + `Lunary slug` / `Migrated to registry?` columns in the inventory. CLAUDE.md gains an "LLM observability" subsection with the three invariants (name required; iterate via renderTemplate post-cutover; prompt edits ship with Promptfoo fixture updates). Dependency notes: installed `lunary@1.0.18` and `promptfoo@0.121.12 --legacy-peer-deps` (lunary's optional `openai@^4` peer clashed with promptfoo's required `openai@^6` — lunary doesn't need openai for our Gemini-only stack); resolver shuffle dropped `react-is` from the top-level hoist and broke recharts at build, fixed by pinning `react-is@^19.2.6` directly. Pre-push 45/45 hermetic green (verified twice). Typecheck clean. Lint clean on touched files. **NOT yet pushed** — local commit only.
- **2026-05-24 (docs)** — **`docs/user-stories-applications.md` → `docs/user-stories.md` rename + §15 local-auth section.** Commit `0581845`. Combines the applications roadmap with the in-flight local-auth draft into one canonical user-stories doc. Local auth lives at §15 (S15.1–S15.39, all unbuilt) with seven Open Questions inline before any implementation begins. Cross-refs in `docs/next_steps.md` and `lib/fetchers/github-public-fetcher.ts` updated to the new path; remaining refs in `docs/implementation.md` will land with in-flight work that's already modifying it.
- **2026-05-23 (M7.6)** — **LLM bullet assist + resume-upload archive (S7.7 + S7.8 + S7.9).** Commits `7ffd5ba` + `fffa038` (rewrite-tag enhancement). 11 numbered tasks M7.6.1–M7.6.11. `ResumeUpload` table + `data/resume-uploads/` storage holds raw resume bytes from every M7.4 import (the previous discard path was lossy). `lib/profile/bullet-assist.ts` ships fill (3–5 starter bullets on empty entries) + rewrite (single-bullet diff via wand icon) modes grounded on entry spine + sibling bullets + archive spans (S7.9) + project README. `MODEL_LITE` (`gemini-3.1-flash-lite`) — initially tried the non-lite SKU per direction but it 404'd; `-lite` is the only 3.1 variant Google ships. Rate-limited 20 / 10 min. Rewrite mode now returns updated tags reflecting wording shifts (not just text). Hermetic smokes `archive-spans-smoke` + `bullet-assist-smoke` + `resume-uploads-smoke` wired into pre-push. See `docs/implementation.md §M7.6` for the canonical design.
- **2026-05-23 (Track D)** — **Mobile layout MD-0 → MD-7.** Commit `893628a`, ~450 lines net across 11 files. Viewport meta + `useEffectiveMobileLayout` + Dashboard fork into `DesktopShell` / `MobileShell` + framer-motion swipe carousel + Launchpad sheet variant + Auto/Mobile/Desktop preference UI. No schema / API / repository / scheduler changes. Pre-existing desktop card-on-canvas frame preserved verbatim in `<DesktopShell>`; narrow viewports get the edge-to-edge swipe carousel.
- **2026-05-23 (Observability + fetcher-health fixes)** — Five commits in a small fix batch (`fee69ec`, `ba9e222`, `792c2d7`, `e0ff6d6`, `55dccb0`, `ebf9f7a`): self-healing SSE + log-buffer noise trim; kill dev-tier upstream errors flagged in audit; tier-aware log path + auth outside cache for fetcher-health; tighten host-extraction regex; `loggedFetch` wrapper so every external call is counted; implementation status table polish.
- **2026-05-22 (RAH-13)** — **DB backups now encrypted with age.** `scripts/backup-db.sh` reworked: auto-discovers an age recipient at `~/.config/mission-control/backup.pub`, encrypts each artifact in place before either local retention or offsite upload sees it, falls back to plaintext with a loud warning so cron doesn't break before initial key setup. New `scripts/backup-decrypt.sh` companion. **User still needs to copy the secret key to 1Password** — see §Immediate next actions follow-up checklist below the "Note" block.

_(Earlier entries pruned per the 3–5-entry protocol — detail lives in `docs/implementation.md` per-milestone sections + `git log`.)_

## Known issues / parked TODOs

- **Manual UI smoke** is still nominally outstanding (eyeball the Profile dash, confirm cards render / drag-reorder works / Import + Generate cards look right, sanity-check `viewHue: 280`). Backend pipe is verified end-to-end so this is low-risk visual confirmation.
- **LLM fuzzy bullet dedup** — current merge dedup is exact-text only. "Built a TS API" vs "Built a TypeScript API" both survive. Add an LLM "are these the same accomplishment?" pass scoped to one parent entity when this becomes painful.
- **LinkedIn export ZIP** — separate unzip path reading `Positions.csv` / `Education.csv`; not wired yet.
- **Legacy `.doc`** — mammoth handles `.docx` only. Either skip or wire a converter.
- **`viewHue: 280`** for Profile dash is a placeholder — easy one-liner change in `components/providers/state/index.ts`.
