# next_steps.md — Living session context

**Purpose.** Cross-session memory for Claude Code: where the last session left off, what's in flight, the umbrella goal, and the immediate next actions. Code-derivable facts live in CLAUDE.md; *state-derivable* facts (uncommitted work, decisions deferred to "next time", critical-path ordering) live here.

**Companion docs:** [`docs/user-stories.md`](./user-stories.md) (what + why) · [`docs/implementation.md`](./implementation.md) (how + in what order, with milestone status + concrete schema / API / file shapes). This file points at the next concrete thing to do; the others are the canonical references.

**Session protocol** (referenced from CLAUDE.md):
- **At session start** — read this file in full before doing anything else. If "In-progress work" conflicts with what's on disk now (file deleted, commit landed, etc.), update this file before continuing.
- **At session end** — update the sections below: move finished items into "Recently completed" (keep last 3–5), refresh "Critical path" and "Immediate next actions", note anything the user deferred.
- **Date format** — absolute ISO dates (`2026-05-14`), never relative phrasings.

---

## Last session

- **Date:** 2026-06-01 (**Resume Re-render = preview-only (no new version) + Content-Disposition em-dash crash fixed; 2 COMMITS scoped; NOT pushed; prod NOT rebuilt**). Two follow-up fixes to the canon resume preview/re-render flow. **(A) Re-render must not create a version** (user: "re-rendering should not generate a new resume or count as a regeneration ... thats only for the generate buttons"). `components/cards/profile/CanonsCard.tsx` — `handleRegenerate` was POSTing `/api/resumes` (which persists a `GeneratedResume` version, bumping the count); now it's a **pure GET** `window.open(/api/canons/[id]/preview)` — no POST, no persistence, no version. Removed the regenerate single-flight plumbing (`regenId`/`setRegenId` state, `regenerating`/`anyRegenerating` props, the Loader2 spinner); button stays "Re-render", gated `disabled={!canon.hasSelection}`, tooltip clarifies "does NOT create a new version". `app/api/canons/[id]/preview/route.ts` — the preview now **self-computes its page-fit count** when opened without `?pages` (renders a PDF via `renderResumePDF` + `countPdfPages` just to count — **persists NOTHING**), so the card's Re-render still shows the authoritative "✓ Fits on one page" banner; the builder's Generate keeps passing `?pages=N` (from its X-Resume-Pages) to skip the extra render. **(Removing `regenId` also cleared the `CanonsCard` tsc errors the concurrent project-dates entry flagged — the tree compiles now.)** **(B) Content-Disposition ByteString crash** (user hit "Cannot convert argument to a ByteString ... value of 8212" re-rendering "security officer — Downey, CA"). Root cause: the em dash (U+2014) in the canon name flowed into `Content-Disposition: filename="…"` (a ByteString — codepoints >255 throw), so building the response threw and the canon handler returned it as the 500. `lib/resumes/labels.ts:sanitizeFilenameSegment` now folds typographic punctuation to ASCII (em/en dashes→`-`, smart quotes→`'`, ellipsis→`...`) and drops any remaining codepoint >255, **keeping Latin-1 accents** (José/Café ≤255 survive). One fix covers all 3 Content-Disposition sites (canon gen, auto pipeline, `[id]/download`). **Verified:** `tsc` clean; resume-labels-smoke **16/16** (+ new regression: em dash folds, no >255 survives, full filename round-trips through `Headers.set` without throwing); canon-manual-selection **34/34**; resume-render PASS; **real-data check**: the card's Re-render path rendered + counted pages (banner "✓ Fits on one page") with `GeneratedResume` count flat **72→72** (creates no version). **`apis.html` deliberately NOT touched** — its staleness is the concurrent job's `lib/schemas/profile.ts` change (Project start/end date schemas, +48 lines), theirs to regenerate; I reverted my accidental regen. **COMMITTED as 2 scoped commits** (B: `labels.ts` + smoke; A: `CanonsCard.tsx` + preview route + this doc); **EXCLUDED** all concurrent churn (`app/api/resumes/route.ts`, `lib/canons/keywords.ts`, `lib/fetchers/{indeed,linkedin}-fetcher.ts`, watchlist modals, `scripts/pre-push.sh`, new `lib/watchlists/keyword-query.ts` + keyword smokes/migration, `docs/architecture.html`, `docs/apis.html`). **NOT pushed** (a push currently fails the api-docs gate until the concurrent schema work + apis.html land). Live on dev :4101 via HMR; **prod :3101 NOT rebuilt**.

- **Date:** 2026-06-01 (**Profile Projects gain start/end dates → rendered in resumes; schema migrated BOTH dbs; COMMITTED scoped; NOT pushed; prod NOT rebuilt**). User: projects have no dates — "no reason not to put that I worked on SEB only 2020–2022"; wants a **start date**, and **no end date ⇒ "Present"**. Implemented end-to-end mirroring the existing WorkRole/Education date machinery. **Schema:** `Project.startDate DateTime?` + `endDate DateTime?` — **nullable like Education** (not WorkRole's required `startDate`) so the additive migration over existing date-less rows needs no backfill. Migration `20260601120000_add_project_dates` (hand-authored additive `ALTER TABLE "Project" ADD COLUMN` ×2; used `migrate deploy` not `migrate dev` — deterministic, no shadow-DB reset risk on the mailbox-equiv prod.db) applied to **BOTH dev.db + prod.db** (prod.db backed up first → `~/backups/mission-control/prod.db.pre-project-dates-20260601T003035`; each tier's web+scheduler momentarily stopped to dodge the PM2 SQLite lock, migrated, `prisma generate`, restarted; dev :4101 + prod :3101 both back to 200). **Zod** (`lib/schemas/profile.ts`): added `startDate`/`endDate` to `ProjectSchema` (**`.nullable().optional()`** — same `scratchpad` precedent so pre-migration snapshots/fixtures validate without churn) + `ProjectPostSchema` + `ProjectPatchSchema`. **Repo** (`lib/repositories/profile.ts`): `ProjectCreateInput`/`ProjectUpdateInput` + create/update payloads (copied Education's). **Route** (`app/api/profile/projects/route.ts`): POST/PATCH ISO-string→`Date` conversion (PATCH preserves the `undefined`-passthrough). **UI** (`components/ui/ProjectRow.tsx`): Start/End `<input type=date>` rows w/ `dateToInput`/`inputToIso` helpers + `placeholder="Present"` (mirrors `EducationRow`). **Render** (`lib/resumes/templates/ats-plain.tsx`): per user's pick (AskUserQuestion — option C), **date + links share the right column on one line** → `"<start> – <end|Present> · Repo · Website"`; date renders **only when `startDate` present** (dateless legacy projects emit links-only, NO `"? – Present"` placeholder). New fields auto-thread through select/compose/canon-selection/specialize because the template consumes the full `ProjectWire`. **Verified:** my files `tsc` clean (the 2 `CanonsCard.tsx` `regenId` errors are a CONCURRENT job's mid-flight break, not mine — proven by grep + scoped tsc); resume-render-smoke PASS (valid PDF); resume-select 14/14; canon-manual-selection 34/34; profile-merge 41/41; a throwaway HTML-render check with dated projects **5/5** (Present, full range, dateless backward-compat — then deleted). `profile-repo-smoke` 18/19: the one failure ("Bullet JSON roundtrip") is **PRE-EXISTING + unrelated** — a stale fixture (`sampleBullets` omits `autoTags`/`removedTags`/`pinnedTags` that `normalizeBullet` stamps); proven by stashing my 2 lib files and re-running (still fails); it's **not in the `pre-push.sh` SUITES** so it doesn't gate. **COMMITTED scoped** to my 6 files + the migration + this doc; **EXCLUDED heavy concurrent churn** (`app/api/resumes/route.ts`, `CanonsCard.tsx` [tsc-broken], `lib/resumes/labels.ts`, `lib/canons/keywords.ts`, `lib/fetchers/{indeed,linkedin}-fetcher.ts`, watchlist modals, `scripts/pre-push.sh`, new `lib/watchlists/keyword-query.ts` + keyword smokes/migration script, `docs/architecture.html`). **NOT pushed.** **Live on dev :4101** via HMR. **Prod :3101 is schema-ready** (migrated prod.db + regenerated client = consistent, no breakage) **but does NOT expose the feature** — needs `npm run build` + `pm2 restart mission-control`, currently **blocked** by the concurrent `CanonsCard.tsx` tsc error (tree won't compile); coordinate the deploy with the user once the tree is green. **Scope note:** the LLM resume-importer (`lib/profile/import-llm.ts` / `synthesize.ts`) does **NOT** extract project dates yet (separate enhancement — manual entry via the Profile dash covers the SEB case).

- **Date:** 2026-06-01 (**Resume HTML preview — authoritative page-fit banner + page sheet/boundary; "Re-render" → HTML preview + disabled-when-no-selection; COMMITTED scoped on top of `a8565f5`; NOT pushed; prod NOT rebuilt**). Follow-ups to the prior entry's preview work. User: (1) the HTML preview is nice but **loses the one-page-fit signal** — only the builder's line-count estimate, which is sometimes wrong; (2) the CanonsCard **"Re-render" button now just re-downloads** — since we keep the HTML it should open the preview (same content); (3) "Re-render" **should grey out when no resume/selection exists** like Download/Specialize do. **Shipped (5 files):** **(1)** `lib/resumes/render-html.ts` — new **`decorateResumePreview(html, pageCount)`** wraps the bare resume HTML with screen-only chrome: a sticky **page-fit banner** from the REAL PDF count (green "✓ Fits on one page" / amber "⚠ N pages" / neutral when unknown) + a Letter **`.mc-sheet`** (8.5×11, 0.5in padding) with a dashed **page-1 boundary line** at `calc(0.5in + 10in)`. Geometry mirrors `render-pdf.ts` exactly (10in printable height, 6.3in text column ⇒ faithful wrapping; only the page-1 boundary is exact, later breaks drift in a continuous preview — hence the banner carries the count). `@media print` strips the chrome; PDF/DOCX artifacts untouched. **(2)** `app/api/canons/[id]/preview/route.ts` — reads **`?pages=N`** (exact count the caller got from `X-Resume-Pages`) → `decorateResumePreview`; absent ⇒ neutral banner (no PDF render in the preview — kept instant). **(3)** overlay `handleGenerate` + CanonsCard `handleRegenerate` both append `?pages=` to the preview URL. **(4)** `handleRegenerate` now opens the **HTML preview** (links work, page-fit banner) instead of `window.open(pdfBlob)`; still POSTs to persist a fresh version. **(5)** Re-render button `disabled={anyRegenerating || !canon.hasSelection}` + conditional title — gated on **`hasSelection`** (its true precondition: verbatim re-render replays the saved selection; this also correctly disables legacy auto-pipeline canons that have a `currentResumeId` but no selection, which would've errored on click). Threaded **`hasSelection`** onto the CLIENT `CanonWireSchema` in `lib/api-client.ts` (`.catch(false)`) — it was on the server `CanonSchema` + `serializeCanon` but stripped on client parse, so `canon.hasSelection` was `undefined` in the card. **Verified:** `tsc` clean; resume-render-smoke pass; canon-manual-selection-smoke 34/34; api-docs in sync (GET surface unchanged); `decorateResumePreview` 3 banner states correct; real-data decorated render of a dev.db canon (body→fitbar→sheet→page order, proper close, anchors target=_blank). **COMMITTED scoped** to the 5 files + this doc; **EXCLUDED** concurrent `app/api/resumes/route.ts` (RAH-12) + `docs/architecture.html`. **NOT pushed.** Live on dev :4101 via HMR; **prod NOT rebuilt** (needs `npm run build` + `pm2 restart mission-control`).

- **Date:** 2026-05-31 (**Resume links — named "Repo"/"Website" anchors + render the missing project page (`liveUrl`) + DOCX hyperlink fix + new-tab via HTML preview; COMMITTED scoped; NOT pushed; prod NOT rebuilt**). User: resumes printed the **full repo URL** as the anchor text instead of a short label, were **missing project pages** (the non-repo `liveUrl` — Seb's & others'), and clicking a link **opened in the same tab / closed the resume**. **Root cause** in `lib/resumes/templates/ats-plain.tsx` projects section: `<a href={repoUrl}>{repoUrl}</a>` (raw URL as text) and **`liveUrl` was never rendered at all**. **Fixes (one template, both PDF+DOCX consume it):** repoUrl→**"Repo"** anchor, liveUrl→**"Website"** anchor (filtered through `isValidUrl`, `·`-separated, href=full URL); added `.entity-line .right a` link color. **Latent DOCX bug found + fixed:** `html-to-docx` does NOT descend into `<span>` for `<a>` (verified by probe — header links worked only because their `<a>` is a direct child of `div.meta`), so the old repo link was **silently dropped from every DOCX**; switched the project links' `<span className="right">`→**`<div className="right">`** (still right-aligns as a flex item) → DOCX now emits real hyperlinks. **New-tab:** added `target="_blank" rel="noopener noreferrer"` to ALL resume anchors (project + header). **Empirically confirmed** (zlib-decompressed the puppeteer PDF) that Chromium encodes resume links as plain `/S /URI` annotations with **no `/NewWindow` flag** — so a PDF-in-Chrome ignores target=_blank (PDFium limitation, not fixable from the doc). Per user's pick, added an **HTML preview** that the builder opens on Generate: NEW `GET /api/canons/[id]/preview` (+`meta.ts`) renders the saved selection **verbatim** (mirrors `generateCanonManualResume`'s no-rewrite path: `resolveSelection`→filter `sectionsOff`→`resolveExtras`→`composeResumeProps`→render) as `text/html`; NEW `lib/resumes/render-html.ts` (shares the `ResumeDoc` template w/ pdf/docx); `ResumeBuilderOverlay.handleGenerate` now opens `/api/canons/${id}/preview` in a new tab for PDF (links open their own tab, never close the resume) instead of the raw PDF blob — PDF/DOCX still persist + are downloadable from the Generated Resumes list. **Verified:** `tsc` clean; `apis.html` regen 65→**66 routes** + api-docs-smoke in sync; resume-render-smoke pass (fixture given a real `liveUrl`); canon-manual-selection-smoke **34/34**; preview route returns 401 unauth on dev :4101 (loads); **real-data render** of the "Avionics Engineering Intern" canon → 5 named anchors all `target=_blank`, incl. a "Website" → berkeleyse.org (the previously-missing project page). **COMMITTED scoped** to 7 files (template, render-html, preview route+meta, overlay, apis.html, render-smoke, this doc); **EXCLUDED** concurrent job's `app/api/resumes/route.ts` (RAH-12 rate-limit gen/render split) + `docs/architecture.html`. **NOT pushed.** Live on dev :4101 via HMR; **prod (:3101) NOT rebuilt** — needs `npm run build` + `pm2 restart mission-control` to ship the new route/overlay/template. _(PDF-in-Chrome link-tab caveat: the downloaded/sent PDF still same-tabs links in Chrome's viewer — unavoidable; the on-screen builder view is HTML where it's fixed.)_

- **Date:** 2026-05-31 (**Resume-builder design doc ARCHIVED (feature shipped); refs swept; CLAUDE.md pointer added; pushed**). Moved `docs/resume-manual-builder.html` → **`docs/archive/resume-manual-builder.html`** (`git mv`) now that P1–P4 are live. Fixed the doc's `./assets/style.css` → `../assets/style.css` (archive is one dir deeper) and added a **`✅ SHIPPED 2026-05-31`** banner (summary + 3 spec divergences recorded: lightweight `hasSelection` flag vs full selection, self-contained handler vs in-route branch, the opt-in-rewrite match-enrichment fix; + verification). Swept all **13 code/script comment refs** (`app/api/resumes/{route,specialize}`, `app/api/canons/[id]/selection/route`, `ResumeBuilderOverlay`, `pre-push.sh`, the smoke, `lib/{repositories/canons,schemas/canons,canons/selection}`) `docs/resume-manual-builder.html`→`docs/archive/...` (historical `next_steps` log entries intentionally LEFT — accurate when written; the path was current then). Added a **CLAUDE.md** note under 'Resume-gen relevance pipeline': **canon generation is the manual exception** (manual-only via `generateCanonManualResume`; the scoring pipeline runs only for url/text/application), with the archived-doc link. `tsc` clean (comment-only code changes). `docs/architecture.html` (M) still a concurrent job's uncommitted work — EXCLUDED. Pushed to `origin/main`.

- **Date:** 2026-05-31 (**Resume builder — P4.2 PROD-DEPLOYED + PUSHING to origin/main; FEATURE COMPLETE (P1–P4 all done), LIVE on dev + prod**). User OK'd the deploy. Ran `npm run build` (BUILD_ID `iZ9Sz83W7Vj1AOXkXS4rM`, clean — new `app/api/canons/[id]/selection/route.js` compiled in) + `pm2 restart mission-control`. **Prod (:3101) verified:** home 200; `GET /api/canons/[id]/selection` + `POST /api/resumes` load (401 unauth). The Phase-1 migration already hit prod.db, so only the compiled routes/UI needed shipping; **scheduler NOT restarted** (doesn't use the feature + already on the new client from the migration restart). **Pushing local `main` → `origin/main`** (8 commits ahead: `925962d`/`7fa4f17`/`b4a6f06`/`8f909de`/`a96d7aa` design + `7673c1d`/`575baf2`/`43999d3` impl) — the pre-push hermetic gate (`scripts/pre-push.sh`, all smokes incl. the new `canon-manual-selection-smoke` 34/34) runs on push. **All 20 doc task boxes (P1–P4.2) checked; the manual per-Canon resume builder is complete + live.** `docs/architecture.html` (M) remains a concurrent job's uncommitted work — EXCLUDED. **Only remaining (user-side): a visual click-through of the overlay on dev/prod to confirm UX** (routes use `requireSession`, so it couldn't be driven headlessly).

- **Date:** 2026-05-31 (**Resume builder — PHASE 3 (UI) + P4.1 (smoke) SHIPPED via 2 parallel bg agents; tsc clean, smoke 34/34; COMMITTED; only P4.2 prod-deploy left; NOT pushed**). Fanned out 2 background subagents at the Phase-3 boundary (API contract locked) on non-overlapping files. **P3.1 (agent):** NEW `components/overlays/ResumeBuilderOverlay.tsx` (860 lines) — Window-class portal overlay; loads profile + `getSelection` (+ prior resume for pre-fill); editable state = `entities: Map<id,{kind,bulletIds:Set}>` + extras Sets + `sectionsOff`; sections Experience/Projects/Education (entity + per-bullet checkboxes) / Skills(by category)/Languages/Interests with per-section on/off; **buildSelection** computes `excluded` = profile entity ids NOT checked (OQ5=B); **Save** PUTs + invalidates; **Generate** saves-first then POSTs `/api/resumes {posting:{canonId}, options:{format,rewrite,tagline}}`, opens PDF/downloads DOCX, reads `X-Resume-Pages` exact count; **live page estimate** heuristic (~48 lines/page) '~N (est.)' + amber >1, exact appended post-Generate (OQ14=A). **P3.2 (me):** `CanonsCard.tsx` — purple **'Edit & generate'** opens the overlay; old auto-'Regenerate' demoted to outline **'Re-render'** (re-renders the saved selection verbatim — still valid since the route reads `Canon.selection`). `lib/api-client.ts` — `api.canons.getSelection`/`saveSelection` + `queryKeys.canonSelection` (imports server `CanonSelectionSchema`). **P3.3 (agent):** first-open seeding priority saved→prior-resume(grouped by sourceId)→empty; extras default all-checked. **P4.1 (agent):** NEW `scripts/tests/hermetic/canon-manual-selection-smoke.ts` — **34 assertions all pass** (resolveSelection order/chosen/deleted-dropped/unscored/keyword-enriched/sourceLabel; resolveExtras intersect; repo round-trip incl. hasSelection flip, non-owner false, corrupt-JSON→null), wired into `pre-push.sh` SUITES, 0 orphan rows; agent found NO bugs. **Verified:** `tsc --noEmit` clean (full tree — both agents' files + my wiring integrate); smoke re-run 34/34; reviewed the overlay's excluded/save-before-generate/seeding logic (correct); dev home 200. **NOT yet done:** the visual/authed click-through of the overlay (needs a browser session — routes use `requireSession`, localhost doesn't bypass) — the only un-verified layer; logic+integration are solid. **COMMITTED scoped** to overlay + CanonsCard + api-client + smoke + `pre-push.sh` + doc; `docs/architecture.html` (M, concurrent) EXCLUDED. **NOT pushed. ONLY REMAINING: P4.2** — prod build (`npm run build`) + `pm2 restart mission-control` + push (coordinate w/ user); the migration already hit prod.db so prod just needs the compiled routes/UI. Suggest the user click-test the overlay on dev :4101 first.

- **Date:** 2026-05-31 (**Resume builder — PHASE 2 SHIPPED (API surface) + tsc clean + routes load on dev; doc P2 boxes checked; COMMITTED; NOT pushed**). **P2.1:** NEW `app/api/canons/[id]/selection/route.ts` GET (parsed selection or null) / PUT (validates `CanonSelectionPutSchema`, saves, broadcasts Canon upsert) + `meta.ts`. **P2.2:** branched canon gen in `app/api/resumes/route.ts` to a **self-contained `generateCanonManualResume` handler** behind an EARLY RETURN — kept the url/text/application auto-pipeline **byte-identical** (zero regression risk). Handler: `resolveSelection(Canon.selection)` → render verbatim (NO one-page prune, OQ9), AI rewrite/tagline opt-in via new `options.rewrite`/`options.tagline` toggles default-off (OQ1/OQ13), exact `X-Resume-Pages` header (OQ14), persists a canonical version + finalizes the canon (mirrors main persist's artifact-rollback). **No auto fallback** (OQ15): canon w/o selection → 'build one first' 400. Removed the now-dead canon branches from the auto path (posting-synth / onePage-default / canonVersion / persist-canonId / finalize — provably null after the early return; non-canon behavior unchanged). **Rewrite-no-op bug caught + fixed:** `rewriteBullets` passes no-match bullets verbatim and `resolveSelection` bullets are unscored → opt-in rewrite would silently no-op; fixed by threading optional `keywords` through `resolveSelection`/`manualBullet` (exported `scoreBullet`) so matches populate against canon keywords ONLY when rewrite is on. Also exported `countPdfPages` (one-page.ts) + added `resolveExtras` (user-picked skills/langs/hobbies ∩ live profile) to selection.ts. **P2.3:** `specialize/route.ts` reads authoritative `Canon.selection` (resolveSelection vs frozen snapshot) when present, else legacy reconstruct path. **P2.4:** `apis.html` regenerated (64→**65 routes**). **Verified:** `tsc --noEmit` clean; all 4 routes return **401 unauth** on dev :4101 (load, no 500/compile errors); dev error log clean. (Authed end-to-end gen deferred to Phase 4 smoke + manual verify.) **COMMITTED scoped** to my Phase-2 files; `docs/architecture.html` (M, concurrent) EXCLUDED. **NOT pushed. NEXT: Phase 3 — `ResumeBuilderOverlay` (plan: FAN OUT a UI subagent against the now-locked API contract) + CanonsCard 'Edit & generate' + api-client; then Phase 4 smoke + ship.**

- **Date:** 2026-05-31 (**Resume builder — PHASE 1 SHIPPED + verified (schema+migration+repo+resolveSelection); doc P1 boxes checked; COMMITTED; NOT pushed**). Began implementation (user gave go). **P1.1 schema+migration:** added `Canon.selection String?` (nullable JSON) → migration `20260601034410_add_canon_selection` (single additive `ALTER TABLE "Canon" ADD COLUMN "selection" TEXT`). Backed up prod.db (`~/backups/mission-control/prod.db.pre-canon-selection-*`), then applied to **both dev.db + prod.db** via `migrate deploy` — hit "database is locked" (PM2 procs hold the SQLite conn), so **stopped each tier's web+scheduler momentarily, migrated, restarted** (prod blip ~3s; prod health re-confirmed HTTP 200). `prisma generate` ran; `selection TEXT` column verified on both DBs. **P1.2 Zod+repo:** `lib/schemas/canons.ts` — new `CanonSelectionSchema` (binary `entities` record keyed entityId→{kind,bulletIds} + `excluded[]` + `extras` + `sectionOrder`/`sectionsOff`) + `CanonSelectionPutSchema`; threaded a lightweight **`hasSelection: boolean`** onto `CanonSchema` (NOT the full selection — keeps the canon-list payload light; full selection loads via the dedicated route). `lib/repositories/canons.ts` — `serializeCanon` sets `hasSelection = row.selection != null`; new `getCanonSelection` (lenient parse, corrupt→null) + `saveCanonSelection`. **P1.3 resolve:** NEW `lib/canons/selection.ts:resolveSelection(profile, selection) → ResumeSelection` — included entities in **profile order**, chosen bullets only (profile bullet order; `bulletIds` is a membership set in v1, OQ8), deleted ids dropped (mirrors `reconstructSelection`); manual bullets are **unscored** (score 0, empty matches, locked=false) so downstream rewrite/compose/persist are identical. **Verified:** `tsc --noEmit` clean; a throwaway runtime check **7/7 PASS** (guard excluded, ML role incl., chosen+profile-ordered bullets w/ deleted dropped, project incl., deleted entity dropped, unscored, correct sourceLabel) then removed. **Doc:** P1 + all P1.x checkboxes checked; P1.2.1 text corrected to the `hasSelection` flag. **COMMITTED scoped** to my files (schema, migration, `lib/schemas/canons.ts`, `lib/repositories/canons.ts`, new `lib/canons/selection.ts`, the doc) — `docs/architecture.html` (M, concurrent job) EXCLUDED. **NOT pushed. NEXT: Phase 2 — `app/api/canons/[id]/selection` GET/PUT (P2.1) + branch `app/api/resumes/route.ts` for canon manual gen incl. rewrite/tagline toggles + page-count header + no-auto-fallback (P2.2) + specialize→`Canon.selection` (P2.3) + regen apis.html (P2.4).**

- **Date:** 2026-05-31 (**Resume-builder design — Tasks-vs-OQ audit → MANUAL-ONLY canon gen (new OQ15) + 4 task gaps patched; doc COMMITTED; design fully consistent; NOT pushed**). User asked to verify the Tasks section against the OQ decisions. Audit found **1 material contradiction + 4 task gaps + polish**: P2.2/risks/rollback kept the keyword auto-pipeline as a **canon fallback**, contradicting OQ4's "retire auto." **User chose A → canon generation is MANUAL-ONLY** (recorded as **OQ15 ✓**): a canon with no `selection` can't auto-Regenerate (route returns "build one first"); "Edit & generate" always routes through the builder; the keyword auto-pipeline survives ONLY in the one-off `GenerateResumeCard` (OQ12); existing canons keep their last resume until re-curated. **Patched gaps:** (a) NEW task **P2.3** — point `app/api/resumes/specialize/route.ts` at the authoritative `Canon.selection` (was in the touch-list but missing from Tasks; would drift from the curated set); (b) P2.2 + touch-list now add `options.rewrite`/`options.tagline` toggles to the resumes POST Zod schema (OQ1) + return the exact page-count header (OQ14); (c) overlay P3.1 computes the `excluded` snapshot (all entities − checked) at Save (OQ5=B); (d) P3.3 now covers the **new-canon empty** initial state (was only existing-canon pre-fill); (e) P4.1 assertions tightened (`excluded` round-trip, pruner-not-applied, no-selection-errors); (f) `sectionOrder` noted fixed-default in v1 (OQ8). Regen-apis renumbered P2.3→**P2.4**. **Validated:** OQ1–OQ15 all ✓ (15 `callout tip` / 0 open), task ids clean P1…P4.2 (20 checkboxes), 0 lingering "auto fallback" contradictions, ToC↔ids 1:1, `<details>` 26/26, no inline `<style>`, no literal `\n` in Mermaid. **COMMITTED scoped to the doc + this entry; concurrent `docs/architecture.html` (M) EXCLUDED. NOT pushed. Design is LOCKED + internally consistent — ready for Phase 1 (P1.1 `Canon.selection String?` + `scripts/backup-db.sh` + migrate dev.db/prod.db).**

- **Date:** 2026-05-31 (**Resume manual-builder design — ALL 14 OQs RESOLVED; design LOCKED; doc COMMITTED; NO code yet; NOT pushed**). Final fork **OQ14 = A** (live page indicator = fast in-browser HTML/height estimate, exact-at-Generate; documented fallback to B = true PDF render-on-edit if the estimate isn't accurate enough in practice). `docs/resume-manual-builder.html` now has **14 ✓ decided/defaulted OQs, 0 open** (validated: 14 `callout tip` / 0 `callout`, `<details>` 25/25, OQ1–OQ14). **Design summary (locked):** a manual per-Canon resume builder replacing the keyword auto-selector — new `Canon.selection` JSON column (binary `entities` map with per-entity `bulletIds` + an `excluded` forward-compat id-list + `extras` + `sectionOrder`/`sectionsOff`); Window-class overlay launched from the Canon row; AI rewrite + tagline are opt-in toggles (default off → verbatim); **NO Suggest / auto-selection in v1** (`selectBullets` retired from the selection path, spine/one-page-prune rules don't run); live page indicator, user owns length; reuses render / `specialize` / persistence / staleness / versioning unchanged. **4-phase plan (P1–P4) in the doc, ready to execute:** P1 = schema (`Canon.selection`) + Zod (`lib/schemas/canons.ts`) + repo + NEW `lib/canons/selection.ts:resolveSelection`; P2 = NEW `app/api/canons/[id]/selection` GET/PUT + branch in `app/api/resumes/route.ts` + regen `apis.html`; P3 = NEW `components/overlays/ResumeBuilderOverlay.tsx` + CanonsCard "Edit & generate" entry + first-open pre-fill from last resume; P4 = hermetic smoke + tsc/build/deploy. **COMMITTED scoped to the doc + this entry; concurrent `docs/architecture.html` (M) EXCLUDED. NOT pushed. NEXT: begin Phase 1 — P1.1 adds `Canon.selection String?` and migrates BOTH dev.db + prod.db (prod.db is mailbox-equivalent + backed up — run `scripts/backup-db.sh` first).**

- **Date:** 2026-05-31 (**Resume-builder design — OQ5 + OQ9-split RESOLVED; only OQ14 (live-preview fidelity) left open; doc updated + COMMITTED; NOT pushed**). Closed the last forks with the user. **OQ5 = B** — binary selection **+ a persisted `excluded` id-list** (snapshot of present-but-unchecked entities at save time); the builder UI stays binary checkboxes and the list is **INERT in v1** (forward-compat for a possible future LLM Suggest only — it'd respect the list and only ever propose entities added to the profile AFTER the last save; precise untouched-vs-excluded semantics deferred to whenever Suggest is built). **OQ9's fidelity sub-point promoted to its own OQ14** (live page indicator: fast in-browser HTML/height **estimate** + exact-at-Generate, vs true **PDF render-on-edit**; lean = fast estimate; gates the P3.1 overlay UI, NOT Phase 1). OQ9 itself now fully ✓ Decided (drop the one-page auto-pruner + show the live indicator). **Net OQ state: 13 decided/defaulted + 1 open (OQ14); Phase 1 is UNBLOCKED.** Doc `docs/resume-manual-builder.html` updated (made `excluded` a real data-model field, revised OQ5/OQ9 callouts → `tip`, added OQ14, fixed OpenQ-intro/Tasks-intro/P3.1 refs); validated (OQ1–OQ14, **13 `callout tip` + 1 `callout`**, ToC↔ids 1:1, `<details>` 25/25, `excluded` field real, 0 stray three-state/`suggestPrefill` refs, no inline `<style>`, no literal `\n` in Mermaid). **COMMITTED scoped to the doc + this entry; concurrent `docs/architecture.html` (M) still EXCLUDED. NOT pushed. NEXT: settle OQ14 (or accept the fast-estimate lean) → begin Phase 1 (P1.1 schema `Canon.selection String?` + migrate dev.db/prod.db).**

- **Date:** 2026-05-31 (**Resume-builder design — OQ resolution round 2; NO-SUGGEST pivot REVISES OQ4 + collapses OQ5/OQ2/OQ11; doc rewritten + COMMITTED; NOT pushed**). Continued resolving the open OQs with the user (supersedes the OQ framing in the entry below). **Decisions made:** OQ10 = Window-class **overlay** from the Canon row; OQ9 = **drop the one-page auto-pruner** + show a **live page indicator** (sub-point still open: true PDF render-on-edit vs fast in-browser HTML estimate confirmed-exact at Generate). **Big pivot — expanding OQ11 reopened OQ4:** user chose **NO "Suggest" button in v1** → the keyword `selectBullets` scorer is **RETIRED from the selection path entirely** (not demoted to a pre-fill), because a keyword-overlap Suggest inherits the exact flaw being fixed; any future suggestion would be a **semantic LLM relevance pass**, deferred. **Knock-on:** with nothing automated mutating the selection, the 3-state entity model (always/auto/never) **collapses to a plain binary checkbox** — so OQ2's "lock" affordance drops out, and **OQ5 is reframed to pure-binary vs binary+forward-compat `excluded` id-list** (lean binary; this is now the ONLY remaining Phase-1-gating OQ). OQ11 now moot. **Defaulted to leans (flagged to user as revisitable):** OQ6 (single JSON `Canon.selection` column), OQ7 (per-bullet — user-implied), OQ8 (inherit profile order, defer drag), OQ12 (keep the one-off `GenerateResumeCard`), OQ13 (keep no-match rewrite passthrough). **Doc `docs/resume-manual-builder.html` rewritten to match:** Suggest stripped from goals/Mermaid/touch-list/tasks (dropped `suggestPrefill` helper + its P1.3.2 task), binary data-model JSON (no `state` field), live-preview moved from non-goal→goal + a new "no one-page guardrail" risk row, all OQ1–OQ13 callouts revised (now **11 ✓decided/defaulted + 2 open = OQ5, OQ9**). Validated (ToC↔ids 1:1, `<details>` 25/25 balanced, 11 `callout tip` + 2 `callout`, 0 stray `suggestPrefill`, no inline `<style>`, no literal `\n` in Mermaid). **REMAINING before any Phase-1 code: OQ5 final nod (pure binary vs forward-compat `excluded` list) + OQ9 fidelity sub-point.** **COMMITTED scoped to `docs/resume-manual-builder.html` + this entry ONLY; concurrent `docs/architecture.html` (M) EXCLUDED. NOT pushed.**

- **Date:** 2026-05-31 (**Resume-generator redesign — DESIGN DOC `docs/resume-manual-builder.html` written + 4 core forks decided with the user; COMMITTED scoped; NO code yet, NOT pushed**). User: "the resume generator isn't really that good — no reason my security-guard experience should be on an ML/AI Engineering resume." Root cause confirmed in code: `lib/resumes/select.ts:scoreBullet` selects bullets by **keyword-overlap count** (`TAG_WEIGHT=2`/`SUBSTRING_WEIGHT=1`) — a fundamental ceiling that can't tell domain-relevant from irrelevant (a guard bullet matches soft ML words like *communication/data/reporting*), and the **work-role spine** (`pickWorkRoleSpineIndex`+`dropZeroScoreEntities`) + **all-education-kept** rules force the latest job in at zero score. **Direction (user's): a MANUAL per-Canon selector** — show the whole profile, hand-pick bullets, lock entities. **Key architectural fit:** the Canon already owns a reusable resume whose fixed selection lives in `GeneratedResume.selections` (flat JSON), and `lib/canons/specialize.ts:reconstructSelection` already rebuilds a nested `ResumeSelection` from stored rows + a frozen profile snapshot (dropping deleted entities). So the redesign = **replace the Canon's "Regenerate" auto-pipeline with a hand-edited selection**; reuse render/specialize/persist/staleness/versioning verbatim. **4 forks DECIDED (AskUserQuestion):** (OQ1) manual selection core, **AI rewrite + tagline opt-in toggles, default off** (verbatim render); (OQ2) **per-entity lock** (🔒 always / out), NOT per-section-type; (OQ3) selection **saved per-Canon**, re-renderable ("ML resume" vs "Security resume" = separate selections); (OQ4) old scoring pipeline survives as a **"Suggest from posting"** pre-fill button (a guess you edit, not the decider). **Proposed data model:** new nullable `Canon.selection` JSON column — `{version, sectionOrder, sectionsOff[], entities:{<id>:{kind,state,bulletIds[]}}, extras}` with per-entity `state ∈ {always, auto, never}` (the `never` state is the structural fix — `selectBullets`/spine/Suggest can't re-add a guard job set to `never`). **9 OPEN OQs (OQ5–OQ13) in the doc, leans recorded, NOT yet resolved with user — NO code until they are; OQ5/OQ6/OQ7 gate Phase 1 (data model):** OQ5 entity-state cardinality (lean 3-state always/auto/never), OQ6 storage (lean single JSON column), OQ7 bullet granularity (lean per-bullet checkboxes), OQ8 ordering (lean inherit profile order, defer drag), OQ9 length (lean drop one-page auto-prune in manual mode + post-render page count), OQ10 UI surface (lean Window-class overlay from the Canon row), OQ11 Suggest input (lean both canon-keywords + pasted posting), OQ12 fate of the one-off `GenerateResumeCard` (lean keep for now), OQ13 rewrite scope when on (lean keep today's no-match passthrough). **Doc follows the `_template.design-doc.html` archetype** (context → design+Mermaid+touch-list+alternatives → risks&rollback → Open Questions → 4-phase Tasks P1–P4 with ≥2 nesting levels); validated (ToC↔ids 1:1, `<details>` 26/26 balanced, no inline `<style>`, no literal `\n` in Mermaid, 4 decided `callout tip` + 9 open `callout`). **Touch list (for when code starts):** `prisma/schema.prisma` (+`Canon.selection`, migrate dev.db+prod.db), `lib/schemas/canons.ts`, `lib/repositories/canons.ts`, NEW `lib/canons/selection.ts` (`resolveSelection`+`suggestPrefill`), NEW `app/api/canons/[id]/selection/route.ts`, branch in `app/api/resumes/route.ts`, NEW `components/overlays/ResumeBuilderOverlay.tsx`, `CanonsCard.tsx`, regen `apis.html`, hermetic smoke. **COMMITTED scoped to `docs/resume-manual-builder.html` + this entry ONLY** — concurrent other-job `docs/architecture.html` (M) EXCLUDED. **NOT pushed. NEXT: resolve OQ5–OQ13 with the user (start with the data-model trio OQ5/6/7), then begin Phase 1.**

- **Date:** 2026-05-31 (**PUSH to `origin/main` — flushed the accumulated unpushed backlog**). `origin/main` was 4 commits behind local: `d6e95ed` (single-line SCH/WEB log badge), `806364e` (AMD RSS fix), `dee32a2` (job fetchers → `loggedFetch`), `843c5ea` (this doc's AMD entry). Ran `git push` — the pre-push hermetic gate (`scripts/pre-push.sh`, every file under `scripts/tests/hermetic/`) ran and passed. **State-reconciliation note for the next session:** the "NOT pushed" tags on the prior 2026-05-31 entries below (AMD/loggedFetch, side-canon last-one-out `14b1f10`, NegFilter leak `43f30d9`, scheduler structured-logs, the arXiv rate-limit 4-commit set) are now **superseded — all of that work is on `origin/main`** as of this push. (The older entries' tags were never flipped in their own sessions; this entry is the authoritative flip rather than rewriting each line.) Working tree clean before + after.

- **Date:** 2026-05-31 (**AMD news fetcher 404 fix + ALL job fetchers routed through `loggedFetch` for health visibility — 2 commits (`806364e`, `dee32a2`); dev scheduler restarted live; prod scheduler restart DONE; NOT pushed**). User flagged the AMD fetcher failing, then asked why only Greenhouse (not the other ATSs) showed up in the dev scheduler. **(1) AMD fix (`806364e`):** `lib/companies/amd.ts` RSS URL `ir.amd.com/rss/PressRelease` now 404s (CloudFront "Page Not Found" HTML → `fetchRSS` parsed 0 items → `ScraperBrokenError` every run); AMD's own 404 page advertised the live feed in its `<link rel=alternate>` → `ir.amd.com/news-events/press-releases/rss` (HTTP 200, valid RSS). Verified the real adapter fetch returns 10 articles. **(2) "why only Greenhouse" — diagnosed, NOT a bug:** `scheduler/jobs/job-watcher.ts:runDueWatchlists` runs every 10-min tick but only fires watchlists whose `lastRunAt` is older than per-watchlist `scheduleMinutes` (dev: almost all **240min/4h**; Anthropic+Rocket Lab greenhouse **60min/1h**; linkedin ~233–251min). At the moment the user looked, **0 were due**; every ATS had run in the prior 1–3.5h. Greenhouse over-represented because it has **23 watchlists** vs ashby/lever/workday 4 + clearcompany 1, plus 2 hourly boards. **(3) Root observability gap (the real find):** ALL 12 job fetchers used **raw `fetch()`** for the discovery (board-listing) call → no `[EXTERNAL API]` line, no fetcher-health row; only the liveness probes (which use `loggedFetch`) were visible, so the Fetcher Health card/log viewer reflected probing volume, not fetch health — and hid breakages (the AMD-class 200-with-changed-shape would read `ok`). **Fix (`dee32a2`, 12 files):** `greenhouse, lever, ashby, workday, smartrecruiters, workable, recruitee, personio, clearcompany, linkedin, indeed, careers-page` all swap `fetch()`→`loggedFetch(url, init, {record:false})` + record exactly ONE outcome per HTTP call (mirrors rss/scrape-fetcher): **error** (HTTP non-2xx / throw / timeout / 429 / 403), **broken** (200 but unparseable — Zod-shape fail, missing XML root, Cloudflare wall), **ok** (fetched+parsed; empty result stays `ok` for the scrapers/careers-page since an empty search is legit). Paginated fetchers (smartrecruiters/clearcompany/workday) record **per page**; partial-crawl semantics preserved. **Observability-only — no fetch contract changed.** **Verified:** `tsc` clean; hermetic `fetcher-unit 72/72`, `fetcher-partial 6/6`, `fetcher-health-store 26/26` (isolated `FETCHER_HEALTH_PATH`); live direct-call test (greenhouse/lever emitted `[EXTERNAL API]` + wrote `ok`/`error` rows); **live on BOTH schedulers** (`pm2 restart mission-control-scheduler-dev` then `-prod --update-env` — schedulers run `scheduler/index.ts` via `tsx` source, so a restart is the whole deploy; dev post-restart tick already logs the LinkedIn **discovery** search URL, previously invisible). Prod web card already reads the store so **NO prod web rebuild needed**. **NOT pushed** (a push runs the full pre-push hermetic gate). **Unrelated pre-existing issue spotted at dev-scheduler boot:** `[llm-cache] disabled — table init failed; running UNCACHED: no such column: value` — the cross-tier LLM dedup cache (`data/llm-cache.db`) is on a stale schema (best-effort so nothing breaks, but cross-tier LLM dedup is OFF until the file's schema is refreshed); flagged to user, NOT addressed.

- **Date:** 2026-05-31 (**Side-canon "last-one-out" cleanup on watchlist delete — IMPLEMENTED + COMMITTED `14b1f10` scoped; this doc bump is the immediately-following 2nd commit; dev orphan canon purged; NOT pushed; prod needs rebuild**). User deleted the "Security Officer — Los Angeles" SIDE watchlist but its side **Canon** survived in the Canons card. Root cause: `Watchlist.canonId` is a one-way FK (watchlist→canon, seeded by `scripts/seed-side-canons.ts`); the watchlist DELETE route was a bare `prisma.watchlist.delete()` that never touches the canon it points at. **Decision (AskUserQuestion): cascade "last-one-out"** — deleting a watchlist retires its linked canon ONLY when no other watchlist still feeds it (the LinkedIn+Indeed Find-Roles pair shares ONE canon by slug, so it survives until both halves are gone); **side-only** (hand-made career canons never auto-delete); **resumes preserved**. **Shipped:** NEW `lib/watchlists/cascade-canon.ts:deleteOrphanedSideCanon(userId, canonId)` (loads canon, gates on `track==="side"`, `watchlist.count({canonId})===0` post-delete → `deleteCanon`; race-safe under the concurrent group-delete since each request counts AFTER its own row is gone, so the last finisher always sees 0); `app/api/watchlists/[id]/route.ts` DELETE captures the owned row, runs the cleanup best-effort (inner try/catch — never fails the delete), broadcasts a `Canon` delete event for live Canons-card refresh; NEW hermetic `watchlist-delete-canon-cascade-smoke.ts` (**9/9** — last-one-out deletes canon + preserves resume with canonId nulled; sibling keeps canon; career protected; null no-op) wired into `pre-push.sh` SUITES. **Resume-survival (user's 2nd ask) already guaranteed:** `GeneratedResume.canon` / `Application.canon` / `Watchlist.canon` are all `onDelete: SetNull`, and the Generated Resumes list filters by user only — orphaned canon resumes stay listed, and the canon name is snapshotted onto `GeneratedResume.postingTitle` at gen time (`app/api/resumes/route.ts:774`) so the label survives (renders "… · v1" under Canon). No code needed there. **Data cleanup:** purged the orphaned "security officer — Los Angeles" canon from **dev.db** (0 watchlists, 0 resumes, guarded delete). **prod.db NOT touched** — its same-named canon STILL has a live LinkedIn watchlist (prod deletion hasn't happened yet), so deleting it now would orphan a real watchlist; once this ships to prod and that watchlist is deleted via the UI, the cascade auto-retires it. **Verified:** `tsc` clean; cascade smoke 9/9. **COMMITTED `14b1f10` scoped** to the 4 code files (`lib/watchlists/cascade-canon.ts`, `app/api/watchlists/[id]/route.ts`, the smoke, `pre-push.sh`); concurrent multi-job WIP EXCLUDED. Live on dev (:4101) via HMR; **prod (:3101) NOT rebuilt** (route change isn't in the compiled build until `npm run build` + `pm2 restart mission-control`). **NOT pushed.**

- **Date:** 2026-05-31 (**NegFilter-Ingest-Co notification leak — root-caused + fixed + dev orphans cleared; COMMITTED `43f30d9`; NOT pushed**). User reported recurring `NegFilter-Ingest-Co — Operations Coordinator` notifications in the **dev** feed. Root cause: `scripts/tests/hermetic/job-watcher-negative-filter-ingest-smoke.ts` owned its fixture watchlist to `prisma.user.findFirst()` (the **real dev user**) with `notificationMode` defaulting to `"each"`, so `runWatchlist` dispatched a per-posting notification for the surviving (correctly-*unfiltered*) `Operations Coordinator` posting, and its `finally` cleanup deleted the JobPosting + Watchlist but **never the Notification** — one orphan per push (the pre-push gate runs every hermetic smoke against `dev.db`). The negative filter itself was never broken: the `Senior Software Engineer` posting is correctly dropped at ingest, and the leaked notification was the *unfiltered* survivor. **Fix (commit `43f30d9`, 1 file):** synthetic throwaway user (mirrors `notification-negative-filter-smoke`'s hygiene) + `notificationMode: "silent"` (this smoke asserts the ingest DROP, not dispatch) + `notification`/`user` cleanup in `finally`. Verified the smoke green (4/4, exit 0) leaving **0** NegFilter notifications + **0** smoke users; **cleared the 12 accumulated orphans from `dev.db`** (prod.db was unaffected — 0). Sibling `job-watcher-classifier-regression-smoke` also uses the real user but is first-run (per-posting dispatch suppressed) AND already cleans up notifications → 0 orphans, left as-is. **NOT pushed** (a push runs the full pre-push hermetic gate). _(Aside: this session hit a long stretch of badly lagged/garbled tool output — several "results" were stale phantoms, including wrong commit hashes; verified the real state via `git log`/`ls`/`sqlite3` before committing.)_

- **Date:** 2026-05-31 (**Doc-authoring rules — new `_template.design-doc.html` archetype starter; generic template + CLAUDE.md + memory point at it. UNCOMMITTED.**). User asked where archetype-specific doc rules (design-doc section order, OQ-numbering, nested Tasks) should live — `_template.html` or elsewhere. **Decision (AskUserQuestion → "embodied starter"):** keep `docs/_template.html` GENERIC (markup cheat-sheet for *all* docs), and embody the design-doc-only rules in a new **`docs/_template.design-doc.html`** copied when starting a feature/design doc — structure is correct before you type, nothing to remember. Rationale: `_template.html` is copied per-doc so anything in it implicitly means "every doc has this," but the Open-Questions→Tasks tail order does NOT apply to generated references (`apis.html`) or runbooks (`backup-recovery`), so it must not be baked into the generic template. **Shipped (4 files):** (1) **`docs/_template.design-doc.html`** (new, untracked) — pre-stubs the fixed order context → design → risks & rollback → **Open Questions** (OQ1/OQ2 open + an OQ3 `✓ Decided` example callout) → **Tasks** (P1→P1.1→P1.1.1 nested ≥2 levels); same hljs/mermaid/svg-pan-zoom init + `./assets/style.css`; a top HTML comment + a §1 "delete before publishing" callout state the archetype rule. (2) **`docs/_template.html`** (M) — subtitle + Tasks-convention callout now point to the design-doc starter. (3) **`CLAUDE.md`** (M) §"HTML over markdown" — new "Archetype starters" bullet under Template. (4) **memory `feedback_design_doc_structure.md`** (outside repo) — added "Where this is embodied" paragraph. **Validated** the new template: HTML nesting balanced, ToC anchors ↔ section ids 1:1, no inline `<style>`, no literal `\n` in Mermaid. **NOT committed** (review-only). Note: a concurrent job committed `docs/next_steps.md` mid-task and wiped an earlier copy of this entry; my code files were untouched and survived. _(Design-doc archetypes today: `canonical-resumes.html`, `fetcher-health-store.html`, `scheduler-structured-logs.html`, `arxiv-rate-limit-fix.html`; non-design: generated `apis.html`/`llm-calls.html`, runbook `backup-recovery.md`.)_

- **Date:** 2026-05-31 (**Scheduler structured logs — FULLY IMPLEMENTED (P1–P7) + verified on dev; COMMITTED scoped; prod web NOT rebuilt; NOT pushed**). Implemented `docs/scheduler-structured-logs.html` §6 end-to-end (builds the design entry below). **Shipped:** **(P1)** `lib/logger.ts` — `LogEntry` + the `{ts,level,msg}` stdout envelope gain auto-derived `source` (`web`|`scheduler`) + `tier` (`dev`|`prod`) from `MC_SCHEDULER_TIER`/`NODE_ENV` (exported `LOG_SOURCE`/`LOG_TIER`); `scheduler/index.ts` calls `initLogger()` (only the logger, not the rest of `register()`); `lib/prisma.ts` + `lib/cache.ts` mute `[DATABASE]`/`[CACHE]` in the scheduler via `&& !process.env.MC_SCHEDULER_TIER` (web unchanged; `DEBUG_PRISMA`/`DEBUG_VERBOSE_LOG` still the opt-in). **Impl refinement:** kept `[EXTERNAL API]` lines (`lib/external-fetch.ts`, ungated) UNMUTED in the scheduler — they're the crawl signal at ~2/min, exactly the "crawl results" OQ7 wanted; only the per-query/cache firehose is muted. **(P2)** `lib/logs-store.ts` — scheduler-only best-effort SQLite sink `data/logs.db` (WAL, guarded `better-sqlite3`, modeled on `lib/fetcher-health/store.ts`): `recordLogLine`/`readLogsSince`(cursor `id>N`)/`readLogsWindow`/`latestLogId`/`pruneLogs` + test seams; scheduler registers `subscribeToLogs(recordLogLine)` (web never writes — OQ8 scheduler-only sink); `.gitignore` += `data/logs.db*`. **(P3)** `app/api/system/logs/route.ts` — SSE keeps the instant in-memory web ring + adds a ~1s `logs.db` poll (tier-scoped) merging scheduler rows live; initial burst merges web ring + recent store rows. **(P4)** `app/api/system/logs/historical/route.ts` — **fixed the OQ5 per-tier path bug** (`PM2_LOG_PATH` unset → always read the prod web file; now picks `mission-control{,-dev}-out.log` by tier) + merges a `logs.db` window query. **(P5)** `components/views/InternalView.tsx` — **All·Web·Scheduler** segmented filter + per-row SCHED/WEB badge (threaded `source`/`tier` through live+historical state). **(P6)** `scheduler/jobs/logs-prune.ts` (48h, mirrors `fetcher-health-prune`) registered (scheduler now runs **11 jobs**). **(P7)** `scripts/tests/hermetic/logs-store-smoke.ts` (**17/17**) wired into `pre-push.sh`. **Verified:** `tsc` clean; logs-store 17/17; cache-smoke 20/20 + llm-cache 18/18 (shared gate edits behavior-neutral in tests). **Live dev:** scheduler emits `{...,"source":"scheduler","tier":"dev"}` JSON with **0 `[DATABASE]`** lines; `data/logs.db` populated; dev web opened the same file for READ (`"source":"web"`); both log routes 401 (loaded, no 500), home 200. **Observed:** the prod scheduler ALSO restarted off the shared checkout → already structured-logging + writing prod-tier rows. **Did NOT run the full pre-push suite** (two concurrent jobs — the live scheduler + the arXiv job's uncommitted schema/routes — would make it noisy; runs clean on push) or **visually confirm the rendered viewer UI** (needs an authed browser session; data path + compile verified). **COMMITTED scoped** to my 12 files + this entry, EXCLUDING the concurrent arXiv research job (`app/api/research/*`, `prisma/schema.prisma`, migration `20260531235412_add_pick_metadata`, `lib/repositories/selected-papers.ts`, `apis.html`) AND the user's doc-convention work (`docs/_template.html`, `docs/_template.design-doc.html`, `CLAUDE.md`). **Deployment:** both schedulers + dev web (:4101) LIVE with this code (source/tsx + restart); **prod web (:3101) NOT rebuilt** — its compiled viewer won't surface scheduler logs until `npm run build` + `pm2 restart mission-control` (prod scheduler already writing). **NOT pushed.** _(Implements the design entry below.)_

- **Date:** 2026-05-31 (**arXiv rate-limit fix — design doc fully decided (OQ1–OQ10) + ALL 3 layers + P4 docs SHIPPED to dev + PROD (4 commits); cross-tier dedup VERIFIED live in prod; NOT pushed to origin**). Root-caused recurring arXiv 429s: dev+prod share one IP and never share research-fetch state → every paper fetched 2×, the two per-process token buckets burst-collide, AND the weekly review/historical routes re-fetch immutable picks every hour on a 1h TTL. Authored `docs/arxiv-rate-limit-fix.html` (3-layer plan: persist picks+TTL / shared cross-tier cache / shared rate bucket); worked **OQ1–OQ10** to decisions with the user — a **pre-implementation review caught a single-flight interface bug** (a `{read,write}` L2 can't do cross-tier single-flight → must be compute-mediating `getOrCompute`) which spawned **OQ10 → dedicated `withSharedCache()` wrapper, generic `withCache` left untouched** (zero blast radius). **Layer 2 (P1) shipped + verified:** 6 nullable cols (title/summary/url/author/publishedAt/citationCount) on `SelectedReviewPaper`+`SelectedHistoricalPaper` (migration `20260531235412_add_pick_metadata`, applied to **dev.db + prod.db** — additive, no data risk; prod migrated to defuse the shared-`@prisma/client` landmine, since both tiers share one node_modules + the running client now expects the cols); `lib/repositories/selected-papers.ts` `record*Pick` persist metadata + new `backfill*Pick`; `app/api/research/{review,historical}/route.ts` serve picks from DB (**0 arXiv/SS calls** when metadata present), legacy-NULL → 1 id_list fetch + backfill, fresh picks persist; TTLs 3600→**43200** (listings) / **86400** (picks); `apis.html` regen (64 routes). **Verified dev (:4101):** 2nd hit serves from DB in 20–50ms with 0 new arXiv calls (log-confirmed). `tsc` clean. A **live arXiv 429 on prod** (couldn't make a fresh physics review pick) = the exact pre-existing problem Layers 1+3 fix. **COMMITTED scoped** to my files (schema+migration, repo, 3 research routes, `apis.html`, the design doc, this entry), EXCLUDING the user's concurrent convention-restructuring (`docs/_template.html`, `CLAUDE.md`, the new `_template.design-doc.html` archetype) AND the scheduler-logs job files (`lib/{cache,logger,prisma}.ts`, `scheduler/index.ts`, `job-watcher-negative-filter-ingest-smoke.ts`). **NOT pushed** (prod web still serves the OLD compiled routes — no fast path until a prod rebuild at ship/P4; new client + new schema are consistent so no P2021). **Conventions saved (memory):** every feature/design doc gets an **Open Questions** section (`OQ#`, not `Q`), resolved with the user **before any code**; doc section order ends **Open Questions (2nd-to-last) → Tasks (last)**; task checklist needs **≥2 nested levels**; user is introducing `docs/_template.design-doc.html` as the design-doc archetype starter (generic `_template.html` stays markup-only). **Layer 1 (P2) shipped + verified + COMMITTED (2nd commit):** NEW `lib/shared-sqlite-cache.ts` *fat* base (exported `openDb()` seam + generic single-flight keyed store `getOrCompute` with reserve→lead/compute→finish, follower poll, stale-lease steal, expired-done reclaim; `ttlSeconds` absent→`expiry=NULL` for llm, present→TTL'd for research; best-effort degrade); NEW `lib/research/shared-cache.ts` (`researchSharedStore` @ `data/research-cache.db` + the dedicated `withSharedCache()` wrapper — own L1 + in-process dedup + stale-fallback + force-on-refresh, OQ10 so `lib/cache.ts` untouched); **`lib/ai/llm-cache.ts` refactored onto the base** (OQ8, full public API + content-hash `cacheKey` byte-identical — caught + fixed a stray join-separator slip that had become a raw NUL in source; `llm-cache-smoke.ts` **18/18**); all 4 research routes (incl. `/api/research/hf`, OQ2) wrapped with `withSharedCache`; **taught `gen-api-docs.ts` the `withSharedCache` name** so all 4 cache chips persist (`apis.html` regen, api-docs-smoke in sync); NEW `research-shared-cache-smoke.ts` **14/14** (cross-tier read-across + single-flight + expiry + force + degrade) in pre-push SUITES; research-cache prune folded into `scheduler/jobs/llm-cache-prune.ts` (no `scheduler/index.ts` edit). The 2 `.db` files are covered by the existing `*.db` gitignore glob (lines 71-74) — no explicit `.gitignore` edit (avoids the concurrent dirty `.gitignore`). **No schema/migration this layer** → no prod landmine (prod keeps its OLD compiled routes + per-tier prisma L2 until a rebuild; new code isn't in the prod build, zero inconsistency). `tsc` clean; dev (:4101) live, `MISS→HIT` confirmed, `data/research-cache.db` populated. **COMMITTED scoped** to my P2 files (the 2 new libs, llm-cache.ts, 4 research routes, gen-api-docs.ts, pre-push.sh, the new smoke, llm-cache-prune.ts, apis.html, the design doc, this entry). **Layer 3 (P3) shipped + verified + COMMITTED (3rd commit):** `lib/arxiv/rate-limit.ts` rewritten — PRIMARY = single shared token bucket in `data/arxiv-bucket.db` (one paced line across all processes, each consume gated by `BEGIN IMMEDIATE` refill→consume-or-reserve-future-slot via the base's `openDb()` seam, OQ9); FALLBACK = the per-process `globalThis` bucket at **half** the rate (so a 2-tier degrade still ≈ ceiling) on store failure. Env flip: `ARXIV_RATE_PER_MIN` 7→**14** (now the SHARED rate, not per-process) in both `.env.*` + code default 14 + rewrote the comment flagging the meaning change. NEW `arxiv-shared-bucket-smoke.ts` **6/6** (two tiers share one paced line — consumes fan out by the refill interval — + refill frees a slot + bad-path→null fallback) in pre-push SUITES. **Verified dev (:4101):** a real physics arXiv call (200) paced through the shared bucket; `data/arxiv-bucket.db` created; log `[arxiv-bucket] ready … (shared, 14/min)` (Next dev auto-reloaded the `.env` change). `tsc` clean. **COMMITTED scoped** to my P3 files (`lib/arxiv/rate-limit.ts`, the new smoke, `pre-push.sh`, the design doc, this entry). NOTE: the `.env.*` edits (`ARXIV_RATE_PER_MIN`=14) are **LOCAL-ONLY** — `.env.development`/`.env.production` are gitignored (`.env*`, .gitignore:41) and untracked, so they were NOT committed; the code default is 14 so a fresh clone still gets the shared rate. (CLAUDE.md's "checked-in `.env.*`" claim is inaccurate — they're untracked; fix in P4.1.) **P4.1 (docs) DONE + COMMITTED (4th commit):** CLAUDE.md gained a "Shared cross-tier SQLite cache base + arXiv rate bucket" subsection (the `createSharedCache` base + `withSharedCache` research wrapper + the arXiv bucket) and the existing "Cross-tier call dedup" para now notes llm-cache sits on the base (OQ8); forward note added to the `docs/archive/cross-tier-llm-dedup.html` SHIPPED banner. **(Did NOT change CLAUDE.md's inaccurate "checked-in `.env.*`" claim — it's a real discrepancy: `.env*` gitignore-rule:41 makes them untracked/local-only despite CLAUDE.md's intent; whether to add a gitignore exception + track them is a USER decision, flagged below.)** **P4.2 (PROD SHIP) — DONE 2026-05-31 (user OK'd the coordinated deploy):** `npm run build` (BUILD_ID `heSdS3U_fSGfMHs28HDQG`) + `pm2 restart mission-control mission-control-scheduler-prod mission-control-scheduler-dev --update-env`. All 4 mc procs online, prod home 200. **VERIFIED cross-tier dedup LIVE in prod:** prod `/api/research/review?topic=physics` returned the SAME physics pick dev had cached into the shared `data/research-cache.db` ("Lumens as active balloons…") with **0 arXiv call** (prod log: `[research-cache] ready … tier:prod`, no `export.arxiv.org` line) — the paper was fetched once by dev and reused by prod. ⚠️ This coordinated deploy **also shipped every concurrent job's held `main` work** (gutter polish, Planning/CardGrid refactor, Generated Resumes card, fetcher-health/internal badge work, scheduler-logs doc, etc.) — those jobs' "PROD DEPLOY HELD" notes are now stale (their work is live on prod). **Open user decision (minor):** whether to track `.env.development`/`.env.production` (add a `.gitignore` exception) so `ARXIV_RATE_PER_MIN`=14 is portable — currently local-only, but the code default is already 14 so a fresh clone still gets the shared rate. **STILL NOT pushed to origin** — local `main` is many commits ahead of `origin/main` across all jobs; a `git push` runs the full pre-push hermetic gate (flaky under the live dev scheduler) + must flush this doc first — defer to a quiet, coordinated moment. **The whole 3-layer arXiv fix is now live on dev + prod; the recurring 429s should resolve as picks backfill + the cross-tier cache/bucket take hold.**

- **Date:** 2026-05-31 (**Scheduler structured-logs design — ALL open questions resolved with the user + `docs/scheduler-structured-logs.html` rewritten from proposal into a decided P1–P7 plan; COMMITTED scoped to the doc; NO code yet, NOT pushed**). Reviewed the doc + every file it concerns (`lib/logger.ts`, `instrumentation.ts`, `scheduler/index.ts`, both `app/api/system/logs/{route,historical/route}.ts`, `InternalView`, `ecosystem.config.cjs`, the `.env.*` + verbose gates). The original 6 OQs were thin; surfaced 3 missing load-bearing forks + 2 false premises and worked them all to decisions with the user. **Decisions (OQ1–OQ9, now ✓ Decided callouts in §5):** in-app visibility WITH live tail (not terminal-only / not historical-only); transport = **Plan B shared SQLite** `data/logs.db` (per-tier, best-effort, the `llm-cache.db`/`fetcher-health.db` house pattern), **scheduler-only sink** (web ring/SSE untouched — web SSE polls the store ~1s for scheduler rows; historical merges the per-tier web PM2 file + a 48h store window); **mute the scheduler's verbose noise** (`[DATABASE]`/`[CACHE]`/`[API Request]`) at the gate via `&& !MC_SCHEDULER_TIER` in `lib/prisma.ts:26` + `lib/cache.ts:40` (prod scheduler runs `NODE_ENV=production` → gates ON → would flood the viewer; the Fetcher-Health diagnosis counted 2913 markers in one scheduler log; `DEBUG_*` stays the opt-in); **structured `source`+`tier` field** auto-derived in `initLogger` from `MC_SCHEDULER_TIER` (text-prefix rejected — only ~6 lines/process carry `[SCHEDULER:tier]`, all job-body lines are unprefixed) feeding an All·Web·Scheduler viewer filter; **full `initLogger()`** in the scheduler (the ring buffer is what the store listener drains); **fix the historical route's per-tier path bug** (`historical/route.ts:12` hardcodes the prod web file — `PM2_LOG_PATH` unset in both env files, so dev reads prod today); keep both file lines (OQ4); logs = substrate for a future notification system (OQ6); **no separate fetcher log view** (OQ9 — the now-shipped `FetcherHealthCard` is the monitor; raw fetch lines live source-filtered in the general viewer; optional future drill-down link card→logger). **Doc rewrite:** status banner → DECIDED; §3 'foundation' (logger + source/tier + mute), §4 'chosen architecture' with a new bridge Mermaid, §5 decision log (OQ1–9), §6 firmed-up **P1–P7** checklist (logger+mute → `lib/logs-store.ts` → live SSE merge → historical merge → viewer filter → `logs-prune.ts` 48h → verify+smoke). Consistency pass: structure balances (6 sections each with a named h3, 7 task blocks, 9 OQ callouts, 2 diagrams), all cited line numbers re-verified, no inbound links to the renamed anchors. **COMMITTED scoped to `docs/scheduler-structured-logs.html` + this entry ONLY** — concurrent other-job dirty files EXCLUDED (`scripts/tests/hermetic/job-watcher-negative-filter-ingest-smoke.ts` M, untracked `docs/arxiv-rate-limit-fix.html`). **NOT pushed. No implementation yet** — next is P1 (the `initLogger()` + `source`/`tier` + verbose-mute foundation). _(Resolves the "Open: 6 OQs unanswered" note in the entry below.)_

- **Date:** 2026-05-31 (**Fetcher Health store — IMPLEMENTED (P1–P6) + COMMITTED + PROD-DEPLOYED; builds the design entry two below**). Implemented `docs/fetcher-health-store.html` §10. **Shipped:** `lib/fetcher-health/store.ts` (per-tier best-effort SQLite at `data/fetcher-health.db`, WAL, guarded `better-sqlite3` import; `recordFetchOutcome`/`readFetcherHealth`/`pruneFetcherHealth` + test seams); chokepoints wired — `loggedFetch` now awaits the response → `ok` (status<400) / `error` (4xx/5xx/throw), `serveStale`→`fallback`, scrape+rss `ScraperBrokenError`→`broken`; `FetcherHealthEntrySchema` gained `error` (apis.html regen'd, 64 routes); route rewritten to read the store (per-tier via `currentTier()`, `?source=`/`?window=`, 30s TTL), **log-parsing fully deleted**; `FetcherHealthCard` got the **All·Web·Scheduler** source filter + **Error** column + **clickable** 1h/6h/1d window badges + near-live (30s + focus refetch); `api.system.fetcherHealth(source,window)`; finance routes → `loggedFetch` (P2.1, else Pulsar vanished); `scheduler/jobs/fetcher-health-prune.ts` (48h) registered + the dead `fetcher-health-roll` placeholder removed. New hermetic `fetcher-health-store-smoke.ts` (**25/25**, in pre-push). **User course-corrections mid-session:** (1) **count local fetches** — a local fetch IS a fetch (reverted an initial loopback/placeholder filter); (2) **`host:port`** — `hostOf` uses `URL.host` not `.hostname` so Pulsar shows as `localhost:3103` (non-default port kept; public APIs stay clean, no `:443`); (3) **`example.com` was a real bug** — the pre-push gate's smokes (which drive `loggedFetch`/fetchers/`serveStale` with `example.com` + local-test-server fixtures) were writing into the REAL `data/fetcher-health.db`; fixed by exporting a throwaway `FETCHER_HEALTH_PATH` in `pre-push.sh` (trap-cleaned), wiped the junk, re-verified the gate leaves the real store at 0. **Gate 91/91 green (82s); tsc clean; prod build clean.** Scoped commit (my ~19 files + this doc), EXCLUDING the concurrent InternalView/Section + `arxiv-rate-limit-fix.html` work. **PROD DEPLOYED:** fresh `npm run build` (incl. host:port) + `pm2 restart mission-control mission-control-scheduler-prod`; dev (:4101) already live via HMR + restarted scheduler. **Open:** `docs/scheduler-structured-logs.html` (OQ14 spinoff) still has 6 OQs unanswered — give the scheduler `initLogger()` + surface its logs in the in-app viewer, separate effort. **Follow-up fix (same session, user-spotted):** a scrape/RSS fetch that 200s but parses 0 items was recording `ok`+`broken` for one attempt → a fully-broken scraper read 50% health. Fixed: `loggedFetch`/`logExternalCall` take `{record:false}`, and scrape/rss fetchers now record exactly ONE outcome per attempt (`ok` parsed≥1 / `broken` 200-but-0-items / `error` HTTP-fail-or-throw). Smoke 26/26, tsc clean. **Committed scoped to `lib/external-fetch.ts` + `lib/fetchers/{scrape,rss}-fetcher.ts` + the smoke** (the tree has heavy concurrent multi-job WIP — scheduler-logs OQ14 + research-routes + a prisma migration); **prod NOT rebuilt** (a build would bundle that WIP) — prod still double-counts until the next clean coordinated deploy; dev (:4101) has the fix live via HMR. _(Supersedes the design entry two below.)_

- **Date:** 2026-05-31 (**Internal Systems · System Telemetry overflow fix + app-wide Section gutter bump — COMMITTED scoped to my 2 files + this doc, tsc clean; NOT pushed**). User reported the **System Telemetry** card (`components/views/InternalView.tsx`) overflowing off the right edge with the last indicators unreachable at md/sm widths. Root cause: the 6 stat tiles were `shrink-0 whitespace-nowrap` with heavy `px-8` inside a `md:flex-nowrap justify-between` row — un-shrinkable AND un-scrollable → clipped. **Fix (iterated live with the user):** tile row → `flex justify-between gap-0 md:gap-8 flex-1 w-full overflow-x-auto custom-scrollbar` (horizontal-scroll safety net so nothing ever clips; **zero gap at sm**, generous auto-distributed gap at md+ via `justify-between`); tiles → `flex flex-col items-center text-center … py-4 px-8 … w-fit shrink-0 whitespace-nowrap` (content-hugging `w-fit` so content isn't dwarfed + centered + `px-8` horizontal breathing room). `custom-scrollbar` already styles horizontal (`height:6px` in `globals.css`). **Spacing convention pinned down with the user (saved as memory `feedback-rem-spacing-convention`):** repo uses **rem via Tailwind's scale** (verified ZERO raw-px padding/margin/gap in `components/`; `px-N` is padding-x in rem, NOT pixels — only `[Npx]` brackets are literal px, reserved for font sizes / fixed dims / shadows) AND **prefer power-of-2 steps** (2/4/8/16; avoid 3/5/6/7). **`components/Section.tsx`** app-wide gutter `mb-8 last:mb-0 px-6` → `last:mb-0 px-8` (user removed `mb-8` themselves mid-session → `last:mb-0` is now vestigial, flagged to them; final gutter is a flat `px-8`). **COMMITTED scoped to `InternalView.tsx` + `Section.tsx` + this doc ONLY** — the concurrent fetcher-health bg job's dirty tree (finance routes, `lib/fetcher-health/`, fetchers, `scheduler/index.ts`, `apis.html`, `pre-push.sh`, untracked `fetcher-health-*` + `arxiv-rate-limit-fix.html`) was EXCLUDED. `tsc --noEmit` clean. Live on dev (:4101) via Turbopack HMR; **prod NOT rebuilt** (cosmetic). **Not pushed.**

- **Date:** 2026-05-31 (**Fetcher Health card — root-caused + full design doc written & reviewed (all 16 OQs decided); scheduler-structured-logs spun out; both design docs COMMITTED; implementation of the fetcher-health store STARTING NEXT**). User flagged the `FetcherHealthCard` showing "No fetcher activity in the last hour / 6h" while the **1d badge read 100%**. **Diagnosed (reproduced the route logic against every PM2 log file):** `app/api/system/fetcher-health/route.ts` lazily **regex-parses ONE PM2 log file** (the *web* process's, picked by `NODE_ENV`) — so it (1) **never sees the scheduler's crawls** (prod scheduler log alone had 2913 `[EXTERNAL API]`/`[CACHE]` markers, all unparseable since the scheduler never runs `initLogger()` → plain text, no `{ts,msg}` JSON), and (2) builds the **per-host table from the last 1h only** while the badges span 1h/6h/1d, so the table empties (firing the "no activity" copy) while a 1d badge still reads 100% off older entries. Plus the route only reads the *current* `out.log` (midnight logrotate truncates the 1d window) and counts "ok" on the *attempt* (logs before the fetch), so a host 500ing on everything reads 100%. **Decision:** replace log-parsing with a dedicated **per-tier, best-effort SQLite store** `data/fetcher-health.db` (gitignored) written at the fetch chokepoints — reusing the cross-*process* better-sqlite3 pattern from `data/llm-cache.db` but per-*tier* by data. NOT a Prisma table in dev/prod.db (the `$allOperations` middleware would log every insert → log-viewer spam; prod.db is mailbox-equivalent + backed up, no place for ephemeral 48h telemetry). **OQ decisions (all 16):** 4 buckets `ok/error/fallback/broken` (a 500 = `error`, recorded from the real `loggedFetch` response; cache hits NOT counted — `cacheStats` already owns hit/miss; no 5th `cache` bucket); per-tier; **web vs scheduler `source` distinction surfaced as an All·Web·Scheduler card filter**; raw events; 48h retention; direct sync write; hostname grouping; near-live TTL + clickable window badges + Error column; start fresh (no backfill); fully delete the log-parsing path; drop the dead `fetcher-health-roll` placeholder; just-color (no notifications — a broader notification system is coming, the `error` bucket is its substrate). **Finance gap found:** `app/api/finance/{route,history/route}.ts` hit Pulsar with bare `fetch()` + relied on `withCache` `upstreamHost` for host attribution — must convert to `loggedFetch` (task P2.1) or Pulsar vanishes from the card. **Docs:** `docs/fetcher-health-store.html` (full plan, §10 P1–P6 checklist) + `docs/scheduler-structured-logs.html` (OQ14 spun out — give the scheduler `initLogger()` for structured stdout + options to surface scheduler logs in the in-app viewer; **6 OQs still OPEN there**). New convention applied: design-doc open questions use **OQ-numbering** (OQ1, OQ2…), not Q. **Committed scoped to ONLY the 2 design docs + this entry** — `components/views/InternalView.tsx` (M) and `docs/arxiv-rate-limit-fix.html` (??) are concurrent other-job work, EXCLUDED. **NEXT: implement `docs/fetcher-health-store.html` §10** — P1 store module first.

- **Date:** 2026-05-31 (**Pre-push gate fix — stale `resume-from-application-smoke` stub synced with the `select` interface; COMMITTED + PUSHED**). Pushing the scrollbar work (below) ran the pre-push hermetic suite and surfaced a **pre-existing, deterministic** failure that had never been caught (the Canons effort kept deferring the push, so the full gate never ran clean on these commits): `resume-from-application-smoke.ts` 500'd at `app/api/resumes/route.ts:675` with `mostRecentEducationId is not a function`. **Root cause = stale test double, not product code:** the smoke injects a fake `@/lib/resumes/select` into `require.cache` (the `injectCacheEntry` block) exposing only `selectBullets`/`flattenSelections`/`selectProfileExtras`/`entityIsPinned`; it was never updated when `a7ebd7f` added `mostRecentEducationId` (the current-education guarantee, route step 4e.2). The real module exports it (direct-import `resume-current-education-smoke` passes; prod build verified working), so only the stub was behind. **Fix:** added `mostRecentEducationId: () => null` to the stub (its `education` is `[]`, so null matches the real fn's empty-list result → route skips the reorder, irrelevant to this smoke's assertions). Smoke back to 14/14. Test-only change; scanned the other 7 select-stubbing smokes — none stale.

- **Date:** 2026-05-31 (**App-wide scrollbar consistency — `custom-scrollbar` applied to every visible scroll region; COMMITTED + PUSHED to `origin/main`**). The two resume cards (`GenerateResumeCard`, `GeneratedResumesCard`) + the Internal-Systems dash were using the default chunky OS scrollbar instead of the app's sleek 6px `.custom-scrollbar` (defined in `app/globals.css`, applied app-wide via `<body>` in `app/layout.tsx` + the `components/ui/Scrollbar.tsx` wrapper). Swept the whole tree: added `custom-scrollbar` to **23 scroll regions across 16 files** — resume cards, `InternalView` (event-log tail + cache-telemetry list), `FetcherHealthCard`, `LLMLeaderboardCard` (incl. its horizontal tab strip), profile cards (`CanonsCard` ×2, `PersonalInfoCard`, `SnapshotsCard`), overlays (`NotificationBell`, `AddWatchlistModal`, `EditFindRolesModal`, `LaunchpadOverlay` ×2, `SavedPapersOverlay`), `ResearchPaperCard`, `SpaceView` ×2, `LaunchCalendarWidget`. Pure additive className changes (no logic). **Intentionally-hidden scrollbars left alone** (`no-scrollbar`/`scrollbar-none` in `SavedPapersOverlay` tab strips, `LaunchCalendarWidget`, `SpaceView` moon scroller). Final grep confirms zero visible scroll regions remain unstyled. Noted but NOT changed: `pb-scroll` on `LLMLeaderboardCard:83` is a dead class (undefined in CSS). Live on `:4101` via Turbopack HMR; **prod NOT rebuilt** (cosmetic-only). **Process note:** this background job hit the worktree-isolation guard (edits landed in a worktree the PM2 dev server doesn't serve, and the default `fresh` baseRef branched from a stale `origin/main` ~12 commits behind local `main`); set **`worktree.bgIsolation: "none"`** in `.claude/settings.local.json` so bg agents edit the shared checkout directly (saved as a memory). Committed scoped to the 16 scrollbar files + this doc; the concurrent PlanningView/CardGrid work (`fd842fe`) was already committed separately. Leftover `worktree-resume-card-scrollbar` worktree removed.

- **Date:** 2026-05-31 (**CardGrid gutter-align fix + PlanningView conformance — the last structural exception removed; COMMITTED; tsc clean**). Follows the gutter/track-accent entry below. Committed two related things: (1) **`CardGrid.tsx` `p-2` → `py-2`** (was left uncommitted in the prior entry): the grid container's horizontal `p-2` double-inset cards ~8px past Section's `px-6`, misaligning them from the section title; dropping the horizontal half (keeping `py-2` for hover-glow breathing) makes Section's `px-6` the sole horizontal gutter so title + cards align. (2) **PlanningView conformance refactor** — `PlanningView` was the ONLY view rendering cards directly inside `<Section>` (bypassing `CardGrid`): `GoalCard` + `ToDoCard` each carried a manual `px-2` inset, non-canonical chrome (`rounded-2xl p-5` vs canonical `rounded-lg`/`p-4`), and a hard-coded height on their own wrapper. Refactored both into chrome-less inner `<Card title=… icon=… action=…>` (no `wrapperClassName`, no `px-2`) exactly like every other CardGrid card (PersonalInfoCard / ApplicationsKanbanCard); `PlanningView` now builds `CardItem[]` and renders each section via `<CardGrid items={…} />` (full-width `colSpan: 3`), with height caps moved onto the `CardItem` (`max-h-[35vh]` goals / `max-h-[65vh]` to-do, mirroring ApplicationsView's `className: "max-h-[50vh]"` kanban) and the `Scrollbar` normalized to the canonical `w-full h-full pb-8`. Net: every dash now uses one `Section → CardGrid → Card` structure — no exceptions. **Behavioral deltas:** To-Do board went fixed `h-[65vh]` → `max-h-[65vh]` (same cap, shrinks when sparse — the CardGrid idiom); dropped PlanningView's `pt-6` top pad to match other views. **Correction to the entry below:** ApplicationsView is NOT a structural exception — its cards already route through `<CardGrid items={pipelineCards} columns={2} />`; only its sign-in empty-state + track-switch tabs are direct-in-Section (neither is a card). **Residual ~1–2px title-vs-card gap is NOT a code bug** — measured via headless Chrome against :4101: the `<h2>` box left and the card box left are identical to the pixel (both 105 in a 1440 viewport); the gap is the Geist font's left side-bearing (ink offset inside the glyph: "Resume" 1.52px, "Goals" 0.95px, "AI News" 0.42px — varies by first letter, lives in the font, a geometric border has none). No optical nudge applied (user: "not a big deal"). `tsc --noEmit` clean. Working tree clean after this commit. **Prod NOT rebuilt** (`npm run build` + `pm2 restart mission-control` to ship the gutter polish + Planning refactor when ready).

- **Date:** 2026-05-31 (**Section gutter + Applications track-accent — the 4 long-standing dirty files finally COMMITTED; CardGrid gutter-align follow-up applied uncommitted; tsc clean**). The four files that had sat uncommitted across many sessions (`components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView}.tsx`, "already in the running prod build", repeatedly EXCLUDED from feature commits) were committed together at the user's request — they form one coherent UI-polish theme: (1) **`Section.tsx`** — the horizontal gutter (`px-6`) moved from the header div onto the section's OUTER container so header + cards + group-labels share one gutter and the scrollbar stays flush to the frame's right edge (the `px-6` also removed from the `groups` sub-label div + ApplicationsView's track-switch div, now that Section owns it). (2) **`CardGrid.tsx`** — new `frameStyle?: CSSProperties` on `CardItem`, applied to the outer `motion.div` frame (so a box-shadow glow isn't clipped by the frame's `overflow-hidden`), distinct from `wrapperStyle` (background-image on the inner Card). (3) **`ApplicationsView.tsx`** — the per-card `trackGlow` (seeded random-origin radial) split into `trackGradient` (center-bloom wash on the inner wrapper) + `trackShadow` (faint box-shadow on the outer frame via the new `frameStyle`); both follow the active track (cyan career / amber side). (4) **`Card.tsx`** — comment-only. **Follow-up (the user's actual ask this session):** the gutter move surfaced a misalignment — section TITLES (e.g. ProfileView "Resume") sat ~8px closer to the frame edge than their cards, because `CardGrid`'s grid container added a horizontal `p-2` (8px) ON TOP of Section's `px-6`, double-insetting the cards but not the title. Direct-child cards (PlanningView `GoalCard`/`ToDoCard`, ApplicationsView) bypass `CardGrid` so they have no `p-2` and already aligned — meaning a header nudge would've broken THOSE. Fix = `CardGrid` `p-2` → `py-2` (drop only the horizontal double-inset, keep 8px vertical breathing for hover glow); now Section's `px-6` is the sole horizontal gutter and title + CardGrid-cards + direct-cards all align to one line. `tsc --noEmit` clean. **This CardGrid fix is left UNCOMMITTED** so the user can view it live via dev HMR (:4101) before committing. Prod NOT rebuilt.

- **Date:** 2026-05-31 (**Profile-card UI polish — Canons row two-column layout + bold language names; COMMITTED on `main`, tsc clean**). Two cosmetic follow-ups, each scoped to ONE file. (1) **`CanonsCard.tsx` — `CanonRow` two-column layout:** was a left block (title + track + stale + version + keywords) stacked over a full-width action-button row; now `flex items-start gap-3` with **left** = title + track pill (inline) then keywords, and **right** (`flex flex-col items-end`) = a badges line (stale + version dropdown) over the action row (**Regenerate · Download · Specialize… · Edit · Delete**). Regenerate folded into the right action row (previously its own row under the keywords on the left) to reclaim a vertical row; all handlers / disabled-states / tooltips / the inline Specialize job-picker unchanged. (2) **`PersonalInfoCard.tsx` — language names `font-medium` → `font-bold`** in the `LanguagesEditor` entry row (the earlier "Languages column 33%→~40% (`grid-cols-[3fr_3fr_4fr]`) + proficiency-radio inlined into the name+trash row" restructure already landed in `04ac51b`). `tsc --noEmit` clean. **Committed scoped to these 2 files** — the concurrently-dirty `components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView,views/ProfileView,cards/profile/GeneratedResumesCard}.tsx` (other jobs' in-flight work) left untouched/unstaged. Live in `mission-control-dev` (:4101) via Turbopack HMR; prod NOT rebuilt (cosmetic-only). **Not pushed.**

- **Date:** 2026-05-31 (**"Generated resumes" archive card — added + deployed**). New `components/cards/profile/GeneratedResumesCard.tsx`: lists EVERY generated resume grouped by kind — **Canon** (`isCanonical=true`, labelled by canon name + `v{canonVersion}`) vs **Specialized** (everything else: normal Generate, URL/Paste, or a canon's Specialize; labelled via `buildResumeDisplayLabel`). Pure UI — reuses the existing `GET /api/resumes` general list (which already returns `isCanonical`/`canonVersion` per row), shares `queryKeys.resumes()` with GenerateResumeCard's dropdown, live-refreshes on `GeneratedResume` SSE. Mounted in `ProfileView.tsx` to the RIGHT of `GenerateResumeCard` (reordered the `resumeCards` 2-col grid: row1 = Generate | GeneratedResumes; **ImportResumesCard moved to full-width** row2; Canons full-width row3). `tsc` 0. Dev breakdown confirms grouping: Canon 2 (Avionics Intern v1/v2) / Specialized 42. **Excluded from commit:** the concurrently-dirty `components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView,cards/profile/PersonalInfoCard,cards/profile/CanonsCard}.tsx` (not this work — other jobs/edits). **COMMITTED `8a2c50e`; live on DEV (HMR). PROD DEPLOY HELD** — user chose option 1 (wait for ONE clean build vs bundling concurrent WIP). Since then the CanonsCard/PersonalInfoCard polish COMMITTED (`c26e91e`), so only the 4 long-standing dirty files (`Section`/`CardGrid`/`Card`/`ApplicationsView`, uncommitted pre-session + already in the running prod build) remain. **DEFERRED ACTION:** `npm run build` + `pm2 restart mission-control` to ship this card + the canon-UI polish to prod (do it when the user OKs, given those 4 unchanged-from-prod files).

- **Date:** 2026-05-31 (**ProfileView · PersonalInfoCard — contact fields folded into the Name row + given text titles; COMMITTED on `main`, tsc clean**). `components/cards/profile/PersonalInfoCard.tsx`: Location / Email / Phone now carry the same `text-[10px] uppercase tracking-wider text-white/30` title label as the existing Name / Tagline fields (the MapPin / Mail / Phone icons are kept inline as a secondary cue) and share ONE `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3` row WITH the Name input — previously the layout was Name → Tagline → a separate full-width 3-col icon-only contact grid, so this reclaims a full vertical row. Tagline now renders directly under that combined row. Pure JSX restructure inside the "Identity essentials" `<section>`: no prop / type / import changes (all three lucide icons still used), `tsc --noEmit` clean. **Committed scoped to this one file** — the 5 unrelated dirty files from concurrent jobs (`app/api/resumes/route.ts`, `components/Section.tsx`, `components/grids/CardGrid.tsx`, `components/ui/Card.tsx`, `components/views/ApplicationsView.tsx`) were left untouched/unstaged. Live in `mission-control-dev` (:4101) via Turbopack HMR; prod build NOT rebuilt (cosmetic-only, no rush).

- **Date:** 2026-05-31 (**Canons polish — version-history UI + "current education always included" fix; verified (user confirmed CSULB now appears), COMMITTED `a7ebd7f` + PROD DEPLOYED**). (1) **Version history:** `GET /api/resumes?canonId=` lists a canon's canonical versions (added `canonVersion`/`isCanonical`/`canonId` to the resume summary; `orderBy canonVersion desc`); `CanonsCard` got a clickable "N versions" → portal popover (built by a parallel subagent) to download any prior version. (2) **Current-education rule (user-reported bug):** the Avionics canon resume included UC Berkeley but DROPPED CSULB (his current school). Root cause: `getUnremovableEntityIds` protected `selection.education[0]`, which is POST-LLM-reorder = relevance-ranked, so the LLM floated Berkeley to `[0]` and the one-page pruner dropped CSULB. Fix: new `mostRecentEducationId(education)` in `lib/resumes/select.ts` (ongoing/no-endDate first → latest endDate → latest startDate → lowest profile position); the most-recent/current school now ALWAYS leads its section (route step 4e.2, after the LLM reorder + pin-front) AND is unremovable (`getUnremovableEntityIds` uses it instead of `[0]`). His education has NULL dates → falls back to position → CSULB (pos 1). NEW `resume-current-education-smoke.ts` (7) wired into pre-push. **Gate NOTE:** the full pre-push flaked on `posting-digest` / `notification-negative-filter` smokes (count mismatches) — confirmed **concurrent-job interference** (those count JobPosting rows the live dev scheduler mutates mid-run; both PASS in isolation; a DIFFERENT one failed each run) — NOT this change; all resume smokes + the new one pass, `tsc` 0. **Advise the user to add real start/end dates to his education** (all NULL now) so dates drive recency + the rendered resume shows ranges. **Excluded from commit:** dirty `components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView,cards/profile/PersonalInfoCard}.tsx` (not this work). **DEPLOYED to prod** (BUILD_ID `Ri6LSAeKmRj89bTP5x54J` + `pm2 restart mission-control`, :3101 200) + dev (HMR); **user regenerated the Avionics canon → confirmed CSULB now appears/leads (v2).** Open (optional): user adds real start/end dates to his education (all NULL); merge the two dup "security officer" seed canons. **Nothing pushed** this whole Canons effort (local `main` ahead of origin; pre-push gate flakes on the concurrent dev scheduler, so push at a quiet moment).
- **Date:** 2026-05-30 (**Canons P5 — opt-in specialization pass — IMPLEMENTED + DEPLOYED to prod (code-only) this session; gate 89/89**). Follows the Canons MVP entry below. **Shipped:** `lib/canons/specialize.ts:reconstructSelection` rebuilds a canon's CURRENT resume's exact nested selection from the stored flat `selections` + `profileSnapshot` (preserves bullet set + order, drops entities deleted since gen). New route `POST /api/resumes/specialize {canonId, applicationId}` (+ `meta.ts`): loads the canon's current resume, `parsePosting`s the app's posting URL, refreshes per-bullet matches vs THAT posting, re-runs `rewriteBullets` (FLASH) + `tailorResumeTagline` over the FIXED selection (NO re-select / NO re-prune — canon selection is already pruned so render full-length), saves an `isCanonical=false` child linked to the app (canon version chain untouched), returns the PDF. UI: `CanonsCard` gained a per-row **"Specialize…"** button (built by a parallel subagent) — disabled until the canon has a base resume, opens an inline Interested-app picker (reuses `GET /api/applications/pipeline-picker`) → tailored PDF. **Tests:** `canon-specialize-smoke.ts` (10, pure reconstructSelection) wired into pre-push → full gate **89/89 in 79s**; `tsc` 0 + prod build clean. **Deployed to prod CODE-ONLY (no migration — P5 adds no schema):** rebuilt BUILD_ID `qwxcNNKIZKLFi5GXobGs4`; **still need `pm2 restart mission-control`** to load it on :3101 (dev :4101 already live via HMR; route 401 confirmed on dev). **Commit:** scoped to P5 files (specialize route+meta, lib/canons/specialize.ts, smoke, CanonsCard, apis.html, pre-push.sh, this doc); the 4 dirty `components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView}.tsx` EXCLUDED. **Only P6 cover letters remain (out of scope).** Next: user hand-creates the 3 career canons (Avionics = priority) + merges the two dup "security officer" seed canons.

- **Date:** 2026-05-30 (**Close-detection probe gate — design doc ARCHIVED after confirming the feature is done + regression-free; doc-only commit**). User asked to make sure the feature was actually done before archiving the doc. Verified all three of the doc's own archival criteria: (1) **code fully landed + live both tiers** — `lib/postings/liveness.ts` (`probeBatch`/`probePostingLiveness`/`PROBE_PROFILES`/`MC_LIVENESS_BYPASS`), the `scheduler/jobs/job-watcher.ts:441-514` probe-gate (closed/alive/unknown bucketing + `refreshedAlive`), `liveness-probe-smoke.ts` wired into pre-push, `recover-false-closed.ts`; commits `9fcd6df` `0deef8e` `9e29040` `98c57b0`. (2) **recovery run on both tiers** — reopened 921 dev + 629 prod false-closed postings. (3) **live logs clean** — 4133 dev / 3740 prod `[liveness]` verdict lines, zero errors, and the targeted failure modes confirmed *preserved* (LinkedIn 24h-filtered + Workday page-capped candidates return `unknown`/`alive`, never false-closed, e.g. `candidates=177 closed=1 alive=29 unknown=147`). The literal "≥7 days without regression" soak (2026-05-25 → **2026-06-01**) was 2 days shy; user chose to archive on the decisive 5-day evidence (via AskUserQuestion). **Did:** `git mv docs/close-detection-probe.md → docs/archive/` + SHIPPED banner + fixed the stale "Plan, not yet implemented" header; repointed every inbound ref — `CLAUDE.md`, `docs/implementation.md` (×3), and 9 code/test comment pointers → `docs/archive/…`; cleared in-flight action #6 (below). Verified zero stale non-archive refs remain. **Doc-only commit on branch `chore/archive-close-detection`** (created off local HEAD — origin/main was 2 commits behind, missing the Canons work — then fast-forwarded onto `main`). Isolated in a worktree because two other parallel jobs were active (P5-specialization untracked files + a `resume-pipeline-doc` worktree). **Left untouched:** the 4 dirty `components/*.tsx` + the parallel P5 untracked files (`app/api/resumes/specialize/`, `lib/canons/specialize.ts`).
- **Date:** 2026-05-30 (**Canons feature IMPLEMENTED — MVP P1–P4 + seeder landed on DEV, hermetic gate 88/88 green, COMMITTED `594064e`; PROD DEPLOYED + seeded**). Built per the §7 plan in `docs/canonical-resumes.html`. **Shipped:** Prisma `Canon` model + nullable `canonId` FKs on Application/Watchlist/GeneratedResume (+ `isCanonical`/`canonVersion`/`currentResumeId`/`resumeEntityIds`); migration `20260530000000_add_canon_model` applied to **dev.db ONLY** (non-destructive table-rebuild; 63 apps / 43 watchlists preserved) — **prod.db NOT yet migrated**. New: `lib/canons/keywords.ts` (`splitCanonKeywords`), `lib/schemas/canons.ts` (the API contract), `lib/repositories/canons.ts` (CRUD + entity-scoped `markCanonsStaleForEntity` + `nextCanonVersion`/`finalizeCanonGeneration`), `app/api/canons/{route,[id]/route,meta,[id]/meta}.ts`. **Generation (P2):** `{canonId}` branch in `app/api/resumes/route.ts` skips parsePosting, builds a synthetic ParsedPosting from the canon's flat keyword text (no weights), runs the UNCHANGED pipeline, persists `isCanonical`+`canonVersion`, then finalizes (currentResumeId/resumeEntityIds, clears stale LAST so gen-time auto-tag can't self-stale). **Tagging (P3):** `canonId` accepted on Application PATCH (`app/api/applications/route.ts` — collection PATCH) + Watchlist PATCH (`[id]`), canon-ownership-checked; `trackAsApplication` inherits `watchlist.canonId`. **Staleness hook (P3.3):** `markCanonsStaleForEntity` called from the profile-entity ROUTE handlers (work-roles/projects/education PATCH+DELETE), NOT the repo. **UI (P4 — built by a parallel subagent):** `components/cards/profile/CanonsCard.tsx` (list/create/edit/delete/regenerate/download + stale badge + version count) mounted in `ProfileView.tsx`; `api.canons` in `lib/api-client.ts`; `'Canon'` added to `lib/events.ts` ModelName + `hooks/useServerEvents.ts`. **Seeder (P1.6):** `scripts/seed-side-canons.ts` (idempotent) ran on dev → **6 side canons created + watchlists linked**. **Tests:** 3 hermetic smokes (keyword-split 7 / crud 18 / staleness 9) wired into pre-push; full gate **88/88 in 78s**; `tsc` 0 errors; `docs/apis.html` regenerated (63 routes). Dev :4101 serves `GET /api/canons` 401 (wired + auth-gated). **Seed artifact:** the two location-variant security watchlists made TWO "security officer" canons (LA + Downey) — merge to one in the UI. **NOT DONE:** P5 specialization pass (opt-in per-job tailoring) — deferred; P6 cover letters (out of scope). **PROD DEPLOY (done this session):** `20260530000000_add_canon_model` applied to prod.db (data preserved: 45 apps / 48 watchlists / refresh token intact; backup at `prisma/prod.db.bak-canon-20260530`, gitignored); `npm run build` (BUILD_ID `i3hUffvOC2eedAATjBdRl` — build's tsc caught a smoke type error my bare `tsc` missed: `createCanon` requires `keywords` after the `.default("")`; fixed by passing `keywords:""` in the 2 DB smokes); `pm2 restart mission-control mission-control-scheduler-prod`; :3101 home 200 + `/api/canons` 401; seeder ran on prod → 6 side canons. **Next:** (1) **P5 specialization pass** (opt-in per-job tailoring — `{canonId, specializeForApplicationId}` re-runs rewrite+tagline over the canon's fixed selection, saves an `isCanonical=false` child); (2) user hand-creates the 3 **career** canons (Avionics = priority) in the new Profile→Canons UI + merges the two dup "security officer" canons into one. The 4 dirty `components/{Section,grids/CardGrid,ui/Card,views/ApplicationsView}.tsx` files are NOT this feature — EXCLUDED from the commits. _(Supersedes the "no code yet" planning entry below.)_
- **Date:** 2026-05-30 (**canonical-resumes planning — ALL 12 open questions ANSWERED; `docs/canonical-resumes.html` heavily revised + consolidated into a committed plan, COMMITTED `77c37b5`; §7 refined into a detailed P1–P6 build plan with per-phase checkable sub-tasks (P n.m, P1.1…P5.3), + a full internal/code-accuracy consistency pass; no code yet**). Worked Q1–Q12 of the canon design with the user, folding code-grounded findings into the questions (rewriting premises, not appending side-notes) and recording each as a ✓ Decided callout in §6. **Decisions:** **Q1** one verbatim resume per canon; canons at role-type grain (museum-rover/mall-patrol collapse into one "Security Officer"); ~up to 10 total. **Q2** canon ⊂ exactly one track; BOTH career+side in v1; a canon is a subset-lens over the one master profile (never replaces track). **Q3** keyword source = user-provided text on both tracks (side copies the feeding watchlist's keyword string; career = manual, user types them) — unified, so aggregation/LLM-synthesis drop out of v1. **Q4** job→canon assignment = MANUAL TAG (per-job `Application.canonId` on the kanban + optional per-watchlist `Watchlist.canonId` link); auto-assign / normalized-role / LLM-classify all DEFERRED. **Q5** default = verbatim reuse (0 LLM/job, confirmed lossless — nothing per-company is on the resume body: H1=headline, subtitle=tagline, company only hits filename/header/stored row) + an opt-in per-job "specialization pass" (posting-parse + FLASH rewriteBullets + resume-tagline over the canon's FIXED selection, saved as a NEW per-job GeneratedResume child, canon untouched). **Q6** keyword importance weights = DEFERRED (scoreBullet's `keywordWeights` is optional/defaults to 1; flat is fine; v1 just splits the keyword text into a term array; weighting is a later refinement). **Q7** staleness = ENTITY-SCOPED + lazy/manual: a canon goes stale only when an experience IN its resume changes (bullets / tags / pinKeywords / deletion) or its own keywords change; unrelated profile edits leave it fresh; needs a per-canon dependency set (`resumeEntityIds` = the sourceIds from the stored `selections`) + a hook on profile-entity PATCH/DELETE; no auto-regen, no schedule. **Q8** per-canon framing pins (pinnedEntityIds / sectionOrder) = DEFERRED (selection spine + the resume-tagline call already order it). **Q9** one resume per canon; default render = PDF + one-page; docx/full-length on demand from the stored selection (0 LLM); AUTO-VERSIONING — keep prior versions, no diff/rollback UI — via `GeneratedResume.canonVersion` + `Canon.currentResumeId`. **Q10** UI = "Canons" manager in the Profile view + a `GenerateResumeCard` canon shortcut + manual-tag on the kanban + one-time idempotent side-watchlist seeding (career canons added by hand). **Q11** cover letters = OUT OF SCOPE (kept as the P5 stretch follow-up). **Q12** keep the name "Canon" (user-facing). **Net v1 reduces to: a saved keyword list + manual canon tag + verbatim reuse (pdf/one-page) + auto-versioning, with generation = the UNCHANGED existing resume pipeline fed canon keywords.** Two new working-style memories saved (rewrite questions when a finding shifts their premise; favor minimal/manual/deterministic v1 + verify each component is load-bearing before including it). **Career canons CHOSEN: Software Engineer, AI/ML Engineer, Aerospace/Avionics Engineer (Avionics = priority focus) — added by hand (career canons aren't watchlist-seeded). Side canons = the 4 side watchlists + Security Officer.** **Next:** implement **P1 (foundation)** — Canon model + nullable `canonId` FKs on Application/Watchlist/GeneratedResume + `canonVersion`/`currentResumeId` + `resumeEntityIds`; migration BOTH DBs + `prisma generate`; Zod (`lib/schemas/canons.ts`) + repository (`lib/repositories/canons.ts`) + CRUD route (`app/api/canons` + `[id]`) + sibling `meta.ts` + regen `apis.html`; idempotent side-watchlist seeder. Full P1–P6 build plan now in §7 of the doc. _(Superseded the 2026-05-29 "parked awaiting answers" entry below.)_
- **Date:** 2026-05-29 (**authored a NEW design+open-questions doc `docs/canonical-resumes.html` — UNCOMMITTED, parked awaiting user answers; no code touched**). User wants a "canonical resumes" feature: stop generating a fresh LLM-tailored resume per job and instead generate **one resume per "canon" (role-archetype)**, reused verbatim across all similar jobs (every security-officer listing, every math-tutor gig). Insight: the existing pipeline is **keyword-driven** (`selectBullets`/`rewriteBullets` consume a keyword list + weights, not a posting object), so a canon just supplies an *aggregate* keyword profile instead of one posting — selector/scorer/one-page-pruner/skills-gap stay untouched. New granularity level **track ⊇ canon ⊇ job**; the four side watchlists already map ~1:1 to canons. The cross-tier LLM cache (`docs/archive/cross-tier-llm-dedup.html`) doesn't capture this — it's content-hashed on the *exact* prompt, so *similar* postings miss; canons capture "same role, different listing". **Doc = HTML per the migration convention** (template + `./assets/style.css`, kicker/h1/subtitle, numbered collapsible sections each w/ named h3, 2 Mermaid diagrams: track⊇canon⊇job hierarchy + the reuse-decision flow). Strawman (§4, explicitly tentative): a `Canon` model + nullable `canonId` on Application/Watchlist/GeneratedResume + `isCanonical` flag; `{ canonId }` input branch on `POST /api/resumes` building a synthetic `ParsedPosting`. **§6 is the payload: 12 open questions (Q1–Q12)** — granularity/"is one resume enough", canon-vs-track, keyword source (manual/aggregated/LLM/watchlist), job→canon assignment, verbatim-vs-tailored reuse, aggregation details, regen/staleness, per-canon framing pins, storage/versioning, UI surface, cover-letters-as-the-real-personalization-home, naming. Each Q has options + a tentative recommendation. Phasing P1–P5 (side-first; career canons + cover letters are stretch). **Next:** user answers Q1–Q12, then we revise the doc into a committed plan before any implementation. Not linked from CLAUDE.md / other docs yet (do that when the design firms up).
- **Date:** 2026-05-29 (archived two now-shipped design docs — **doc housekeeping; archival edits sit UNCOMMITTED in the working tree**). Verified `docs/cross-tier-llm-dedup.html` and `docs/gmail-realtime-push.html` are fully landed against this doc + the commit log + the on-disk code they touch (dedup: `lib/ai/llm-cache.ts` / `scheduler/jobs/llm-cache-prune.ts`, commit `36a0d4d`; push: `lib/gmail/{watch,history-errors}.ts` / `scheduler/jobs/gmail-watch-renew.ts`, commits `1f73067` + `a41045f`). **Reconciled a doc lie first:** the Gmail doc's P3.3 (CLAUDE.md + next_steps + supersede hosting.md) was shown unchecked though all three were done — ticked P3.3 + the P3 phase box + annotated them, and added a "✅ SHIPPED — archived" callout (the dedup doc already had one). **`git mv`'d both into `docs/archive/`** (renames staged) and **repointed every inbound link** (unstaged edits): each doc's own `./assets/style.css`→`../assets/style.css`, the cross-tier→`../llm-calls.html` link, the two docs' mutual cross-refs, plus `CLAUDE.md` (2 design links), `docs/llm-calls.html` (2), `docs/hosting.md` (1), and ~15 code-comment pointers across `lib/ai/*`, `lib/gmail/*`, `lib/auth.ts`, `lib/email-parser.ts`, `scheduler/*`, `next.config.ts`, the probes + hermetic smokes — all → `docs/archive/…`. Grep confirms zero stale non-archive refs remain outside this doc's historical entries (left as archival per the link-repoint convention). **COMMITTED `ac201c6`** (the user approved the archival after this entry was written). **Working tree also carries concurrent non-mine changes:** the staged `docs/architecture.md` deletion (entry below), `components/providers/state/index.ts` (M at session start), `components/views/ApplicationsView.tsx`, and untracked `docs/applications-view-redo.html` — a commit of the archival must be scoped to exclude these.
- **Date:** 2026-05-29 (`docs/architecture.md` deleted — HTML conversion finalized as single source of truth). Re-audited `architecture.html` against the `.md` before deleting: 18 sections + all subsections (4.x–9.x, 17.x) present, 10 Mermaid diagrams = 10, 9 tables = 9, and a word-level prose diff surfaced only 14 "missing" words — all of which were present inside the HTML's code/Mermaid blocks (false positives from the prose extractor). With fidelity reconfirmed, **`git rm docs/architecture.md`**. Updated the two live inbound refs in this doc (the conversion entry + the open follow-up). `docs/_template.html` already pointed at `.html`. **Left untouched:** the `docs/archive/*` mentions of `architecture.md` (historical snapshots per the archive convention — and several reference *Pulsar's* `architecture.md`, not this repo's). **COMMITTED `6636dfd`.**
- **Date:** 2026-05-29 (ApplicationsView single-track switch redo + track-colored accents — **implemented + plan doc ARCHIVED, COMMITTED this session, tsc clean**). User wanted to kill the doubled UI: the Applications dash rendered both tracks' cards across four section headers (Applications Pipeline / Job Discovery / Side Pipeline / Side Discovery). Replaced with **one section + a track switch** that flips a single card stack between tracks. **The three cards were already `track`-aware** (`ApplicationsKanbanCard` / `NewPostingsCard` / `WatchlistsCard` each take `track` + carry `TRACK_PRESETS`), so the redo is confined to the view + one store field — no card/schema/API changes. **Shipped:**
    - **`docs/archive/applications-view-redo.html`** (new, HTML per the migration convention; authored at `docs/applications-view-redo.html` then **archived in the same commit** once the work shipped, per the archive convention — `../assets` repath + ✅ SHIPPED banner capturing the accents + ticked P1–P3). Plan doc with before/after Mermaid card-blockout, the switch design, state/data-flow. Inbound refs repointed (the two code-comment pointers in `state/index.ts` + `ApplicationsView.tsx`).
    - **`components/providers/state/index.ts`** — new `applicationsTrack: PostingsTrackKey` (+ `setApplicationsTrack`) in `DevicePrefsSlice`, default `"career"`, in `partialize`, persist **version 8 → 9** + migrate line. **Additive in DevicePrefsSlice — does NOT touch the just-committed `hiddenWatchlistIds` work in ThemeSlice** (which landed this session as `a3d09e9` + `3c94bb6`; my change sits cleanly on top).
    - **`components/views/ApplicationsView.tsx`** — config-driven `TRACKS` segmented switch under the "Applications Pipeline" header; the other three section headers removed. One `<CardGrid columns={2}>` in the user-specified order: **Upcoming Interviews (shared, full) → Pipeline Kanban (active track, full) → New Postings · Watchlists (active track, 50/50) → Account Status (shared, full, bottom)**. Cards carry `key={activeTrack}` so a switch remounts them (resets page/search/select). Kept **both** apps queries enabled (preserves dual-cache `handleStatusChange` + kanban "move to other track"); the two `AddApplicationModal`s collapsed to one with `defaultTrack={activeTrack}`.
    - **Layout decision (mine, easy to flip):** New Postings + Watchlists sit side-by-side 50/50 (matches the old Job Discovery grid) rather than full-width stacked. To rename/add a track later: `APPLICATION_TRACKS` enum + `PostingsTrackKey`/`DEFAULT_POSTING_FILTERS_BY_TRACK` + each card's `TRACK_PRESETS` + the `TRACKS` array in the view.
    - **Follow-up (same session):** user found the per-track **title + icon swap** jarring on switch ("Watchlists"→"Side Watchlists", Eye→Briefcase, etc.). Made title + icon **FIXED across tracks** in all three cards' `TRACK_PRESETS`; only the COLOR (iconColorClass / addBtnClass / accent) still varies by track. Fixed surfaces: Kanban = suitcase + "Pipeline Kanban"; New Postings = newspaper + "Postings" (was "New postings"); Watchlists = eye + "Watchlists". Dropped now-unused icon imports (`Mail` from kanban, `Briefcase` from postings + watchlists) and the `NewPostingsCard` "Side Watchlists" empty-hint reference (card is uniformly "Watchlists" now). Note: `Card` CSS-uppercases titles, so these render PIPELINE KANBAN / POSTINGS / WATCHLISTS.
    - **Track-colored accents (user asked for "more color that changes with the toggle"):** (1) Kanban career accent moved blue → **cyan** to match Postings + Watchlists. (2) **Random-per-card radial glow** on the three switchable cards (user picked "random" from center/corners/random via AskUserQuestion): `trackGlow(cardId, track)` in `ApplicationsView` hashes the card id → a fixed `x%/y%` origin (stable across reloads, distinct per card), colored cyan (career) / amber (side) at 0.20 opacity → transparent 62%, rendered as a `background-image` so it layers over the canonical `bg-black/40`. Threaded via a **new optional `wrapperStyle?: React.CSSProperties`** on `components/ui/Card.tsx` (applied to the outer wrapper) + `components/grids/CardGrid.tsx`'s `CardItem` (passed to the Card). Only the three switchable cards carry it; Interviews + Account Status stay neutral. (3) `NewPostingsCard` top-right count subtitle tinted to the track via new preset fields `subtitleClass` / `subtitleMutedClass` (cyan-300 ↔ amber-300).
    - **Verified:** `tsc --noEmit` clean. Live in `mission-control-dev` (:4101) via Turbopack HMR. **COMMITTED this session** (the commit carrying this entry — also swept in the concurrent uncommitted 1-line `CLAUDE.md` note that `docs/next_steps.md` stays markdown, per the user's decision not to convert it). **Prod DEPLOYED** (`npm run build` clean → `pm2 restart mission-control`; :3101 HTTP 200, BUILD_ID `4YZuBu1fvHyHJHOWY6kHw`). Dev (:4101) was already live via HMR.
- **Date:** 2026-05-29 (per-watchlist visibility toggle for the postings feed — **implemented + verified + COMMITTED this session**, cross-device synced). User wanted to show/hide each watchlist's postings from `NewPostingsCard` (career + side) via an **eye icon on every `WatchlistsCard` row** (open/visible by default, click to close → that watchlist's postings vanish from the feed). First built it **per-device** (localStorage, `DevicePrefsSlice`); user then said "we definitely want the hide state to be synced — I'm one user, all my devices should show the same thing," so it was reworked to **cross-device sync** mirroring the global-negative-filters path. **Shipped:**
    - **New DB column `GlobalSetting.hiddenWatchlistIds`** (JSON `string[]`, default `"[]"`) — migration `20260529000000_add_hidden_watchlist_ids`, **applied to BOTH `dev.db` and `prod.db`** (had to `pm2 stop` each tier's web+scheduler to clear the SQLite write lock, migrate, restart) + `prisma generate`.
    - **`lib/repositories/settings.ts`** — added `hiddenWatchlistIds` to `GlobalSettingData`, `parseGlobalSetting` (+ tolerant `parseStringArray` helper), `serializeGlobalSetting` (writes even when empty — an explicit un-hide is meaningful). **`lib/schemas/settings.ts`** — `HiddenWatchlistIdsSchema` (`z.array(z.string().max(100)).max(1000)`), added to `SettingsDataSchema` (`.default([])` so an old-server GET still parses) + `SettingsPostSchema` (`.optional()`).
    - **`components/providers/state/index.ts`** — `hiddenWatchlistIds` lives in the cross-device `ThemeSlice` (hydrated from `/api/settings`, NOT in `partialize`/localStorage), no store setter (set via `useAppStore.setState`, like `negativeFilters`). The per-device version (persist v8→v9, partialize, migrate) was reverted.
    - **`components/cards/applications/WatchlistsCard.tsx`** — shared `VisibilityToggle` (Eye / dimmed EyeOff) wired into both `WatchlistRow` (leftmost action) and `FindRolesGroupRow` (left of the edit pencil). A find-roles group reads hidden only when **ALL** members are hidden (mirrors the all-active pause semantics); toggling fans out across every member. The toggle does an **optimistic store flip + debounced (400ms) `/api/settings` write** — coalesces rapid toggles into one request so the `If-Match` version never races itself; conflict/error → refetch-and-reconcile (mirrors `ThemeProvider`/`GlobalNegativeFiltersEditor`). Local `debounce` helper added.
    - **`components/cards/applications/NewPostingsCard.tsx`** — derives `visiblePostings` by dropping postings whose `watchlistId ∈ hiddenWatchlistIds` before partition/pagination (counts + paging reflect what's shown); new empty-state hint when every match is from a hidden watchlist. (Cross-watchlist dedup in `/api/postings` keeps the freshest row per job, so a job on both a hidden + visible watchlist is hidden only when its surviving row is the hidden one — acceptable for a discovery convenience, noted in a comment.)
    - **`docs/apis.html`** regenerated (settings POST request schema changed; the `api-docs-smoke` pre-push gate enforces freshness).
    - **Verified:** `tsc --noEmit` clean; no new lint (flagged items are all pre-existing `any`/`prefer-const`); **full pre-push hermetic 85/85 green (63s)**; **live `/api/settings` round-trip on :4101** — GET returns `hiddenWatchlistIds`, POST persisted a test id, GET reflected it, reset to `[]` (dev.db left clean).
    - **COMMITTED this session** — ONLY this feature's files (user said "commit your work but just your work"). **Left unstaged:** `components/overlays/ApplicationDetailOverlay.tsx` (an unrelated career/side optimistic-cache-targeting change, not mine) and the pre-existing untracked `scripts/tests/debug/create-side-watchlists-noheard.ts`.
    - **Prod (:3101) not yet live** — it runs the compiled build, so the feature needs `npm run build` + `pm2 restart mission-control` to appear there; the column + migration are already on `prod.db` and the running old prod code safely ignores the extra column. **Not pushed.**
- **Date:** 2026-05-29 (LinkedIn watchlist burst-pacing — durable scheduler fix + 4 new no-degree "side" watchlists brainstormed from the profile — **burst-pacing COMMITTED this session**; deployed to both scheduler tiers). From the dev.db profile (Salvador Salcedo — Downey CA, bilingual ES/FR + Seal of Biliteracy, applied-math, **no completed 4-yr degree yet**; security + event-staff + freelance-SWE + math-tutoring + estimator history), brainstormed no-degree side-income categories beyond "security guard" and created **4 LinkedIn `side` watchlists, one per CATEGORY** — **not** one-per-keyword: `LinkedinConfigSchema.keywords` is a single string but the fetcher passes it verbatim and LinkedIn honors boolean `OR`, so one OR-query per category keeps crawl count + cross-watchlist dupes down. Categories: **AI coding evaluator — Remote**, **Math / test-prep tutor — LA**, **Bilingual CS / interpreter — LA**, **Junior web dev & QA — Remote**. Created in **dev.db** (user `cmopgsol…`) then mirrored to **prod.db** (user `cmpeehlib…`) via the parameterized `create-side-watchlists-noheard.ts` (`MC_WATCHLIST_USER_ID` env).
    - **Burst problem (user-spotted):** 4 created together → identical `lastRunAt` + `scheduleMinutes=240` → re-fire as a batch every 4h; `runDueWatchlists` crawls serially but with ZERO inter-crawl delay, so a synced batch = a tight LinkedIn burst (worst after a >cadence scheduler downtime — every overdue watchlist fires on the first tick back).
    - **Durable fix — `scheduler/jobs/job-watcher.ts`:** `runDueWatchlists` inserts a jittered **3–10s gap before each 2nd+ crawl to a fragile scraped source** (`isFragileSource` = linkedin/indeed only; ATS APIs unaffected). A throw still counts as a fragile attempt so the next is still spaced. Made injectable (`RunDueDeps`: `loadDue`/`processFn`/`sleepFn`/`jitterMs`); the scheduler calls it with no args (defaults preserve prior behavior). New hermetic `job-watcher-crawl-pacing-smoke.ts` (**19 assertions**) → wired into `pre-push.sh`. **Full gate 85/85 green (62s); `tsc` clean.**
    - **Data desync (both DBs)** via `desync-side-watchlist-cadence.ts`: distinct **prime** cadences (233/239/241/251 — pairwise-coprime → huge LCM, never re-cluster) + a next-due phase stagger (15 min > the 10-min tick ⇒ distinct ticks). Result: no tick has >1 LinkedIn crawl on either tier (prod has a transient 06:08/06:13 2-way the jitter fix spaces + the primes drift apart next cycle).
    - **Live crawl test** (`crawl-watchlists-now.ts`, jittered gaps): all 4 crawled, **no 429/bot-detection**; postings AI-eval 10 / Bilingual 9 / Junior dev 20 / **Math 0** — diagnosed as an empty 24h window (query fine: `week`=10; brand names not hurting), will fill as tutoring jobs post. Open option for the user: leave Math at 24h, one-time seed at `week`, or set its stored `timeRange=week`.
    - **Deploy:** scheduler runs source via tsx, so `pm2 restart mission-control-scheduler-{dev,prod}` made the fix live on both (confirmed online + `ready`); prod web needs NO rebuild (only uses the unchanged `runWatchlist`).
    - **COMMITTED this session** (burst-pacing only): `scheduler/jobs/job-watcher.ts`, `scripts/tests/hermetic/job-watcher-crawl-pacing-smoke.ts`, `scripts/pre-push.sh`, the 3 `scripts/tests/debug/{crawl-watchlists-now,create-side-watchlists-noheard,desync-side-watchlist-cadence}.ts`, this doc. **Left UNTOUCHED** (a parallel agent's then-uncommitted "hide watchlists" work): `components/cards/applications/{NewPostingsCard,WatchlistsCard}.tsx`, `components/providers/state/index.ts`, `prisma/schema.prisma`, `prisma/migrations/20260529000000_add_hidden_watchlist_ids/`. **Not pushed** (user asked to commit only). _(Update: that "hide watchlists" feature was finished + reworked to cross-device sync + committed in its own commit — see the new top "Last session" entry.)_
- **Date:** 2026-05-29 (GLOBAL negative filter now gates classification + ingestion — **implemented + tested + deployed + COMMITTED this session**). User saw Gemini classify-batches full of "Sr"/"Senior"/"Lead" postings — titles already in their **global** negative filter — and wanted them gated out of any LLM call, ideally never stored at all ("compute efficiency on the Mac mini"). Chose **Both** (ingest drop + pre-LLM gate) via AskUserQuestion, scoped to the GLOBAL filter (`GlobalSetting.globalNegativeFilters`, a singleton). Prior state: negative filters were applied **read-time only** (`/api/postings` feed + `posting-digest`); job-watcher stored everything and classified it, only suppressing the *notification*. **Shipped:**
    - **`scheduler/jobs/job-watcher.ts` — global filter = ingestion DROP.** Right after fetch (before the existence query, cross-watchlist reuse, inline classify, AND the row create), postings matching the global filter are removed (`kept` partition); a blacklisted title now costs nothing past one cheap regex test. All fetched externalIds are added to `seenExternalIds` BEFORE the drop so close-detection still treats a globally-filtered-but-live legacy row as seen (never false-probes/closes it). Per-watchlist filter UNCHANGED — still read-time only (row stored, per-posting notification suppressed via `watchlistNegativeRegexes`). Logs `dropped N posting(s) at ingest via global negative filter`.
    - **`scheduler/jobs/classify-pending-employment-types.ts` — pre-LLM gate (defense-in-depth).** Loads the global filter once (singleton query) and skips matching candidates in the dedup loop, so legacy NULL+blacklisted rows (stored before the ingest drop landed) never reach Gemini. A global match = hidden on every watchlist, so excluding the whole externalId is safe. Logs `skipped N distinct posting(s) via global negative filter`.
    - **Tests:** `classify-pending-sweep-smoke` +test-8 (global "Senior" row excluded from LLM, non-filtered still classified — 11/11); **new `job-watcher-negative-filter-ingest-smoke`** (HTTP fixture, global ["Senior"], non-first-run + no pre-existing rows to stay hermetic — asserts the Senior posting is NOT stored, the kept one is, newPostings=1; 4/4); **updated `notification-negative-filter-smoke`** Parts 1 & 5 to the new contract (global-filtered postings DROP at ingest — were asserting "all land in DB"; Parts 2-4 insert rows directly so they still test the digest read-filter). `pre-push.sh` SUITES += the new ingest smoke.
    - **Verified:** full pre-push hermetic **83/83 green in 57s**; `tsc --noEmit` clean; prod `npm run build` clean. **Deployed:** rebuilt prod + restarted all 4 PM2 tiers (prod :3101 + dev :4101 HTTP 200). **LIVE confirmation — dev scheduler's first post-deploy sweep logged `skipped 74 distinct posting(s) via global negative filter — not sent to Gemini` alongside `swept 76 … 76 classified`** — i.e. ~49% of that sweep's candidates were gated out of Gemini (74 calls saved in one sweep). The ingest drop (`dropped N at ingest`) additionally fires as crawls surface blacklisted titles, stopping them before they're ever stored.
    - **Non-destructive on legacy rows:** already-stored global-blacklisted JobPostings are NOT deleted — the read-time feed filter hides them and the sweep gate skips classifying them (so zero ongoing compute), but they stay in the table. A one-time cleanup DELETE is deferred (offer it if the user wants them physically gone).
    - **COMMITTED this session** (the commit after the classifier fix `bfb938e`). Files: `scheduler/jobs/job-watcher.ts`, `scheduler/jobs/classify-pending-employment-types.ts`, `scripts/tests/hermetic/{classify-pending-sweep,notification-negative-filter,job-watcher-negative-filter-ingest}-smoke.ts`, `scripts/pre-push.sh`, this doc. (Coexists with the parallel Gmail agent's uncommitted `CLAUDE.md`/`gmail-realtime-push.html`/`hosting.md` — left untouched.)
- **Date:** 2026-05-29 (Gmail real-time push — **Phase 2 (GCP + env) DONE + live first-light steps 1–2 verified**; the user-side half of `docs/gmail-realtime-push.html` §4, following Phase 1 `1f73067`). Installed `gcloud` via Homebrew cask (its `beta` component is unavailable — brew owns the component manager, so `gcloud beta services identity create` can't run; worked around by constructing the Pub/Sub service-agent email from the project number). User ran the interactive `gcloud auth login` (only step Claude can't do). **GCP — project `crypto-lexicon-490719-q9`** (= OAuth client number `500673715504`): enabled the Pub/Sub API (was OFF — a prereq the doc assumed); set it as the active project; created topic `gmail-push` + bound `gmail-api-push@system.gserviceaccount.com` as `roles/pubsub.publisher` (§4.1). **Created the OIDC signer SA `pubsub-mc-publisher@crypto-lexicon-490719-q9.iam.gserviceaccount.com` and granted the Pub/Sub service agent `service-500673715504@gcp-sa-pubsub.iam.gserviceaccount.com` `roles/iam.serviceAccountTokenCreator` ON it — a prerequisite the doc's §4.2 omitted** (without it every push fails to mint its OIDC token). Created the two push subscriptions `gmail-push-prod` / `gmail-push-dev` → the `ms-prod`/`ms-dev` `/api/gmail/webhook` URLs, audiences matching each tier's `PUBSUB_AUDIENCE` byte-for-byte; verified the webhook's check (`app/api/gmail/webhook/route.ts:33` asserts the OIDC `email` claim == `PUBSUB_SERVICE_ACCOUNT_EMAIL` + `email_verified`) (§4.2). **Env (§4.3):** added `GMAIL_PUBSUB_TOPIC` + `PUBSUB_SERVICE_ACCOUNT_EMAIL` to shared `.env`, removed the dead `PUBSUB_WEBHOOK_SECRET` (grep-confirmed zero code refs); `PUBSUB_AUDIENCE` was already per-tier in the tier env files. Restarted all four PM2 processes `--update-env`. **First-light (Phase 3 steps 1–2) VERIFIED on BOTH tiers:** the scheduler startup tick logged `[gmail-watch] armed user=… historyId=5413959` + `[gmail-watch-renew] 1/1 re-armed, 0 failed` (dev user `cmpeehlib…` / prod user `cmopgsol…` — same mailbox, different DB row), `lastSyncedHistoryId` seeded to `5413959`, expiration `1780695373xxx` (~2026-06-06, ~7d out) — which proves both schedulers see the topic. **Receive path then FULLY VERIFIED** via new probe `scripts/tests/probes/gmail-push-receive-probe.ts` — 10/10 on both tiers (reachable, NOT behind Cloudflare Access, real impersonated-signer OIDC token accepted → 404 at unknown-mailbox before any ingest, redelivery deduped 200; needed a one-time `iamcredentials` API enable + a token-creator grant to the human owner on the signer SA for test-token minting — user ran both via `!`). **End-to-end ROUND-TRIP CONFIRMED** via new probe `scripts/tests/probes/gmail-push-roundtrip-probe.ts`: a self-sent test email tripped `[GMAIL WEBHOOK] history=5414135` on BOTH tiers within ~3s, no Scan-Inbox press (`skipped:1` — the deliberately non-application probe mail was classified out, so no junk kanban row). Baseline at send time showed the watch had already processed real pushes for the prior hour (`lastSyncedHistoryId` advanced 5413959→5414104; 18 webhook lines/tier before the test). NOT separately exercised: the single-notification assertion (step 4) — probe mail isn't application-shaped so no notify fired; notify-dedup + dev mute are covered by hermetic tests + config. **Gmail push is fully live (Phases 1–3 complete).** Doc `gmail-realtime-push.html` task list: P1 + P2 (incl. all subtasks) checked, P3.1 annotated receive-path-verified. P3.3 docs DONE this session (CLAUDE.md watch note + `hosting.md` §1 superseded). **C4 (P3.2) ALSO built** this session — `lib/gmail/history-errors.ts:isStaleHistoryError` + the route's stale-history 404 → re-seed-from-envelope + ack-200 recovery, with `gmail-webhook-404-recovery-smoke.ts` (16/16, wired into pre-push). **Committed + pushed this session** (webhook + helper + 2 probes + 1 smoke + 4 docs); `.env` stays untracked.
- **Date:** 2026-05-29 (employment-type classifier sweep was **~100% broken** — diagnosed + fixed + deployed + **COMMITTED this session** [the commit directly after the dedup `36a0d4d`/`f15f7bf` pair]). Found while reading scheduler logs to verify the dedup work below. **Diagnosed (both tiers):** scheduler-dev had **18 successful per-batch lines but 0 `[employment-type-classifier] DONE`** in an 8000-line window; scheduler-prod 4 failures / 0 DONE. Root cause = two compounding pre-existing bugs (NOT caused by the cache work — the cache logs just surfaced it): (1) the cheapest model (`MODEL_LITE_CHEAP`) intermittently returns a bare array instead of `{types:[...]}` or an out-of-enum value, and the strict `ResultSchema` rejected the **entire 50-item batch**; (2) `classifyEmploymentTypes`'s loop had no per-batch guard, so one bad batch threw the whole sweep AND **discarded every already-classified batch** → `runClassifyPendingEmploymentTypes` persisted **0 rows** despite ~18 good batches, burning ~20 Gemini calls/sweep for nothing. **Fixed (`lib/ai/classify-employment-type.ts`):**
    - **Tolerant `ResultSchema`** (now exported): `z.preprocess` rewraps a bare array → `{types:[...]}`; each element is `z.enum(...).nullable().catch(null)` so an out-of-enum / wrong-case value coerces to null instead of failing the batch. Mirrors the existing count-drift tolerance.
    - **Per-batch isolation** in `classifyEmploymentTypes`: each `classifyOneBatch` is try/caught; a still-failing batch defaults ITS items to null (Unspecified → retried next sweep) and the loop continues. JSDoc + the "throws if any batch fails" contract updated accordingly (job-watcher's inline caller also benefits — it now keeps the good batches instead of falling back to all-Unspecified).
    - **Cache interaction:** a tolerant parse SUCCEEDS so its result is cached (lib/ai/llm-cache.ts) normally; only a genuinely unparseable response (`types` not an array) throws, and that throw is isolated per-batch (not cached → retried). Schema change → cache key changes → today's classifier entries auto-invalidate.
    - `scripts/tests/hermetic/classify-employment-type-smoke.ts` — repurposed the two cases that asserted the OLD throw-on-failure contract: **#5** now tests the tolerant schema directly (bare-array rewrap; `'manager'`/`'Internship'` → null; non-array `types` rejected), **#8** tests per-batch isolation (batch 2 throws → its 50 items null, batches 1+3's 70 survive, no whole-sweep throw). **13/13.**
    - `docs/llm-prompts/employment-type-classifier.md` Notes updated (doc-only — System/User template unchanged, so **no Lunary sync needed**).
    - **Verified:** classify-employment-type-smoke **13/13**, classify-pending-sweep-smoke **9/9**, `tsc` clean. **LIVE (both schedulers restarted):** the dev sweep now **completes + persists** — `DONE 1000 items / 20 batch(es) — 946 classified, 54 null` → `swept 1000 distinct externalIds — 946 classified, 946 rows updated` (was 0/0 before). prod gets the fix on its next sweep (same code; restarted).
- **Date:** 2026-05-29 (cross-tier LLM-call dedup — **implemented + verified + prod redeployed + COMMITTED `36a0d4d`** atop the Gmail Phase 1 commit `1f73067`; the handoff implementation of `docs/cross-tier-llm-dedup.html`, which the Gmail session below wrote for "another agent"). dev (:4101/dev.db) + prod (:3101/prod.db) + both schedulers run the same code on one box against the same data, so every cacheable LLM step called Gemini **twice** for identical input. Three decisions taken via AskUserQuestion up front: (1) **promote `better-sqlite3` to a direct dep** — it was only a transitive *devDependency* via promptfoo and ships no types, so the doc's "zero new deps" wouldn't survive a prod-only install (added `@types/better-sqlite3` devDep too); (2) **caching default-ON with opt-outs**; (3) **full deploy incl. prod redeploy**. **Shipped:**
    - **`lib/ai/llm-cache.ts`** (new) — content-addressed `cacheKey()` (`sha256` of resolved model + system + rendered user prompt + temp + maxOut + `z.toJSONSchema(schema,{io:'input'})`); a **shared WAL SQLite** at `data/llm-cache.db` both tiers open (`busy_timeout=5000`, `synchronous=NORMAL`). `llmCached(req, compute)` runs the reservation protocol: `INSERT OR IGNORE` reserve → **leader** computes; losers **follow** (poll 150ms), **steal** a stale lease (>30s → conditional-UPDATE takeover), or **fall back** to self-compute past 8s. **Best-effort, never load-bearing:** the native binding loads via a guarded dynamic `import()` so an ABI mismatch / unwritable file degrades to direct *uncached* compute (warn once); `compute()` errors propagate untouched and are **never cached** (leader `DELETE`s its reservation + rethrows). `pruneLlmCache()` (60d `done` / 24h crashed-`pending`) + test seams (`_resetLlmCacheForTests`, `_statsForTests`, `_seedPendingForTests`).
    - **`lib/ai/gemini.ts`** — `chatJSON` gains `cache?: boolean` (default `true`); body refactored into a `compute()` closure with **`acquireGeminiSlot()` moved INSIDE it** (only the leader spends a rate token); wraps in `llmCached` unless `cache:false`. **6 generative opt-outs** (`cache:false`): `discovery-suggest`, `bullet-assist-fill`, `bullet-assist-rewrite`, `tagline-draft`, `bullet-tags-from-profile`, `scratchpad-synth` (they re-roll on identical input per the anti-repetition rule). Cached (default): `email-parser`, `employment-type-classifier`, `posting-parse`, `resume-rewrite`, `profile-import`, `profile-synthesize`, `bullet-tags-from-posting`, `resume-tagline`.
    - **`lib/email-parser.ts`** — `loadPrompt` moved before the key (the rendered prompt IS the key); `acquireGeminiSlot` + `generateObject` + `safeTrack` all inside `compute()`, so a cache hit/follower spends no token and is **not** traced as a model call. Keyed on `(MODEL_ID, rendered prompt incl. the sentAt anchor, applicationSchema)` — stable across both webhooks for the same Gmail message (the push fan-out simultaneity case).
    - **`scheduler/jobs/llm-cache-prune.ts`** (new) + registered daily in `scheduler/index.ts` JOBS — **coexists with the Gmail work's `gmail-watch-renew`** entry (the shared-file merge the design doc predicted; both present, no conflict).
    - **`next.config.ts`** — `better-sqlite3` → `serverExternalPackages` (native module, webpack can't bundle). **`.gitignore`** — explicit `data/llm-cache.db*` (also caught by the global `*.db*` globs). **`package.json`** — `better-sqlite3@12.10.0` dep + `@types/better-sqlite3` devDep, installed with `--legacy-peer-deps` (repo has a pre-existing promptfoo↔lunary `openai` peer conflict; no `.npmrc`) + `npm rebuild better-sqlite3` to match the active Node **v24.15.0** ABI (prebuilt binary was ABI 147; all 4 PM2 tiers run v24.15.0, so the rebuild is correct everywhere).
    - **Bug found + fixed during smoke dev:** the leader's `lead()` call was initially inside the try/catch that swallows *store* errors, so a real `compute()` throw was mis-caught as a store failure and re-ran compute (double-bill). Moved `lead()` outside the try (matching the follower path); the smoke's error-not-cached scenario caught it (`calls=2`).
    - **Docs:** `docs/llm-calls.html` new §7 "Cross-tier call dedup" (cache-on/off table + cache-hit-tracing note + best-effort) + ToC entry + changelog line; **`CLAUDE.md`** note under "Gemini rate limiting + model fleet".
    - **`scripts/tests/hermetic/llm-cache-smoke.ts`** (new, **18/18**) — cacheKey determinism, single-flight (20 concurrent → 1 compute), error-not-cached, stale-takeover, cache-down degrade, prune. Wired into `scripts/pre-push.sh` SUITES after `gemini-rate-limit-smoke`.
    - **Verified:** **full pre-push hermetic 82/82 green in 61s** (incl. the new smoke + `api-docs-smoke`); `tsc --noEmit` clean; prod `npm run build` clean (better-sqlite3 externalized fine).
    - **Prod REDEPLOYED ✅:** `pm2 restart mission-control mission-control-scheduler-{dev,prod}` + dev web; all 4 online, prod :3101 + dev :4101 **HTTP 200**; **`[llm-cache] ready at /Users/sal/salsquared/mission-control/data/llm-cache.db (WAL)`** logged in BOTH scheduler tiers (proves live engagement at the shared path).
    - **LIVE cross-tier dedup CONFIRMED:** `scripts/tests/debug/verify-llm-cache-cross-process.ts` (new) — process A computed + cached; process B (separate process, same real `data/llm-cache.db`) reused A's **exact value with NO recompute**. Probe row cleaned up after.
    - **Observed (pre-existing, NOT a regression):** `employment-type-classifier` intermittently fails Zod validation (model returns a bare array or an out-of-enum option); the sweep is best-effort and retries next 4h cycle. Caching can't change model output shape (key inputs unchanged; validation runs on the raw response inside `compute()`; cache hits return an already-validated object).
    - **COMMITTED `36a0d4d`** (20 files; clean working tree after). Mid-commit discovery: the parallel Gmail agent had **committed its Phase 1 as `1f73067`** during this session (so the Gmail wiring was already in HEAD, not uncommitted as the entry below still implies). The two shared files (`scheduler/index.ts`, `scripts/pre-push.sh`) were staged as the both-version so `36a0d4d` only **adds** the llm-cache lines on top of HEAD's Gmail — verified the staged diff contained zero Gmail deletions before committing. `1f73067` had also swept this `next_steps.md` (with my dedup entry) into itself, so the doc landed there; `36a0d4d` carries the code + the other docs.
- **Date:** 2026-05-29 (Gmail real-time push — diagnosed dead, designed, **Phase 1 committed `1f73067` + pushed**; cross-tier LLM-dedup design doc written for handoff — its implementation landed as `36a0d4d`, see the entry above). User asked whether a job scans their inbox or if "Scan Inbox" is manual, then reported only seeing new mail on button-press. **Diagnosed: the real-time push path was never wired end-to-end** (not a 7-day watch expiry). Evidence: `gmail.users.watch()` is called **nowhere** in app code (only a comment in `scripts/tests/debug/gmail-inbox-debug.ts`); `WebhookDelivery` count = **0** and `user.lastSyncedHistoryId` = **null** in prod.db (webhook's first action has never run); **0** `[GMAIL WEBHOOK]` log lines ever (vs. live `[BACKFILL]`); `PUBSUB_AUDIENCE` env **unset** (webhook would 500 anyway). The webhook + ingest pipeline are fully built and correct — they were waiting for a registration + push that never came. The manual `POST /api/applications/backfill` (Scan Inbox, last 180d / 200 msgs) is and always has been the only ingestion path.
    - **Two design docs written (both new HTML, per the migration convention):**
        - **`docs/gmail-realtime-push.html`** — the wiring plan. Key constraint surfaced mid-design: **dev.db AND prod.db are linked to the same mailbox** (`salsalcedo4321@gmail.com`, refresh token in both), and Gmail permits **one watch per mailbox** — so dev/prod can't point at separate topics (renewals would clobber). Resolution = **one shared topic, two push subscriptions** (fan-out to each tier's webhook). Env split: `GMAIL_PUBSUB_TOPIC` + `PUBSUB_SERVICE_ACCOUNT_EMAIL` shared in `.env`; `PUBSUB_AUDIENCE` per-tier (`ms-prod.salsquared.xyz` / `ms-dev.salsquared.xyz` — user confirmed both public hostnames already route). Includes the future-schema-migration story (per-DB `migrate deploy` + the scheduler P2021 graceful-degrade guard; watch is DB-independent).
        - **`docs/cross-tier-llm-dedup.html`** — a SEPARATE concern the user wants handed to another agent. Both tiers double-call Gemini for identical input (and run independent per-process rate buckets). Design: content-addressed LLM cache (`sha256(model+rendered prompt+schema)`) in a **shared `better-sqlite3` file** (already installed, zero new deps) with an **in-flight reservation** (leader/follower/steal/fallback) — read-through alone double-misses because the push fan-out fires both tiers simultaneously. Wrap at the two chokepoints (`chatJSON` + `parseApplicationEmail`); gateway is the documented later evolution. Parallel-safe with the Gmail work except two shared files: `scheduler/index.ts` (JOBS array) + this doc.
    - **Gmail Phase 1 built (UNCOMMITTED) — all no-ops while `GMAIL_PUBSUB_TOPIC` is unset, so safe to land before any GCP setup:**
        - `lib/gmail/watch.ts` (new) — `registerGmailWatch(userId)`: INBOX-scoped `users.watch()` against the shared topic; seeds `lastSyncedHistoryId` only when null (webhook owns it on renewal); no-ops + returns null when topic unset. Exports `RegisterGmailWatchFn` type for test injection.
        - `scheduler/jobs/gmail-watch-renew.ts` (new) + registered in `scheduler/index.ts` (daily, 24h) — stateless re-arm of every Google-linked user with a refresh token; `register` fn injectable. 10s startup stagger also re-arms on every scheduler restart.
        - `lib/auth.ts` — best-effort watch arm in the `signIn` callback (never blocks sign-in).
        - `scripts/tests/hermetic/gmail-watch-renew-smoke.ts` (new, **11/11**) — selection (google+refresh armed; no-refresh + non-google skipped), Set dedup (2 google accts → armed once), per-user error isolation. Wired into `scripts/pre-push.sh` SUITES after `webhook-prune-smoke`.
        - `scripts/tests/probes/gmail-watch-probe.ts` (new, diagnostic) — calls `registerGmailWatch` against the real account; ran against dev.db → clean no-op (topic unset, `lastSyncedHistoryId` untouched).
    - **Verified:** smoke 11/11; `tsc --noEmit` clean (0 errors); probe no-ops safely. Full pre-push suite NOT run this session.
    - **Remaining — Phase 2 ✅ DONE 2026-05-29 (see the top "Last session" entry); originally (user/GCP, see `gmail-realtime-push.html` §4):** create `gmail-push` topic + grant `gmail-api-push@system.gserviceaccount.com` publisher; two push subscriptions → `ms-prod`/`ms-dev` webhook URLs (neither behind Cloudflare Access); set env (topic+signer in `.env`, `PUBSUB_AUDIENCE` per tier in tier files); `pm2 restart mission-control mission-control-dev mission-control-scheduler-prod mission-control-scheduler-dev --update-env`. **Phase 3:** run the probe to arm + verify (`historyId`/`expiration` non-null, `lastSyncedHistoryId` set), send a test email → assert `[GMAIL WEBHOOK]` on both tiers with no Scan-Inbox press + only one notification email (dev muted); optional 404-recovery hardening (folds in audit bug #3 — webhook doesn't paginate `history.list`); add a CLAUDE.md note under "Gmail webhook + ingest" + retire stale `docs/hosting.md`.
- **Date:** 2026-05-29 (SPA / embedded-JSON posting-text fallback — **implemented + verified, COMMITTED `815da88`**). User pasted a Dayforce HCM job URL (`jobs.dayforcehcm.com/en-US/cosm/venues/jobs/5344`) into the resume URL tab and got "the page didn't contain enough to produce a resume." **Diagnosed:** the error is `lib/resumes/posting.ts:parsePosting`'s `rawText.length < MIN_POSTING_TEXT_CHARS` guard. Dayforce is a client-rendered Next.js SPA — 312 KB of HTML, but `fetchVisibleText`'s static fetch + cheerio DOM read returns **0** visible chars (all content is JS-hydrated). The posting IS in the HTML, embedded in the `__NEXT_DATA__` `<script>` blob (which our DOM pass strips). **Fixed:**
    - **`lib/resumes/posting.ts`** — new exported pure fn `extractPostingTextFromHtml(html)`: prefers rendered DOM text, falls back to embedded posting JSON when DOM is empty/too short. Two carriers: `<script type="application/ld+json">` `@type:JobPosting` (schema.org — Greenhouse/LinkedIn/many boards; standardized `description`) and `<script id="__NEXT_DATA__">` (Next.js; Dayforce nests the posting under a node with `jobTitle` + `jobPostingContent` — `findNextDataJobNode` anchors on that **pair** so it won't grab the i18n string dictionary the blob also carries, which has innocuous keys like `"description":"Description"`). All extracted fields run through `stripHtmlToText` (cheerio strip tags + decode entities). `fetchVisibleText` now delegates its tail to this fn. New `MIN_POSTING_TEXT_CHARS = 30` constant replaces the magic `< 30`. Workday + pure-XHR pages still fall through to the too-short error → user pastes (the message already points at the paste tab).
    - **`scripts/tests/hermetic/posting-spa-extract-smoke.ts`** (new, 7 assertions) — server-rendered passthrough, DOM-wins-over-embedded priority, Dayforce `__NEXT_DATA__` recovery (tags stripped + entity decoded), JSON-LD (plain + `@graph`-wrapped), i18n-dictionary NOT mistaken for a job, empty-SPA → "". Wired into `scripts/pre-push.sh` SUITES (after `posting-parse-cache-smoke`).
    - **`docs/resume-pipeline.md`** — new lead bullet documenting the SPA/embedded-JSON behavior + that Dayforce/Workday/Next.js boards store posting text this way (per user's explicit ask to note it).
    - **Verified:** new smoke **7/7**; `posting-parse-cache-smoke` still **9/9** (no regression); `tsc --noEmit` clean (exit 0). **Live end-to-end** against the real Dayforce URL: `extractPostingTextFromHtml` now recovers **5507 chars** (title "Security Guard (Part Time)" + full Cosm description) vs **0** before. Fix is live in dev via Turbopack HMR; **prod (`mission-control` :3101) is a compiled build — needs `npm run build` + `pm2 restart mission-control` to pick it up.** Full pre-push suite NOT run this session.
    - **Not committed.** Tree: `lib/resumes/posting.ts`, `scripts/pre-push.sh`, `docs/resume-pipeline.md` (M), `scripts/tests/hermetic/posting-spa-extract-smoke.ts` (??).
- **Date:** 2026-05-28 (LLM **Tier 2** cost optimizations — 2a parallelize + 2b batch scratchpad-synth — **committed + prod redeployed**). Follows the Tier-1 session below. User asked to "do tier 2" after a validation pass (confirmed Tier 1 shipped + Tiers 2–4 still-open) and whether Tier 1 had regression + promptfoo coverage. **Answered:** Tier 1 = hermetic **79/79** green (re-ran the full gate this session) + `tsc` clean; promptfoo correctly N/A for Tier 1 (changed no prompt + no output shape — pure cache + skip-guard).
    - **Tier 2a — PARALLELIZE, not merge (user chose this).** The doc's "fold tagline into the FLASH rewrite" proposal would likely *increase* cost: the tagline's input is a superset of rewrite's (multi-KB `profileSummary` + per-entity evidence), so merging moves it onto FLASH pricing to save only a ~250-tok system prompt + one slot (which §7 says isn't the bottleneck). The two calls have no data dependency, so `app/api/resumes/route.ts` now fires `rewriteBullets` ∥ `tailorResumeTagline` via `Promise.all` (rewrite fatal-on-throw; tagline `.catch`→null best-effort, same fallback as before). No prompt/schema change → no promptfoo needed for 2a.
    - **Tier 2b — batched scratchpad-synth N→1.** Replaced `synthesizeBulletsForEntity` (singular) with `synthesizeBulletsForEntities` (batch) in `lib/profile/scratchpad-synth.ts`: ONE chatJSON call carries every gated entity as a delimited `### Entry N` block, returns `{ entries: [...] }` positionally aligned, output budget `min(8192, 512 + N×1536)`, tolerates count drift (align by index, missing→empty/extra→ignored). `runScratchpadSynth` builds one batch call + maps results back by entityId (best-effort: a batch throw drops all synth, resume still generates). Cross-entity isolation = structural (delimited blocks) + system rule 8. Prompt blob `docs/llm-prompts/scratchpad-synth.md` rewritten (vars now `postingKeywords` + `entriesBlock`). `eval/provider.ts` handler supports single-entity fixtures (`{bullets}`) AND a new multi-entity fixture (`{entries}`); `eval/suites/scratchpad-synth.yaml` +1 isolation fixture. `scripts/prompt-samples.ts` + `prompt-render-smoke.ts` updated (scratchpad-synth now `expectMaxTokens:false` — budget is caller-computed, so the blob declares NO static number; a number there would OVERRIDE the dynamic value via `prompt.maxOutputTokens ?? computed`). `scratchpad-synth-smoke.ts` rewritten: 2 gated entities → asserts exactly 1 batched call + correct A=2/B=1 attribution + delimited Entry blocks + batch-throw degrade.
    - **Verified:** hermetic **79/79 green**; `tsc --noEmit` clean; **Promptfoo scratchpad-synth 4/4 PASS** vs real Gemini (disk-forced via `LUNARY_PUBLIC_KEY=""`, incl. the multi-entity isolation fixture — backend entry didn't borrow React, project entry didn't borrow PostgreSQL). Scoped the eval with `--filter-pattern scratchpad` to avoid billing all 14 callsites. Had to `npm rebuild better-sqlite3` first (promptfoo native dep built for an older Node ABI; current Node v26).
    - **Lunary template SYNCED 2026-05-28 ✅ + verified live.** 2b changed the `scratchpad-synth` output shape (`{bullets}`→`{entries}`). Ran `ONLY=scratchpad-synth npx tsx scripts/sync-lunary-templates.ts` (real run → `versioned`, pushed a new non-draft version). Verified through the live prod path: scoped Promptfoo `--filter-pattern scratchpad` WITHOUT disk-force → **4/4 PASS** (the identical command errored 4/4 immediately before the sync, proving `renderTemplate` now serves the batch template — no version-vs-deployed lag). NOTE for future prompt-shape changes: `DRY_RUN=1` hardcodes a misleading "up-to-date" (sync.ts:156-159); use the real run. The pre-push gate forces the disk fallback (`LUNARY_PUBLIC_KEY=""`) so it would NOT have caught a missed sync.
    - **Prod redeployed 2026-05-28 ✅.** `npm run build` (clean) → `pm2 restart mission-control` → verified `HTTP 200` on :3101. Both tiers now run new code + new template: `mission-control-dev` (:4101) was already correct via Turbopack HMR; `mission-control` (:3101, compiled) now serves the new build. Scheduler tiers don't call scratchpad-synth (unaffected, not restarted).
    - **Doc-accuracy pass (user-flagged during review):** confirmed **no callsites were merged** — 2a parallelized (both `resume-rewrite` + `resume-tagline` remain), 2b batched (same `scratchpad-synth` callsite, fewer calls) — so the inventory count stays **14**; §2.1 rows already updated (scratchpad-synth → batched, resume-tagline → concurrent). Fixed §8.2's stale "All 10 chatJSON callsites" → now states prompt-loading is uniform across all 14 via `loadPrompt`, with the model-call split called out (13 via `chatJSON`/Gemini SDK + the lone `email-parser` via Vercel AI SDK `generateObject`).
    - **Committed this session:** Tier 2 code (`lib/profile/scratchpad-synth.ts`, `app/api/resumes/route.ts`, `eval/provider.ts`), `docs/llm-prompts/scratchpad-synth.md`, `eval/suites/scratchpad-synth.yaml`, `scripts/prompt-samples.ts`, two hermetic smokes (`prompt-render-smoke.ts`, `scratchpad-synth-smoke.ts`), `docs/llm-calls.html` (Tier 2 shipped + §2.1/§5/§8.2/changelog), `docs/assets/style.css` (`.table-wrap` util), `docs/next_steps.md`. The two touched smokes were already in `pre-push.sh` SUITES (no SUITES change needed). **Unpushed** — this Tier-2 commit + the prior Tier-1 commit `d61c249` are both ahead of origin (push runs the pre-push hermetic gate, which forces the disk fallback).
- **Date:** 2026-05-28 (LLM cost-optimization review + Tier-1 wins + doc merge — working tree to be committed). User asked to review `docs/llm-prompts/` for prompts to merge/eliminate to cut Gemini calls + tokens, then chose "Doc + implement Tier 1", then asked to merge `llm-calls` and the new optimization doc into one. **Review finding:** the inbox + scheduler paths are already lean (email-parser has the `looksRelevant()` pre-gate; employment-type-classifier has the title-regex heuristic + cross-watchlist reuse; rewrite has the no-match pre-filter; scratchpad-synth is gated). Remaining fat is concentrated in resume-gen + import. **Shipped (Tier 1, both with hermetic smokes):**
    - **`lib/resumes/posting.ts`** — content-keyed result cache on `parsePosting`. Key = `"t:"+sha256(cleaned+capped text)` for pasted text (never stale) or `"u:"+sourceUrl` for URL-only (computed before the fetch, so a hit skips both the scrape AND the LLM). 30-min TTL (bounds URL staleness), 200-entry LRU cap, `structuredClone` on read/write so callers can't poison the cache. Added an injectable `chatFn: ChatJSONFn = chatJSON` test seam + `_clearPostingParseCache()`. Eliminates N−1 parses when generating N variants against one posting. Smoke `posting-parse-cache-smoke.ts` (9 assertions: text hit, distinct-text miss, whitespace-collapses-to-same-key, URL cache skips fetch, clone isolation, clear resets).
    - **`lib/profile/synthesize.ts`** — `canSkipSynthesis(existing, drafts)` predicate + early-return in `synthesizeMasterResume`. Skips the FLASH/32k call when `drafts.length === 1 && existing has zero entities` (header-only still counts as empty) — returns the lone extraction verbatim. The single most expensive call in the app, eliminated on the common first-import path. Narrow by design (≥2 files or non-empty profile still synthesizes). `app/api/profile/import/route.ts` comment updated. Smoke `synthesize-skip-guard-smoke.ts` (6 assertions: truth table + identity-return short-circuit).
    - **`scripts/pre-push.sh`** — both new smokes wired into SUITES.
    - **`docs/llm-calls.html`** (new, replaces `docs/llm-calls.md` AND the briefly-created `docs/llm-cost-optimization.html`) — converted md→html per the migration convention and MERGED the optimization content in. Sections: model fleet, 14-callsite inventory (slugs link to llm-prompts blobs), the cost model (calls × tokens), patterns-already-in-place, the resume-gen pipeline (mermaid + why it's the target), Tier 1–4 opportunities, non-goals, observability, defaults+rate-limiting, adding-a-new-caller (wiring steps + cost checklist), change log. `docs/llm-calls.md` + `docs/llm-cost-optimization.html` deleted.
    - **Inbound links repointed** `llm-calls.md` → `llm-calls.html` across CLAUDE.md, implementation.md, architecture.{md,html}, llm-prompts/{posting-parse,employment-type-classifier,email-parser,README}.md, eval/README.md, scripts/dryrun-prompt.ts, and the lib/*.ts comments (rewrite, posting, email-parser, classify-employment-type, import-llm, gemini, prompts). `next_steps.md` historical entries left as archival.
- **Backlog captured in the doc (NOT yet implemented):** Tier 2 — fold `resume-tagline` into the `resume-rewrite` FLASH call (one call returns bullets+tagline+ordering); batch `scratchpad-synth` N→1 via positional output. Tier 3 — factor the shared rule block between `tagline-draft` and `resume-tagline` into a `loadPrompt` partial; consolidate the bullet-drafting hallucination guard. Tier 4 — measure whether `bullet-tags-from-posting` (LLM auto-tag) earns its call over the deterministic substring scorer.
- **Verified:** **79/79 pre-push hermetic green in 57s** (includes both new smokes + `api-docs-smoke`). `tsc --noEmit` clean. No route surface changed (parsePosting's new param is internal), so api-docs stayed in sync.
- **Open follow-ups:** none blocking. Tier 2–4 are the backlog (documented in `llm-calls.html` §6). If pushing, the doc-link repoint + the two new code files go together.
- **Date:** 2026-05-28 (roleless-email career/side miscategorization — diagnosed + fixed, working tree to be committed in this same logical step). User noticed an Allied Universal email got tracked onto the **career** kanban when it should have landed on **side**, and asked whether there's a career/side email classifier. **Diagnosed (dev.db):** there is NO track classifier — `lib/email-parser.ts` is track-blind by design; an email inherits a track only by deduping onto an existing app, else `lib/applications/ingest.ts` hardcodes `track: "career"`. The offending email was a generic ATS message (subject "Instructions for Completing Your Form Later") that carried **no role** → `incomingRole` defaulted to `"Unknown"` → `findApplicationByCompanyAndRole` searched `normalizedRole="unknown"` and missed all 5 of the user's Allied side rows; the legacy company-only fallback was gated to NULL-normalizedRole rows only; the senderDomain fallback missed (side rows had `senderDomain: null`, having been created from postings not email) → fell through to create a 6th row on career (`cmpoogopy000lt0dxl5w60a1i`, role "Unknown"). User chose **merge-into-most-recent** for the multi-app ambiguity. **Shipped:**
    - **`lib/applications/ingest.ts`** — folded the legacy company-only fallback and a new roleless branch into one cross-track `findApplicationByCompany` query with two acceptance reasons: (a) `!byCompany.normalizedRole` (legacy NULL rows, unchanged) and (b) `!parsed.role` (roleless email → merge into the most-recently-updated app for the employer on **any** track, inheriting its track, instead of creating a career row). Create branch's explicit `track: "career"` literal untouched (so the cross-track smoke's case-D source assertions still hold). Gated on *empty* role — a role-bearing new role still creates a separate row (multi-role-per-company preserved); role-string *drift* on a present role is NOT covered (still falls to senderDomain).
    - **`scripts/tests/hermetic/ingest-roleless-merge-smoke.ts`** (new, 11 assertions) — replays the dedup decision tree (same pattern as ingest-cross-track-dedup-smoke): roleless email at a side-only employer merges into the most-recent side row (track inherited); a role-bearing new role does NOT merge; roleless at unknown employer still creates; source-grep guards the `!parsed.role` branch + the surviving `track: "career"` literal. Wired into `scripts/pre-push.sh` SUITES.
    - **`CLAUDE.md`** — rewrote the senderDomain dedup-chain bullet to describe the two-reason company fallback + the roleless-merge gate/limits.
- **Verified:** new smoke 11/11 green; `ingest-cross-track-dedup-smoke` still 12/12 green. Full pre-push suite NOT run this session.
- **Cleanup done (dev.db):** ran `scripts/tests/debug/reingest-allied-roleless.ts` (new, one-off) — confirmed msg `19e6bae01e52855a` still fetchable, deleted the phantom career row `cmpoogopy000lt0dxl5w60a1i` (cascaded 2 events), re-ingested. The roleless-merge branch fired live and merged it into the most-recent Allied **side** app (Museum Rover, `cmponlfq80001t0dxjtn6834u`); the stale-status guard correctly attached only the `EMAIL_RECEIVED` record without touching status. Final state: **5 Allied apps, all side, 0 career.**
- **Open follow-ups:**
    - **Not committed yet** this session: `lib/applications/ingest.ts`, `scripts/tests/hermetic/ingest-roleless-merge-smoke.ts`, `scripts/pre-push.sh`, `CLAUDE.md` (+ the one-off `scripts/tests/debug/reingest-allied-roleless.ts`, optional to commit — debug/ one-off, re-running now no-ops on the missing id).
    - **Prod unaffected by the data artifact** (dev-only observation); the code fix applies to both tiers automatically once committed/deployed.
- **Date:** 2026-05-28 (cross-watchlist duplicate postings in the side feed — diagnosed + fixed, working tree to be committed in this same logical step). User saw duplicate jobs in "Side postings", some already applied for, and suspected overlapping watchlists. **Diagnosed (dev.db):** two side watchlists — `security officer — Los Angeles` and `security officer — Downey, CA` (Downey ⊂ LA metro) — both keyword "security officer", so LinkedIn returns the same jobs for both. `JobPosting`'s unique key is **per-watchlist** (`@@unique([watchlistId, externalId])`), so each watchlist stores its OWN row for the same job → **94 jobs duplicated across the two, 188 of 243 side rows (~77%)**. The scheduler's existence check is per-watchlist so it never sees the twin (user's hypothesis #2 — same watcher re-importing — was NOT happening; confirmed all dup sets span exactly 2 distinct watchlists). Compounding bug: `track-as-application.ts` flipped only the clicked row to `tracked`, leaving its twin as `new` in discovery → the "already applied but still showing" symptom (7 such jobs). User chose feed-dedup + flip-siblings (keep both watchlists), then asked for a key resistant to BOTH same-URL and different-URL (LinkedIn repost) duplicates. **Shipped:**
    - **`lib/postings/dedup-key.ts`** (new) — `postingDedupKey(company, title)` = `normalizeCompanyName(company).toLowerCase()` + NUL + `normalizeRoleName(title)`. Reuses the exact Application-level normalizers, so a posting and the Application it'd become share one identity. Resistant to sourceUrl drift (LinkedIn reposts under a new id), title-punctuation drift, legal-suffix drift, and employment-modality noise — but narrow enough to keep genuine multi-role postings distinct (Studio vs Mall vs Museum Patrol). Chosen over `externalId` (= sha256(company|title|**sourceUrl**)), which folds the URL into the hash and so over-splits reposts.
    - **`app/api/postings/route.ts`** — feed-level dedup: after negative-filter, collapse `filtered` by `postingDedupKey` (Set keep-first; rows are `lastSeenAt`-desc so first = freshest), before the `limit` slice. N-way by construction (a Set collapses 2 or 200 rows identically), so adding a statewide `— California` watchlist on top is safe.
    - **`lib/postings/track-as-application.ts`** — new `flipSiblingPostings(tx, userId, {id, company, title, track})` called inside both the create-tx and the merge-link tx. Fetches the bounded `status="new"` set for the same user + **track** (JobPosting stores raw company/title, so the normalized key can't be a SQL filter — fetch + JS-filter), matches on `postingDedupKey`, and `updateMany`s every match → `tracked`. So tracking ONE posting clears every same-job row from discovery regardless of which/how many watchlists surfaced it, and regardless of externalId drift. Scoped to the same track (mirrors the per-track feed + per-track Application unique). Removed the now-used `void userId` in `mergeLinkExistingApplication`; widened its `posting` param to carry `title` + `watchlist.track`.
    - **`scripts/tests/debug/backfill-posting-dedup-flip.ts`** (new) — one-time retroactive flip: any `status="new"` posting sharing a (user, track, dedupKey) with a `tracked` posting → `tracked`. Idempotent, `--dry` preview. Ran on dev.db: flipped **2** rows the storage-key-based pass would have missed (two "Studio Patrol" reposts under different sourceUrls/externalIds — exactly the URL-drift case). (An earlier externalId-keyed SQL backfill in the same session already flipped 7 same-URL lingering twins.)
    - **`scripts/tests/hermetic/track-as-application-sibling-flip-smoke.ts`** (new, 11 assertions) — Part A (no DB): key collapses punctuation/suffix/modality drift, keeps Mall≠Studio and distinct company words distinct. Part B (DB): 3 overlapping side watchlists + 1 career, same job with different externalId/url/title/company-suffix in each → track one, assert all side siblings flip (incl. the different-externalId one), Mall Patrol stays `new` (multi-role preserved), career row stays `new` (cross-track isolation). Wired into `scripts/pre-push.sh` SUITES.
- **Verified:** new smoke 11/11 green; the 5 existing track/dedup smokes still green (track-as-application 22/22, multi-role 27/27, bulk-track 17/17, app-race-dedup 6/6, notification-dedup 9/9); `tsc --noEmit` clean. On dev.db side feed: 202 `new` rows → 126 distinct externalId → **118 distinct normalized keys** (the stronger key collapses 5 extra reposted jobs: Target ×4, Walmart ×3, plus three ×2) while keeping all 20 distinct Allied Universal roles separate (no over-merge). Full pre-push suite NOT run this session.
- **Open follow-ups:**
    - **Prod.db backfill not run.** When ready: `DATABASE_URL="file:./prod.db" npx tsx scripts/tests/debug/backfill-posting-dedup-flip.ts --dry` then without `--dry`. (Code paths apply to prod automatically; only the retroactive cleanup is per-DB.)
    - **Storage key unchanged.** `JobPosting` still stores per-watchlist rows (`@@unique([watchlistId, externalId])`) — dedup is presentation/track-time only. This is intentional (each watchlist keeps its own provenance + `lastSeenAt`); we just collapse at read + flip-on-track. A global storage-level dedup would be a much larger change and isn't needed.
    - **Notifications already deduped** by `dedupKey = posting:${externalId}` (one bell per externalId). Note that's still externalId-keyed, so a URL-drifted repost could in theory fire a second notification — not observed, low-tier "posting" notifications, left as-is.
- **Date:** 2026-05-28 (auto-generated API reference — kills the stale-`apis.md` problem). User asked for a utility that generates API docs from the route code so `docs/apis.md` doesn't rot as routes grow. Built a custom generator (option #2 of: off-the-shelf `next-swagger-doc` / custom generator / framework adoption) because the repo's Zod-v4 dep gives `z.toJSONSchema` for free and it fits the in-progress HTML-docs migration. **Shipped:**
    - **`scripts/gen-api-docs.ts`** (new, `npm run gen:api-docs`) — statically AST-parses (TypeScript compiler API; routes are **never executed**) all 61 `app/api/**/route.ts` for the facts that go stale: HTTP methods (both `export async function GET` and `export const GET =` styles), auth guard (any identifier imported from `lib/auth-guards` that's invoked — catches `requireSession`/`requireLocalOrSession`/`requireSessionOrService` — plus inline `getServerSession`), and `withCache` TTL + upstreamHost (handles both the options-object and bare-number arg styles, and resolves top-level `const` TTL/host references like `SATELLITE_TTL_SECONDS`/`UPSTREAM_HOST`; dynamic host fns → host omitted, not guessed). Request bodies render from the route's Zod schema via `z.toJSONSchema(schema, { io: 'input' })` (input mode = pre-transform client shape; survives `.transform()`/`.coerce` that plain `toJSONSchema` chokes on). Imports only the pure `lib/schemas/*` modules. Emits `docs/apis.html` from the shared template/stylesheet. `--check` mode = exit 1 if the committed HTML drifts (ignores the informational date line).
    - **`lib/api-docs/meta.ts`** (new) — `ApiMeta` type for the one un-inferable bit: `export const apiMeta: ApiMeta = { purpose, external?, notes?, methods? }`. Plain object literal (read statically, not run). **It lives in a sibling `meta.ts` next to each route (e.g. `app/api/ai/meta.ts`), NOT in `route.ts`.** Critical gotcha discovered mid-build: Next.js validates a route module's named exports (generated `.next/types/**/route.ts` uses an `OmitWithTag<…, "">` check), so an `apiMeta` export in `route.ts` is a build/`tsc` error (no `typescript.ignoreBuildErrors` in next.config, and `.next/types/**` is in tsconfig include → 61 errors). The sidecar isn't a route file so it escapes validation, is never bundled, and is still colocated (co-edited in the same PR). The 61 `route.ts` files are therefore **pristine vs HEAD** — all metadata is in the new `meta.ts` files.
    - **`docs/apis.html`** (new, generated) + **API CSS** appended to `docs/assets/style.css` (`.api-route`, `.api-method.{get,post,patch,put,delete}` verb badges, `.api-chip.{auth,cache,ext}`, schema blocks).
    - **All 61 routes documented.** Seeded 8 by hand (ai, calendar/event, space/moon, gmail/webhook, tasks, finance, applications, space/satellites — chosen to exercise every extraction path), then backfilled the remaining 53 via 5 parallel subagents (porting prose from old `apis.md` where it existed, deriving from code otherwise). A one-time AST migration script relocated every `apiMeta` from `route.ts` → sibling `meta.ts` after the Next-export gotcha surfaced (script since deleted).
    - **`scripts/tests/hermetic/api-docs-smoke.ts`** (new) wired into `scripts/pre-push.sh` SUITES — runs the generator `--check`; **a route surface change without regenerating `apis.html` now fails the push.** This is the enforcement that makes "never stale" real.
    - **`docs/apis.md`** — deprecation banner at top (superseded by the generated doc; all 61 now have a `meta.ts` so it can be deleted). **`CLAUDE.md`** — new "API reference is generated" paragraph under "API routes + caching" (documents the sidecar-not-route-export rule + the gate).
- **Verified:** generator runs clean over all 61 routes, **61/61 documented**, 0 schema-render failures (the `io:'input'` fix handles `.transform()`/`.coerce`); `--check` passes when synced and fails on injected drift (both tested); the smoke passes; **`tsc --noEmit` is clean (0 errors)** — confirming the sidecar resolved the route-export build break. Spot-checked rendered HTML for the tricky extractions (generalized guard incl. `requireSessionOrService`, const-resolved TTL, dynamic-host omission, full JSON Schema bodies) — all correct. Full pre-push suite NOT run this session.
- **Committed** this session: 61 `app/api/**/meta.ts`, `docs/apis.html`, `scripts/gen-api-docs.ts`, `lib/api-docs/meta.ts`, `scripts/tests/hermetic/api-docs-smoke.ts`, the `gen:api-docs` npm script, the pre-push SUITES entry, and the API CSS. **`docs/apis.md` deleted** (fully superseded); inbound links in `README.md` / `CLAUDE.md` / `architecture.{md,html}` repointed to `apis.html`.
- **Open follow-ups:**
    - **Per-method `apiMeta.methods` overrides** are supported by the type + renderer but not yet exercised by any route (multi-verb routes use a single top-level purpose). Use them if a route's verbs need divergent purpose/external.
    - **Two doc nits the subagents flagged** (in the old `apis.md`, not the new metas — the metas follow the code): `research/historical` picks papers 3–30 years old (code), not "1–5 years" (old doc); `company-news` now imports from `lib/companies`/`lib/fetchers`, not `lib/company-registry.ts` (old doc predates a refactor). Worth reconciling `CLAUDE.md`'s "Pluggable news ingestion" section against the current module layout sometime.
- **Date:** 2026-05-28 (HTML doc template + shared stylesheet — the start of the `.md` → `.html` docs migration). User wants `docs/` to move from markdown to HTML (richer layout, embedded diagrams, consistent styling) — except `README.md` (GitHub-rendered) and `docs/llm-prompts/*.md` (machine-read prompt blobs). New docs are HTML; existing `.md` convert when next touched (no big-bang sweep). **Shipped:**
    - **`docs/assets/style.css`** (new) — shared stylesheet, single source of truth (no inline `<style>` in docs). Palette: bg `#141414` / box `#282828` (= task depth-1; `--box` was briefly `#2e2e2e` but the user found it too light). Headings Sora 800 (Google Fonts), UPPERCASE: **h1 white `#fafafa` · h2 blue `#60a5fa` · h3 green `#4ade80` · h4 teal `#2dd4bf`**. Body + code mono; inline `<code>` light-blue `#93c5fd`, code-block text light-green `#86efac`. Components: `.callout`(+`.tip/.warn/.danger`), `.kicker` (project-name eyebrow above h1), numbered+collapsible `<details class="section">` with `.section-body` grey box (auto-numbered via CSS counter, matches the ToC), `<ol class="toc-list">` in a `.no-count .toc` section, `<details class="task">` with `P<phase>.<task>.<sub>` ids — collapsible, checkbox completion, depth-graded id colors (blue→green→teal→sky→lime) **and** depth-graded card bg (`--task-d1..d5`, ~5% darker per level), `.doc-footer` copyright (year auto-fills via JS), `.mermaid` container + `.pannable` zoom viewport, on-brand `highlight.js` token theme (`.hljs-*` keyed to the palette), `svg-pan-zoom` control-icon theming. Lists use indexable markers by depth (a/i/1/A) via `:is()` specificity.
    - **`docs/_template.html`** (new) — renders *as* the live style guide (every component shown rendered + the markup to write it), so the template is itself the cheat-sheet. Copy → rename to start a doc. Mermaid `@10` ESM CDN init themed to palette, **lazy-rendered** on section-expand (sections start collapsed; `startOnLoad:false` avoids zero-size render inside hidden `<details>`). Footer reads `© <year> salsquared`.
    - **`CLAUDE.md`** — new "HTML over markdown" subsection under Documentation conventions (migration policy, template/stylesheet locations, the don't-convert exceptions, the convert-a-doc checklist).
    - **Mermaid-in-HTML gotcha captured** (shown as a danger callout in the template + the CLAUDE.md convention): never put a `-->` edge arrow (or any `--`) inside an HTML comment — it closes `<!-- -->` early and dumps raw markup. Author diagrams only in real `<pre class="mermaid">` blocks.
    - **Syntax highlighting + diagram pan/zoom (added same session).** Two more ESM-CDN imports in each doc's module script (same pattern as mermaid): `highlight.js@11` (`hljs.highlightAll()`, auto-detect or `class="language-xxx"`) and `svg-pan-zoom@3.6.1` (applied after each diagram renders → fit-to-view, drag-pan, scroll/buttons-zoom; re-applied on lazy section-expand via `attachPanZoom`). All three CDNs cache after first load; offline → code degrades to flat green, diagrams render static.
    - **First real conversion: `docs/architecture.html`** (959-line `architecture.md` → HTML, delegated to a subagent then audited). Faithful/verbatim: 18 numbered collapsible sections, 10 mermaid diagrams, 9 tables (70 data rows), 63-term glossary, 3 escaped code blocks — all counts reconciled against the source. H1 set to "Architecture" (source said "Architecture Design Review" — wrong, user confirmed) and the redundant subtitle+blockquote collapsed to one `<p class="subtitle">`. **`architecture.md` deleted 2026-05-29** once the HTML was re-confirmed faithful (structural + word-level diff) — `architecture.html` is now the single source of truth. See the top "Last session" entry.
    - **Subsection auto-numbering (`X.Y`).** Nested CSS counter: `details.section > .section-body h3` increments a `subsection` counter (reset per section-body), rendered as `counter(section)"."counter(subsection)` before each `<h3>`. CONVENTION: every section carries ≥1 *named* `<h3>` (its `X.1`) so any content is referenceable as `X.Y` even when there's only one — authors write just the name, no manual prefix; only `<h3>` is numbered (`<h4>` isn't). Applied to `architecture.html` (stripped the 34 manual `N.M ` prefixes; added a named `X.1` to the 11 subsection-less sections — e.g. 16.1 Summary, 1.1 Operating Context). Documented in the template (Headings §1.1) + the CLAUDE.md template bullet.
- **Verified:** visual only (iterated live with the user on palette/fonts/components/highlighting/pan-zoom). Conversion fidelity audited via tag/row/term counts (all matched). No automated tests — pure static docs assets, nothing wired into the app or pre-push suite.
- **Open follow-ups:**
    - **Remaining `.md` docs unconverted.** `implementation.md`, `apis.md`, `frontend_terminology.md`, `hosting.md`, etc. convert when next touched (gradual, per the convention). The architecture conversion proved the template end-to-end.
    - **`architecture.md` deleted (2026-05-29)** — `architecture.html` is the single source of truth. The remaining `architecture.md` mentions are all in `docs/archive/*` historical snapshots (and some actually point at *Pulsar's* `architecture.md`), left untouched per the archive convention.
    - **`CLAUDE.md` Mermaid bullet** (the older "use `<br/>` not `\n`" one under Documentation conventions) predates the HTML subsection and still applies to both `.md` and `.html` — left as-is.
- **Date:** 2026-05-27 (multi-role-per-company dedup — dev.db + prod.db BOTH migrated, working tree to be committed in this same logical step). User reported a 500 when clicking "Track as app" on the Allied Universal "Security Officer Part Time Museum Rover" LinkedIn posting in dev. Root cause: schema's `@@unique([userId, normalizedCompany, track])` made a second Allied Universal app on the side kanban (the user already had a different Allied role linked to a different posting) impossible — the P2002 caught in `lib/postings/track-as-application.ts:104` only handled the postingId-race recovery, so the company-track collision rethrew → 500. User reframed the bug: it should be legal to have multiple applications at the same company for different positions, but we need more data than just the company name to distinguish them. Operator picked **Rule B** (keep parens content as additional tokens) for role normalization. **Shipped:**
    - **`lib/applications/normalize-role.ts`** (new). Rule B normalizer: lowercase + NFKC, punctuation (including parens) → token separators, drop noise tokens (part/time/full/pt/ft/remote/hybrid/onsite/contract/contractor/temp/intern/internship/permanent + light articles), preserve substantive modifiers (senior/staff/lead/principal/junior naturally — not in the drop list). Idempotent. Worked example: `"Security Officer Part Time Museum Rover"` → `"security officer museum rover"`; `"Part-Time Security Officer (Museum)"` → `"security officer museum"` — these are now legitimately distinct rows.
    - **`prisma/schema.prisma`** — added nullable `normalizedRole` + `sourceJobId` columns; swapped unique from `(userId, normalizedCompany, track)` → `(userId, normalizedCompany, normalizedRole, track)`; new `@@index([userId, sourceJobId])`.
    - **Two migrations** applied to BOTH dev.db AND prod.db (each tier's PM2 web + scheduler briefly stopped):
        - `20260527230000_add_application_role_columns` — nullable columns only.
        - `20260527231500_swap_application_unique_for_role` — drops old unique, creates new + index. Phase-2 gated on backfill collision audit ✓.
    - **`scripts/backfill-normalized-role.ts`** (new) — populates `normalizedRole` for all rows + `sourceJobId` from `postingId → posting.externalId`. Includes a collision audit on the future unique quadruple. Ran clean on both DBs: dev.db **46 rows updated, 29 sourceJobIds populated, 0 collisions**; prod.db **45 rows updated, 27 sourceJobIds populated, 0 collisions**. Sanity-check before running migrate on prod confirmed zero pre-existing duplicates on the OLD `(userId, normalizedCompany, track)` triple, which mathematically guarantees the new quadruple unique can't collide either (NULL-distinct during phase 2, and post-backfill the column adds CAN'T create a new violation on a previously-deduped triple).
    - **`lib/repositories/applications.ts`** — `ApplicationCreate` gains optional `normalizedRole` + `sourceJobId` (auto-derived in `createApplication`). `updateApplication` keeps `normalizedRole` in sync when `role` changes (with a guard against poisoning the key with empty normalize output). Two new helpers: `findApplicationByCompanyAndRole(userId, company, role, track?)` and `findApplicationBySourceJobId(userId, sourceJobId)`. `bulkMoveApplicationsTrack` conflict detection now keys on the (company, role) pair — same-company different-role moves succeed (which they couldn't pre-fix).
    - **`lib/applications/ingest.ts`** — dedup priority: (1) `findApplicationByCompanyAndRole` cross-track + senderDomain tie-breaker; (2) legacy company-only fallback for NULL-normalizedRole rows (transitional safety net, prints warn pointing at backfill script); (3) `findApplicationBySenderDomain` CSULB-drift fallback; (4) create. P2002 race-recovery now uses the role-aware lookup with company-only fallback.
    - **`lib/postings/track-as-application.ts`** — new dedup chain: (1) same posting twice (idempotent); (2) `findApplicationBySourceJobId` for same job via different posting row; (3) `findApplicationByCompanyAndRole` scoped to watchlist track; (4) create. Cases 2+3 use **merge-link semantics**: don't overwrite existing `postingId`, log NOTE event ("Tracked from …" if linking, "Also saw …" if not), flip new posting to "tracked", return `merged=true` `created=false`. Brand-new create writes both normalized keys + sourceJobId inline. P2002 catch handles both racing cases (postingId vs. company+role+track).
    - **`TrackResult` type + `app/api/postings/[id]/track-as-application/route.ts` + `lib/api-client.ts`** — added `merged: boolean` to the success response. Client-side schema accepts it as optional (backwards-compat).
    - **`scripts/tests/hermetic/normalize-role-smoke.ts`** (new, 23 assertions, pure function): basic shape, the museum-rover/mall-patrol distinction, Rule B parens-as-tokens, modality stripping, modifier preservation, punctuation handling, idempotency, all-noise → empty.
    - **`scripts/tests/hermetic/track-as-application-multi-role-smoke.ts`** (new, 27 assertions): case 1 same company different role → two apps coexist (the bug regression test); case 2 same company same role different posting → merge-link, original postingId preserved, "Also saw" NOTE event recorded; case 3 manual-created app with NULL postingId → merge-link with linkage (postingId set, sourceJobId stamped); case 4 same posting twice → idempotent created=false merged=false.
    - **`scripts/tests/hermetic/ingest-multi-role-per-company-smoke.ts`** (new, 11 assertions): two roles at same company on same track resolve to the right row; cross-track + track-scoped lookups; empty-role short-circuit; sourceJobId helper.
    - **`scripts/tests/hermetic/bulk-track-smoke.ts`** — `mkApp` fixture builder now populates `normalizedRole: 'smoke role'` directly (this smoke writes via `prisma.application.create` bypassing the helper's auto-derive); without this the new conflict detection couldn't see anything to collide on.
    - **`scripts/pre-push.sh`** — three new smokes wired into SUITES.
    - **`CLAUDE.md`** — race-safety + dedup invariants bullet rewritten for the new quadruple unique, with the touch list (normalizers, sourceJobId chain, classifier role-blindness call-out).
- **Verified:** **73/73 pre-push hermetic green in 54s.** `tsc --noEmit` clean. Backfill ran clean on both DBs (0 collisions). All four PM2 tiers online under new schema (prod web `:3101` returning 200 after restart). User confirmed the original Allied Universal "Track as app" click works in dev (the previously-500ing scenario now creates a brand-new app with the distinct normalized role).
- **Open follow-ups:**
    - **UI surfacing of `merged=true`.** Route now returns `merged: boolean`; nothing in the UI consumes it yet. Future polish: surface a "Linked into existing X application" toast distinct from the "Created new application" toast.
    - **Future: req-ID extraction from email bodies.** `sourceJobId` is populated only at track-as-application time today (from posting.externalId). Many ATS emails include req IDs in the body ("Req# 1234567" — Allied Universal does this); LLM-extracting them would let Gmail ingest dedup at the high-precision tier too. Deferred — currently company+role normalizes well enough for the cases tried.
    - **Behavior change** — clicking Track on a posting whose company+role normalized pair already exists on the user's kanban no longer 500s. It silently merges into the existing app, leaving original postingId intact and recording the additional posting URL in the timeline. If the user wants the merge-link to instead REPLACE the postingId (the alternative we discussed earlier), that's a one-line change in `mergeLinkExistingApplication` — but the conservative direction was chosen.
- **Date:** 2026-05-27 (Gmail-ingest cross-track dedup + explicit-track invariant — UNCOMMITTED). User reported a Mattel application email did not move their existing `Mattel, Inc.` side-track kanban row to APPLIED. Root cause: `lib/applications/ingest.ts:131` hardcoded `const ingestTrack = "career"` and passed it as the lookup scope to both `findApplicationByCompany` and `findApplicationBySenderDomain`. A user's manual side-track row was structurally invisible to dedup — ingest spawned a new career-track Mattel row instead of updating the side row. Compounding leak: `lib/postings/track-as-application.ts:57` bypassed the `createApplication` helper (writes through `tx.application.create({})` to stay inside the watchlist→application transaction), so every "Track this posting" promotion landed with `normalizedCompany=null`. 30 such rows in dev.db. **Shipped:**
    - **`prisma/schema.prisma`** — dropped `@default("career")` on `Application.track`. New migration `20260527213324_require_application_track` (recreate-table dance to drop the SQL DEFAULT). Applied to dev.db (PM2 dev + scheduler-dev briefly stopped). Prod.db still needs `DATABASE_URL="file:./prod.db" npx prisma migrate deploy` when ready.
    - **`lib/repositories/applications.ts`** — `ApplicationCreate.track` typed as **required** (was `track?: string`). Both lookup helpers gained an optional third arg: `findApplicationByCompany(userId, company, track?)` and `findApplicationBySenderDomain(userId, domain, track?)` — when omitted, the track predicate drops and the lookup spans both kanbans. Added `orderBy: { lastUpdateAt: "desc" }` to the indexed and legacy-raw paths so cross-track ties default to most-recent.
    - **`lib/applications/ingest.ts`** — rewrote the dedup block (lines 119–144). Lookups now cross-track. Added a sender-domain tie-breaker: when a company match exists AND the email's sender domain differs from the matched row's stored domain, look across tracks for a same-company sibling whose `senderDomain` matches; switch to it if found (guarded by `normalizeCompanyName(byDomain.company) === normalizeCompanyName(existingApp.company)` so CSULB-drift unrelated employers don't cause a wrong switch). Brand-new-row branch now sets `track: "career"` as an **explicit literal** at the call site — no `?? "career"`, no schema default. Race-recovery `findApplicationByCompany` at the P2002 catch is also cross-track now (so a race against track-as-application resolves correctly when the winner landed on `side`).
    - **`lib/postings/track-as-application.ts`** — plugged the normalize leak: imported `normalizeCompanyName`, added `normalizedCompany: normalizeCompanyName(posting.company)` to the `tx.application.create` data.
    - **`lib/schemas/applications.ts`** — dropped `.default('career')` from `ApplicationSchema.track` (response shape) and `ApplicationPostSchema.track` (manual-create request). `AddApplicationModal` always passes track from its `defaultTrack` prop, so removing the Zod default turns the UI invariant into a 400 if it ever regresses.
    - **`scripts/backfill-normalized-company.ts`** (new) — one-shot to repopulate `normalizedCompany` for the 30 leaked rows. Ran against dev.db: **14 updated, 16 skipped due to conflicts** (user has multiple JobPostings for the same company sitting as separate Applications — Hermeus ×7, Rocket Lab ×6, Stoke Space ×2 — needs manual dedup in the kanban). Prod.db not yet run.
    - **`scripts/tests/hermetic/ingest-cross-track-dedup-smoke.ts`** (new, 12 assertions) — Case A (preexisting side-track row found by cross-track lookup), Case B (sender-domain tie-breaker switches to side row when career is newer but side has matching domain), Case C (most-recent wins when no domain match), Case D (brand-new company → cross-track lookup returns null + source-grep verifies the explicit `track: "career"` literal + no `?? "career"` fallback in ingest.ts), Case E (source-grep on `ApplicationCreate` interface confirms `track: string;` not `track?: string;`). Wired into `scripts/pre-push.sh` SUITES.
    - **Test-suite track audit** — 29 TS errors across 8 hermetic smoke files (all from `prisma.application.create` / `createApplication` calls without explicit track). Fixed by adding `track: "career"` to each. Files touched: `app-race-dedup-smoke.ts`, `deadline-nudge-smoke.ts`, `find-app-by-company-smoke.ts`, `pipeline-picker-smoke.ts`, `resume-from-application-smoke.ts`, `sender-domain-smoke.ts`, `stale-nudge-smoke.ts`, `stale-status-ingest-smoke.ts`.
- **Verified:** **70/70 pre-push hermetic green in 55s.** `tsc --noEmit` clean. Backfill script ran end-to-end; Mattel side row now has `normalizedCompany=Mattel`. PM2 dev + scheduler-dev back online with the new schema. Both Mattel rows (career + side) still exist post-backfill — user needs to manually delete the duplicate auto-created career row (or merge timelines) in the kanban.
- **Open follow-ups:**
    - **User cleanup in the kanban** — delete the duplicate career-track `Mattel` row (or move the timeline events onto the side row + delete the career one). Also resolve the 16 backfill conflicts (Hermeus / Rocket Lab / Stoke Space duplicates).
    - **Prod.db migration + backfill not yet applied.** When ready: `pm2 stop mission-control mission-control-scheduler-prod; DATABASE_URL="file:./prod.db" npx prisma migrate deploy; DATABASE_URL="file:./prod.db" npx tsx scripts/backfill-normalized-company.ts; pm2 restart mission-control mission-control-scheduler-prod`.
    - **Behavior change** — emails from brand-new companies still auto-create career rows (unchanged). Emails from companies that already exist on `side` now correctly land on the side row (the bug fix). If a user wants email ingest to never auto-create anything, this would need a follow-up — but the current behavior is what the user explicitly asked for ("don't keep track as the default; make it explicit or throw" interpreted as "the explicit literal lives in the create call site, not a schema default; brand-new rows still default to career via that explicit literal").
    - **Classifier remains track-blind by design.** `lib/email-parser.ts:parseApplicationEmail` emits `company`/`role`/`status`/`kind`/`nextSteps` — no `track`. Track is a user-organizational concept (a full-time barista role might be career or side; the LLM can't tell). One-click reclassify in `ApplicationDetailOverlay` remains the right UX.
- **Date:** 2026-05-27 (lockstep employment-type classifier). User flagged Lunary showing 2 `employment-type-classifier` calls a few minutes apart every 30–50 min and asked whether the cadence could be batched to save tokens/spend. Diagnosed: `scheduler/jobs/job-watcher.ts` fires its own classifier batch per watchlist per tick whenever new postings land + the heuristic returns null — 39 active watchlists in dev.db (37 @ 240min, 2 @ 60min) with offset `lastRunAt` clocks means the 10-min scheduler tick scatters small classifier batches across the day. **Refactor (user's design):** keep the inline batch ONLY on a watchlist's first crawl (`watchlist.lastSuccessAt === null`) so newly-added watchlists feel responsive; everything else writes new postings with `employmentType=null` and waits for a new lockstep sweep. **Shipped:**
    - **`scheduler/jobs/job-watcher.ts`** — wrapped the `classifyEmploymentTypes` call's input builder in `isFirstRun ? […] : []` so non-first-run new postings skip the inline LLM call. The cross-watchlist reuse block above it (lines ~219–253) still runs and still hydrates raw.employmentType from a sibling's stored value when present — that path is FREE (no LLM call) so it stays unconditional.
    - **`scheduler/jobs/classify-pending-employment-types.ts`** (new) — `runClassifyPendingEmploymentTypes(chatFn?)` selects every `JobPosting WHERE employmentType IS NULL AND status NOT IN ('closed','hidden')` ordered by `firstSeenAt ASC`, dedupes by externalId (caps at `SWEEP_CAP=1000` distinct), calls `classifyEmploymentTypes`, then `updateMany` per externalId with `employmentType: null` race guard. `chatFn` parameter forwards to the shared `ChatJSONFn` injection seam — same hermetic pattern as `lib/ai/classify-employment-type.ts`.
    - **`scheduler/index.ts`** — new IntervalJob entry at `4 * 60 * 60 * 1000` (4h) cadence. The 10s-per-job startup stagger already in place means it kicks at boot+30s.
    - **`scripts/tests/hermetic/classify-pending-sweep-smoke.ts`** (new, 9 assertions) — empty-pending no-call, end-to-end 3 null rows classified, cross-watchlist dedupe (shared externalId → 1 input → 2 rows updated), closed/hidden rows skipped, pre-classified preserved, null verdict re-tried next sweep. **Subtle gotcha:** seeded rows MUST backdate `firstSeenAt` to epoch — dev.db has 4759 distinct null externalIds older than any freshly-seeded row, and the sweep's `firstSeenAt asc` orderBy would otherwise push test rows past `SWEEP_CAP*4=4000` take cap. Wired into `scripts/pre-push.sh` SUITES.
- **Verified:** new smoke 9/9 green, `job-watcher-classifier-regression-smoke` 5/5 green (Bug A/B/D paths intact — Bug-B's W2 is `lastSuccessAt === null` so its first-run still hits the cross-watchlist reuse → no LLM call needed), `classify-employment-type-smoke` 10/10 green, `watchlist-hermetic-smoke` 23/23 green. Full pre-push not run yet (user can run `bash scripts/pre-push.sh` before pushing).
- **Open follow-ups:**
    - **Tradeoff:** new postings on non-first-run crawls now show "Unspecified" in the UI for up to 4h (next sweep). `employmentType` is display-only so nothing else routes on it — but worth communicating if anyone notices.
    - **Tunable:** `SWEEP_CAP` (1000 distinct ids per sweep) + 4h interval. 4759 distinct nulls in dev.db today means the queue will take ~5 sweeps (≤20h) to fully drain on first deploy if the sweep runs nominally. Bump cap or shorten interval if user wants faster catch-up; the rate-limit (12 req/min) on `acquireGeminiSlot` naturally serializes batches so a single sweep at SWEEP_CAP=1000 → 20 batches takes ~100s wall-time.
    - **No docs/llm-calls.md row needed** — this is a new invocation site for the existing `employment-type-classifier` callsite, not a new callsite. The inventory entry already exists.
    - **Migration:** none. New file + intervaljob entry; PM2 scheduler restart picks up both. `pm2 restart mission-control-scheduler-dev mission-control-scheduler-prod --update-env`.
- **Date:** 2026-05-26 (eval downgrade probe — can MODEL_LITE callsites move down to MODEL_LITE_CHEAP?). User flagged that only `employment-type-classifier` currently uses `MODEL_LITE_CHEAP` (`gemini-2.5-flash-lite`) and asked whether other picker/enum-shaped callsites could safely move down a tier. Built a Promptfoo-based A/B harness, ran it against three candidates (`bullet-tags-from-posting`, `bullet-tags-from-profile`, `discovery-suggest`), expanded the fixture coverage on the two most promising, re-ran, and ended with a more honest "not enough signal yet" verdict. **Shipped:**
  - **Override hook** in `lib/ai/gemini.ts:chatJSON` — when `MC_EVAL_DOWNGRADE_CALLSITES` (comma-separated callsite `name:` values) and `MC_EVAL_DOWNGRADE_MODEL` (gemini model id) are both set, the named callsites' model swaps to the downgrade model for that call. Other callsites in the same run stay on their hardcoded models. Prod never sets these env vars; the hunk is a true no-op there.
  - **Probe driver** `scripts/tests/probes/eval-downgrade-probe.ts` runs `promptfoo eval` twice (baseline + downgrade), writes `eval/output/probe-{baseline,downgrade}.json`, and prints a per-callsite × per-assertion pass-rate diff. Tolerates promptfoo's exit-100 ("some assertions failed" — the whole point of the diff).
  - **Fixture expansion:** `eval/suites/bullet-tags-from-posting.yaml` 4 → 9 fixtures (multi-bullet batch, synonym match `PostgreSQL`↔`Postgres`, close-but-not-match `React`↔`React Native`, multi-keyword coverage, no-match bullet). `eval/suites/discovery-suggest.yaml` 1 → 3 fixtures (added `developer tools` + `climate tech` topics with anti-repetition asserts + internal-uniqueness check).
  - **README:** `eval/README.md` gained a Downgrade probe section with the workflow + the actual findings from this run + the next-step coverage gap.
  - **Drive-by:** fixed a pre-existing YAML indentation bug in `eval/suites/profile-synthesize.yaml` (two `location:` keys over-indented under `header:`) that was blocking the whole Promptfoo suite from loading.
- **Findings (two probe runs):** `bullet-tags-from-posting` passed 4/4 → 4/4 on the original fixtures, then **regressed 9/9 → 8/9** after the expanded fixtures caught the cheap model tagging "Built the public-facing marketing site as a React SPA with Next.js" as `React Native`. Classic close-but-not-match false positive — MODEL_LITE correctly returned `{"proposals":[]}` on the same fixture. **`bullet-tags-from-profile`** stays on MODEL_LITE — the bartending cross-domain fixture is rubric-noise between runs (5/6 baseline, then 6/6 the next pass) but the regression is real enough not to flip. **`discovery-suggest`** survives 3/3 → 3/3 with the expanded fixtures, but user pushed back (correctly) that 3 fixtures is too thin given that the assertions are all hard-rule checks (shape + excludes-respected + internal-uniqueness) and don't catch the soft quality drop most likely on the cheaper model — suggestion plausibility, topic adherence, diversity. **Decision: don't flip anything yet.** Net cost of this whole exploration: ~$0.10 in Gemini spend across the probe runs.
- **Open follow-ups for this exploration:**
  - **Before flipping `discovery-suggest` to `MODEL_LITE_CHEAP`** — expand `eval/suites/discovery-suggest.yaml` to 5–7 fixtures (cover ambiguous-topic like "shell", broad-topic like "fintech", niche like "fusion energy", no-excludes-diversity test). Add an `llm-rubric` assertion per fixture grading suggestion *plausibility* ("Are at least 3 of these recognizable real companies that fit the topic?") since the current hard-rule assertions survive a quality drop. Then re-run the probe. Cost: ~5–20 extra outbound HTTP probes per new fixture; ~$0.05 extra Gemini.
  - **`bullet-tags-from-posting` stays on MODEL_LITE.** Regression is real on the React/React Native close-match — keep as is. The probe is now a re-usable harness if a future Gemini generation changes the calculus.
  - **No code changes to `lib/profile/auto-tag.ts` / `lib/discovery/suggest.ts` / `lib/profile/bullet-tag-suggest.ts`** — model selection unchanged on all three.
- **Date:** 2026-05-26 (gemini.ts thinking-budget comment refresh — drive-by). Comment above `thinkingConfig: { thinkingBudget: 0 }` in `lib/ai/gemini.ts:rawGenerate` said "Gemini 2.5 Flash defaults to thinking mode" — stale since the fleet now spans `gemini-3.5-flash` (MODEL_FLASH), `gemini-3.1-flash-lite` (MODEL_LITE), and `gemini-2.5-flash-lite` (MODEL_LITE_CHEAP). Rewrote to make explicit that the budget=0 is load-bearing for full Flash variants (default thinking ON) and a no-op for `-lite` variants (default thinking OFF) — kept explicit so the guarantee survives a future caller flipping to full Flash. Comment-only; no behavior change. Other uncommitted work in tree (eval-downgrade probe hunk in same file, `eval/README.md`, `eval/suites/profile-synthesize.yaml`, untracked `scripts/tests/probes/eval-downgrade-probe.ts`) was explicitly NOT included in this commit.
- **Date:** 2026-05-26 (GitHub README ingest fully retired). Two-stage cleanup. **Stage 1** stripped the prompt injection: `lib/resumes/rewrite.ts` lost `ProjectReadmeContext` + `PROJECT_README_PROMPT_LIMIT` + the `readmeCtx` param on `rewriteBullets` / `buildRewriteUserPrompt`; `lib/profile/bullet-assist.ts` lost `renderReadme` + `ProjectReadmeContext` + `README_CAP_BYTES` + the `readmeContext` field on `BuildBulletAssistPromptInput`; `app/api/resumes/route.ts` + `app/api/profile/bullets/assist/route.ts` lost their per-project README assembly. Prompt docs (`resume-rewrite.md`, `bullet-assist-fill.md`, `bullet-assist-rewrite.md`) updated — `{{readmesBlock}}` and `{{readme}}` variables removed; overflow trim sequence is now `archive → siblings → scratchpad` (scratchpad still drops last). Fixtures + eval suites scrubbed (`scripts/prompt-samples.ts`, `eval/provider.ts`, `eval/suites/bullet-assist-{fill,rewrite}.yaml`). Hermetic `readme-prompt-smoke.ts` deleted + removed from `scripts/pre-push.sh`; README assertions stripped from `bullet-assist-smoke.ts`, `bullet-assist-scratchpad-smoke.ts`, `bullet-rewrite-text-only-smoke.ts`, and the debug `bullet-overflow-check.ts`. Three updated templates pushed to Lunary (`bullet-assist-fill`, `bullet-assist-rewrite`, `resume-rewrite`) via `sync-lunary-templates.ts`. **Stage 2** killed the fetch path: deleted `scheduler/jobs/github-readmes.ts` + `lib/fetchers/github-public-fetcher.ts` entirely; removed the `github-readmes` job from `scheduler/index.ts`; dropped `Project.readme` + `Project.readmeUpdatedAt` from `prisma/schema.prisma` and `ProjectSchema` in `lib/schemas/profile.ts`. New migration `20260526233301_drop_project_readme` applied to both `dev.db` + `prod.db` (PM2 dev + prod tiers briefly stopped, migrated, restarted). 68/68 pre-push hermetic green; `tsc --noEmit` clean. All four PM2 processes back online under the new schema. Earlier in the same session: `resume-tagline` prompt deduped — the `entityIdsBlock` no longer carries truncated sample bullets (those duplicate `profileSummary`); only the `[matched: …; aggregate-score=N]` annotation remains.
- **Date:** 2026-05-26 (CLAUDE.md compaction — 312 → 166 lines / 43 KB → 24 KB, ~44% byte reduction / ~4.7k tokens saved per turn). Two extractions + a delete pass. **Extracted to new docs:** `docs/backup-recovery.md` (full age-encryption setup + cron + Mac-died recovery runbook) — CLAUDE.md keeps a 2-paragraph pointer + the Cloudflare-tunnel note. `docs/resume-pipeline.md` (the 7 pipeline rules + the touch list for adding a selection-affecting field) — CLAUDE.md keeps a 1-paragraph pipeline-stage summary + "most-common-bug" pointer. **LLM observability prose** trimmed in place to the 3 invariants; the design-doc cross-reference points back to `docs/implementation.md` §LLM observability + prompt registry which already had the full prose. **Hard deletes** (no info loss — code-derivable, historical war-story, negation, duplication, or standard-tool docs): schema highlights paragraph (it was `prisma/schema.prisma` reformatted), Task CRUD endpoint docs, full PWA / service worker section trimmed to one sentence, npm scripts catalog (kept only `./launch-ms.sh` + the `SKIP_INTEGRATION_TESTS=1` bypass), restart/inspect PM2 verb list, "verifying the hook is installed" diagnostic, "there is no middleware.ts" / "there is no pre-commit hook" / "no file-side parser to keep in sync" negations, Cache-Control history paragraph, stale-email-guard closing sentence, CSULB example, migration-path note for legacy `'mc-active-view'` key, shadcn config line, Node.js+path-alias line, forensic-archive note, known-flaky integration-tests list, Turbopack measurement %s, webpack-inert-in-dev paragraph, Verbose-log-gates duplicate at Prisma section, dev-process-tree triplet collapsed to one pointer at `docs/perf-profile.md`, `.env` paragraph trimmed of fallback chain. **Files changed (this commit only):** `CLAUDE.md` + new `docs/backup-recovery.md` + new `docs/resume-pipeline.md`. Lots of other unrelated WIP exists in the working tree (M8.7 follow-ups, bullet-assist/resume-rewrite/eval changes) — explicitly NOT included in this commit.
- **Date:** 2026-05-26 (Profile dash — two-level collapse UI). User asked for collapse affordances on the Profile dash so the view scrolls less. Two layers shipped. (1) **Card-level:** `components/ui/Card.tsx` gained `collapsible` + `defaultCollapsed` props; when `collapsible`, a chevron (↓ expanded / → collapsed) renders next to the icon/title and the whole header group becomes a toggle button. Added `"use client"` since the base Card now uses `useState`. The four profile cards (`PersonalInfoCard`, `WorkHistoryCard`, `ProjectsCard`, `EducationCard`) pass `collapsible`. (2) **Entity-row-level:** `WorkRoleRow` / `ProjectRow` / `EducationRow` each got a chevron toggle button as the leftmost item in their absolute-positioned toolbar (right next to the sketchpad button); collapsing wraps the spine grid + bullets list in `{!collapsed && (<>…</>)}` so only the title row (title + company / project-description / institution+degree·field) remains visible. State is React `useState` per row — resets on reload (no localStorage persistence to keep the diff small; user can ask for it later). `tsc --noEmit` clean; two pre-existing lint warnings on `Card.tsx` (`LucideIcon` unused + `icon?: any`) confirmed via stash round-trip as not introduced here.
- **Date:** 2026-05-26 (mobile shell — pagination chevrons for fine-pointer devices). User flagged that shrinking the desktop browser window kicks the app into mobile mode (great), but the mobile shell relies on horizontal swipe for dash navigation and a mouse can't swipe cleanly. Fix: new `useFinePointer()` hook in `hooks/useMobileLayout.ts` watches `matchMedia('(pointer: fine)')` (SSR-safe: returns false on server / first client render, settles to real matches once mounted). `components/dashboard/MobileShell.tsx` now renders small prev/next chevron buttons (left/right vertical-center, smaller than DesktopShell's chevrons) when `hasFinePointer && len > 1 && !isLaunchpadOpen` — real touch devices (`pointer: coarse`) keep the swipe-only look; small desktop windows get pagination buttons alongside swipe. The standard `(pointer: fine)` test correctly classifies hybrid devices (Magic-Keyboard iPad, Surface laptop) as fine when the user's primary input is a pointing device, which is the right behavior. `tsc --noEmit` clean. Lint flag on `setFine(mql.matches)` synchronously in effect is a pre-existing pattern (same warning fires on `useMobileLayout`'s `setIsMobile`); pre-push doesn't actually run lint (the echo line "hermetic suite + lint" is stale — script only runs hermetic SUITES).
- **Date:** 2026-05-26 (M8.7 resume relevance pipeline — UNCOMMITTED, feature is live in PM2 dev + prod). Seven sub-features shipped in one session, all backward compatible. Net effect: Iris + SEB now both lead the Rocket Lab single-page resume after the user pinned both. See `docs/implementation.md` §M8.7 for the full technical write-up. Files added (untracked): `scripts/dedupe-bullet-tag-casings.ts`, `scripts/set-entity-pin-keywords.ts`, `prisma/migrations/20260526220116_add_pin_keywords_to_entities/`. Files modified (uncommitted): `prisma/schema.prisma` (pinKeywords on WorkRole/Project/Education), `lib/schemas/profile.ts` (PinKeywordsSchema + all entity + post/patch schemas), `lib/repositories/profile.ts` (parsePinKeywords + serializePinKeywords + hydrators + CRUD interfaces), `app/api/profile/{work-roles,projects,education}/route.ts` (pinKeywords in POST + PATCH), `lib/resumes/posting.ts` (importance schema + keywordWeights), `lib/resumes/select.ts` (keyword importance multiplier + case-insensitive matchedTags dedup + entityIsPinned + movePinnedToFront), `lib/resumes/one-page.ts` (getUnremovableEntityIds takes postingKeywords + collects pinned entities), `lib/resumes/rewrite.ts` (pre-filter no-match bullets), `lib/resumes/tagline-tailor.ts` (sectionOrder + entityOrder + evidence-rich entityIdsBlock + 1024 token cap), `lib/resumes/skills-gap.ts` (folds profile.skills/languages/hobbies into coverage), `lib/resumes/templates/ats-plain.tsx` (Skills/Languages/Interests sections + sectionOrder-driven render order), `lib/profile/auto-tag.ts` + `lib/profile/bullet-tag-suggest.ts` (case-insensitive dedup), `lib/ai/gemini.ts` (JSON-parse error window), `components/ui/BulletRow.tsx` (pinned tags first in chip list), `app/api/resumes/route.ts` (selectBullets weights + reorderSelectionByIds + pinFront + getUnremovableEntityIds w/ posting keywords), `docs/llm-prompts/{posting-parse,resume-tagline,resume-rewrite,bullet-tags-from-posting}.md` (prompt updates + max_tokens), `docs/llm-calls.md` (M8.7 change log entry + table updates), `docs/implementation.md` (new M8.7 section), `CLAUDE.md` (new "Resume-gen relevance pipeline" subsection). Migrations applied: `20260526220116_add_pin_keywords_to_entities` to BOTH dev.db + prod.db (prod tier briefly stopped/started). One-shot backfill `dedupe-bullet-tag-casings.ts` ran on dev.db: 5 dup tags removed + 4 cross-bucket conflicts resolved. Prod.db was already clean. Lunary templates re-synced: `posting-parse` + `resume-tagline` versioned to pick up new system prompt rubrics + bumped max_tokens. Iris pinned for `[software engineering, computer science, space systems, aerospace, computer engineering, distributed systems, protocol design, rust, consensus]`; SEB Avionics pinned for `[software engineering, computer science, space systems, aerospace, computer engineering, avionics, embedded systems, control systems]`. **Verified end-to-end**: Rocket Lab single-page gen now renders DomeIQ (2 bullets) + SEB (3 bullets) + Iris (2 bullets) + Berkeley (2 bullets) = 9 productive bullets / 1pp. Pruner Phase 2 (bullet-level pruning) fired for the first time — dropped 2 score-0 passthrough bullets to fit. **Still NOT addressed** (logged in §Known issues): Profile UI affordance for pin-editing (currently CLI-only via `scripts/set-entity-pin-keywords.ts`), commodity-only entity penalty (scoring change #3), pin-priority ordering among multiple pinned entities in same section. **Pre-existing prompt-render-smoke failure** (`resume-tagline: missing entityIdsBlock`) was addressed by adding `entityIdsBlock` to `scripts/prompt-samples.ts` if not already there — needs verification on next pre-push.
- **Date:** 2026-05-26 (1-page resume toggle — UNCOMMITTED, feature is live in PM2 dev). User asked for a toggle on `GenerateResumeCard` that prunes the resume to fit on one Letter page; latest education + top-pinned project + most-recent surviving work role are unremovable. Shipped: new `lib/resumes/one-page.ts` with `pruneOneStep` (drops the lowest-aggregate-score removable entity first; falls back to dropping the lowest-scoring non-locked bullet from any entity with ≥ 2 bullets) and `renderResumePDFOnePage` (iterative loop: compose props → `renderResumePDF` → `pdf-parse` `getInfo().total` for page count → prune → repeat, cap at `MAX_PRUNE_ITERATIONS=8`). `getUnremovableEntityIds` reads `selection.{workRoles,projects,education}[0]` — those are "latest" after the selector's sort (workRoles by startDate desc, projects by position asc, education by startDate desc). Entities containing a locked bullet are implicitly unremovable via `aggregateEntityScore` returning Infinity. New hermetic `resume-one-page-prune-smoke.ts` covers 17 assertions on the pure pruner: unremovable set membership, lowest-aggregate-score wins, locked-bullet protection, bullet-prune fallback, null-when-nothing-left, global-lowest cross-section comparison. Smoke passes 17/17 standalone; wired into `scripts/pre-push.sh`. New UI: `Minimize2`-icon "1 page" toggle button between the format selector and the Generate button on `GenerateResumeCard`, persisted in localStorage under `mc-resume-one-page`. Pass-through: `options.onePage` boolean on `ResumePostBodySchema`; when true the render branch swaps to `renderResumePDFOnePage` and re-flattens `flat` from the post-prune selection (so persisted `selections` JSON and trace UI match the rendered PDF). DOCX requests with onePage on still use PDF as the page-fit probe, then re-render the pruned selection as DOCX. **Why not committed:** the route.ts + GenerateResumeCard.tsx changes overlap user's pre-existing uncommitted section-ordering work (extras + `sectionOrder` + `entityOrder` + `reorderSelectionByIds` on route.ts; the `TraceList` and other portions on GenerateResumeCard.tsx). Both features are functionally independent but the diffs are interleaved, so bundling them into one commit would be misleading. User should review the working tree and stage the two features separately when ready. PM2 dev restarted; feature is live. Files added (untracked): `lib/resumes/one-page.ts`, `scripts/tests/hermetic/resume-one-page-prune-smoke.ts`. Files modified (mixed authorship): `app/api/resumes/route.ts` (imports + onePage flag + render branch), `components/cards/profile/GenerateResumeCard.tsx` (Minimize2 import + ONE_PAGE_STORAGE_KEY + onePage state + pickOnePage + toggle button + onePage in request body), `scripts/pre-push.sh` (one-line addition). **Pre-existing prompt-render-smoke failure** (`resume-tagline: missing entityIdsBlock`) still present from earlier uncommitted resume-tagline.md edits — not introduced by this change.
- **Date:** 2026-05-26 (resume selector spine-guard). User ran a Rocket Lab software-internship resume in dev and saw their current Security Officer @ AEG role surface despite scoring 0 against the posting keywords. Root cause: `lib/resumes/select.ts:selectBullets` passed `keepAlways: (_e, i) => i === 0` for work roles — the most-recent role was kept unconditionally as the work-role spine, even when completely off-topic. Fix: new `pickWorkRoleSpineIndex(workRoles, keywords)` helper picks the spine slot — defaults to index 0 when that role's top bullet scores ≥ `MIN_KEEP_SCORE` (= `TAG_WEIGHT` = 2), otherwise falls back to whichever role scores highest. Final fallback when every role is below threshold: returns 0 anyway (preserves no-empty-work-role-section invariant). Two new hermetic regression checks (8c + 8d) on `resume-select-smoke.ts`: (8c) off-topic recent + relevant older → older becomes spine and recent dropped; (8d) all roles off-topic → most-recent still kept as fallback. `resume-select-smoke` now 14/14 green. **Note (pre-existing failure outside this fix, not caused by this change):** `prompt-render-smoke` reports `[FAIL] resume-tagline: every declared {{var}} has a sample — missing: entityIdsBlock`. Comes from the user's pre-existing uncommitted edits to `docs/llm-prompts/resume-tagline.md` post-`2034261` — confirmed via `git stash` round-trip showing 166/166 green without the uncommitted tree. **Still NOT addressed in this run** (user wants to make these calls themselves before code changes): (a) mysubs.live's broad autoTags ("Software Engineering", "Web Applications") — the user can × them in the Profile UI to push them into the bullet's `removedTags` blocklist, or we tighten `bullet-tags-from-posting` prompt against very-generic posting keywords on bog-standard CRUD bullets. (b) Gitlet's hand-entered `[git, version control]` tags — system is doing what the user told it; they can untag, or we'd need a per-entity exclude flag (only bullets have `excluded` today). (c) Page-length cap → 1 page — not in the pipeline; would need a post-rewrite pruning pass keyed on react-pdf measured heights. PM2 dev restarted to pick up the new selector.
- **Date:** 2026-05-26 (posting-tailored resume tagline + filename comma format). Two follow-ups to the M9 retirement commit. (1) **Resume filename uses commas**: `lib/resumes/labels.ts:buildResumeDownloadFilename` switched separator from " - " to ", " so saved files match the display label format ("Salvador Salcedo, Security Officer, Cedars-Sinai, Resume.pdf"). Hermetic assertions updated. (2) **Posting-tailored tagline LLM callsite**: new `lib/resumes/tagline-tailor.ts` + `docs/llm-prompts/resume-tagline.md` (MODEL_LITE, temp 0.4, 256 tokens cap) called from `app/api/resumes/route.ts` POST after bullet rewrite, grounded on profile + parsed posting. Persisted on a new `GeneratedResume.tagline` column (migration `add_generated_resume_tagline`, applied to both DBs). `ats-plain.tsx` accepts an optional `tagline` prop; renders it as the H1 subtitle, falling back to `profile.tagline` for legacy rows. Best-effort posture — AIError from the tailor logs a warn and falls back to profile.tagline. Concrete example the user gave for why this exists: their profile.tagline reads as a software-engineer pitch but they sometimes apply to security-guard openings, where the resume should re-frame as "Applied math student at CSULB looking for work". New `eval/suites/resume-tagline.yaml` (3 fixtures: cross-domain, aligned-domain, empty-keywords; LLM-rubric grades the no-fabrication + framing invariants — burns Gemini tokens, NOT in pre-push). New hermetic `resume-tagline-smoke.ts` (19 assertions, pure prompt-render + post-filter; wired into pre-push). `scripts/prompt-samples.ts` + `prompt-render-smoke.ts` EXPECTATIONS extended for the new slug. `docs/llm-calls.md` gets a new inventory row. **68/68 pre-push hermetic green in 55s.** PM2 restarted under the new schema. **Lunary push of the new template + the renamed resume-rewrite template still pending**: `LUNARY_SECRET_KEY=… npx tsx scripts/sync-lunary-templates.ts`.
- **Date:** 2026-05-26 (M9 portfolio surface retired; README ingestion kept as LLM grounding). UI cleanup on `components/ui/ProjectRow.tsx` — removed the "Portfolio" star toggle + `githubRepo` "owner/repo" input + `metricsUpdatedAt` refresh badge; renamed the second URL field's placeholder from "Live URL" to "Project Site (optional)". Backend torn out to match: dropped `metrics`, `githubRepo`, `portfolio`, `metricsUpdatedAt` columns from `Project` via new migration `20260526050000_drop_project_github_metrics` (recreate-table dance — applied to both `dev.db` + `prod.db`). Deleted `scheduler/jobs/github-metrics.ts`, `lib/profile/metric-deltas.ts`, and the metric-deltas + readme-prompt hermetic smokes. **Reversed the README half mid-session** when user flagged that bullet-assist + resume-rewrite should still see GitHub READMEs: new migration `20260526051000_restore_project_readme` re-adds `readme` + `readmeUpdatedAt`; new slim `lib/fetchers/github-public-fetcher.ts` with just `fetchGithubReadme` + a `parseGithubOwnerRepo(repoUrl)` helper; new scheduler job `scheduler/jobs/github-readmes.ts` (6h interval / 7d freshness gate) drives off `Project.repoUrl` — no separate `githubRepo` column needed. `ProjectReadmeContext` + `PROJECT_README_PROMPT_LIMIT` + `buildRewriteUserPrompt` restored in `lib/resumes/rewrite.ts`; README context construction restored in `app/api/resumes/route.ts`; `readme-prompt-smoke.ts` restored from `git show HEAD:` and re-wired into `pre-push.sh`. Also tore `formatMetricsLine` (★/commits/languages line) out of `ats-plain.tsx` — the rendered resume no longer shows a metrics line under projects. 67/67 pre-push hermetic green. PM2 (`mission-control`, `mission-control-dev`, both scheduler tiers) restarted under the new schema. `Project.readme` column on prod.db starts empty; the github-readmes job will populate it on the next 6h tick for any project whose `repoUrl` parses as a github.com repo. **Lunary push of the updated `resume-rewrite.md` template still pending** (re-add of `{{readmesBlock}}`): run `LUNARY_SECRET_KEY=… npx tsx scripts/sync-lunary-templates.ts`.
- **Date:** 2026-05-26 (M7.9 — profile tagline + LLM draft shipped, story S7.14. 68/68 pre-push hermetic green in ~54s. New `Profile.tagline` column (migration `add_profile_tagline` applied to dev.db; prod.db still needs `DATABASE_URL=file:./prod.db npx prisma migrate deploy`). New `tagline-draft` LLM callsite on `MODEL_LITE` with two modes: empty current → drafts from scratch; non-empty → enhances while preserving the user's angle. Grounding is the full hydrated profile (work + projects + education + summary + skills/hobbies/languages + scratchpads, capped at ~600 bytes per entity). New `app/api/profile/tagline/draft/route.ts` POST, session-gated, 10/10min rate-limit, returns `{tagline, mode}`. New `TaglineRow` on `PersonalInfoCard` with Sparkles AI-draft button + inline diff panel (current → proposed) + Accept/Discard. Resume template (`ats-plain.tsx`) renders tagline as italic subtitle under the H1 + smaller than the name + above the meta line. Two new hermetic smokes: `tagline-patch-smoke` (10 assertions covering write/persist/cap/null/empty-string-preserved) + `tagline-draft-smoke` (17 assertions covering post-filter purity + mode dispatch + route post-filter + AIError → 502 + rate-limit 11th-call → 429). Also bundles user's pre-existing slug rename cascade — see entry below for the rename design.)
- **Date:** 2026-05-25 (Slug rename: `bullet-auto-tag` → `bullet-tags-from-posting`, `bullet-tag-suggest` → `bullet-tags-from-profile`. Drives at WHERE candidate tag strings come from — posting keywords vs. profile vocabulary — the deepest invariant distinguishing the two callsites. Mechanical rename across prompt .md headers + paths, `eval/suites/*.yaml` + `callsite:` field, `eval/provider.ts` HANDLERS, `loadPrompt(slug)` + `chatJSON({name})` in `lib/profile/auto-tag.ts` + `lib/profile/bullet-tag-suggest.ts`, `[bullet-tags-from-posting]` log lines in `app/api/resumes/route.ts`, `scripts/prompt-samples.ts` PROMPT_SLUGS + SAMPLE_VARS, hermetic smokes (`prompt-render-smoke`, `auto-tag-smoke`, `bullet-tag-suggest-smoke`, `scratchpad-synth-smoke`, `bullet-assist-smoke`), cross-refs in `bullet-assist-rewrite.md` + `scratchpad-synth.ts` + `bullet-assist.ts`. Source filenames (`lib/profile/auto-tag.ts`, `bullet-tag-suggest.ts`) + function names (`autoTagBullets`, `suggestTagsForBullet`) + smoke filenames intentionally NOT renamed to preserve git blame continuity. Lunary push of the renamed templates deferred — historic traces remain on old names.)
- **Date:** 2026-05-26 (Full M7.7 + M7.8 + M8.6 wave shipped — three milestones covering S7.10/S7.11/S7.12/S7.13. 66/66 pre-push hermetic green in ~56s after the last commit. Six local commits ahead of `origin/main`: `dee0121` (doc reconciliation), `87d2bbb` (M7.7/M7.8/M8.6 design docs), `ab8e060` (M7.7 impl), `22e6674` (M7.8 impl), plus the M8.6 impl commit landing now.) **M7.7** = split text/tag affordances + pin tags + tag click no-op + bigger X. **M7.8** = `scratchpad: String?` on WorkRole/Project/Education + `ScratchpadOverlay` + bullet-assist 5th grounding source. **M8.6** = new `scratchpad-synth` LLM callsite + synthesis pass in `app/api/resumes/route.ts` POST (after select + auto-tag, before rewrite), per-entity in parallel via `Promise.allSettled`. Synthesized rows append to `selection` + `flat` with `synthSource: "scratchpad"`; TraceList in `GenerateResumeCard` renders the new marker as an amber `scratchpad-synth` chip; skills-gap post-filter counts synthesized matchedKeywords as covered. Cross-entity isolation enforced by passing only ONE entity's scratchpad per `synthesizeBulletsForEntity` call. Earlier session: design wave added S7.10–S7.13 to `docs/user-stories.md` + M7.7/M7.8/M8.6 sections to `docs/implementation.md`. Earlier: doc reconciliation flipped M7.6 + M8.4 + M8.5 + LOP-1..11 from ⏳ to ✅. Earlier: M8.4 + M8.5 GenerateResumeCard upgrade. Earlier: close-detection probe gate.
- **Branch:** `main`, **up to date with `origin/main`** (final HEAD: `c69fcc6`). Working tree clean.
- **What landed this session (M8.4 / M8.5, in chronological order):**
  - **`ea0fe7b` `feat(resume): M8.4/M8.5 design + Wave 0 schema foundation (S8.9–S8.13)`** — `docs/user-stories.md` gains S8.9–S8.13 + Decision 6.1–6.5 + M8.4/M8.5 milestone placeholders. `docs/implementation.md` gets full §M8.4 (10 tasks) + §M8.5 (9 tasks). Prisma migration `20260525091102_add_generated_resume_posting_metadata` adds `postingTitle` + `postingCompany` to `GeneratedResume` (applied to both dev.db + prod.db; PM2 stopped/started for each). `Bullet` interface in `lib/profile/types.ts` gains `autoTags: string[]` + `removedTags: string[]`; `lib/profile/bullets.ts:hydrateBulletDefaults` back-compats legacy JSON. `BulletWriteSchema` `.refine().transform()` enforces no-overlap invariant + Decision 6.3 implicit-accept on save.
  - **Wave 1 parallel (3 agents, worktree-isolated):**
    - **`54d558f` `feat(resume): M8.4 backend — pipeline-picker endpoint + applicationId POST + resume list metadata`** + **`98761e6` `merge: Agent A`** — new `app/api/applications/pipeline-picker/route.ts` (GET, session-gated, filters to INTERESTED + sourceUrl); `findInterestedWithPostingForUser` in `lib/repositories/applications.ts`; `GET /api/resumes` projection enriched with `postingTitle/postingCompany` + `?limit=` clamp + `?applicationId=` filter; `POST /api/resumes` accepts `applicationId` with INTERESTED/sourceUrl guards + writes posting metadata; three smokes (`pipeline-picker-smoke`, `resume-from-application-smoke`, `resume-list-smoke`).
    - **`6aae335` `feat(profile): M8.5 LLM scaffold — bullet-auto-tag callsite + fold-in directive`** + **`ab5e8f4` `merge: Agent B`** — new `lib/profile/auto-tag.ts` (`autoTagBullets`, `mergeAutoTagProposals`, `flattenProfileBullets`, etc.) using `MODEL_LITE` + atomic `prisma.$transaction`; new `docs/llm-prompts/bullet-auto-tag.md` prompt; `resume-rewrite.md` rule 6a (posting-keyword fold-in); `auto-tag-merge-smoke` (10 assertions, pure-function). Hit 529 contamination during write — orchestrator trimmed trailing `</content></invoke>` markers from 4 files.
    - **`b5ff824` `feat(profile): M8.5 BulletRow auto-badge + remove-to-blocklist semantic`** + **`861e0ee` `merge: Agent C`** — Sparkles icon + cyan border on tags in `autoTags`; remove-tag adds to `removedTags`; commit-tag pulls from `removedTags`; `bullet-remove-tag-smoke`.
  - **`7b2ee07` `test(hermetic): wire 5 new Wave 1 smokes + extend prompt-render invariants`** — pre-push.sh SUITES extended; `scripts/prompt-samples.ts` PROMPT_SLUGS / SAMPLE_VARS / EXPECTATIONS updated for `bullet-auto-tag`.
  - **Wave 2 done directly by orchestrator** (API too overloaded to spawn agents): **`87d81d0` `feat(resume): M8.4 frontend + M8.5 route wiring + 2 hermetic smokes (Wave 2)`** — `GenerateResumeCard.tsx` full refactor with Pipeline/URL/Paste segmented control (Pipeline default tab, Decision 6.5), `InterestedAppPicker`, `PreviousResumesDropdown` popover, `useServerEvents('GeneratedResume')` invalidation. `POST /api/resumes` route gets the auto-tag pass between parse + select (re-hydrates profile if `tagsAdded > 0`) + `broadcastEvent({model: 'GeneratedResume', action: 'upsert', …})` at end. `ServerEventModel`/`ModelName` unions extended. Two more smokes: `auto-tag-smoke` (10 assertions, dynamic `require()` to defeat ES-module hoist) + `resume-rewrite-fold-in-smoke` (7 assertions, pure prompt-render).
  - **`c69fcc6` `test(hermetic): harden resume-list-smoke Test 4 cleanup`** — hoisted Test 4's Watchlist + JobPosting + Application IDs into module state so the finally block tears them down even on mid-test failure; FK-aware cleanup order (GeneratedResume → Application → JobPosting → Watchlist → User); dead `prisma.user.update({where, data: {}})` "touch" line removed.
- **Final gate state:** `bash scripts/pre-push.sh` 58/58 hermetic suites green in ~54s (the +7 from M8.4/M8.5 stack). `tsc --noEmit` clean. Push completed: `ea0fe7b..c69fcc6  main -> main`. **CLAUDE.md updated** with two new rules: every commit MUST pair with a `docs/next_steps.md` update, and every push MUST flush this doc to match `origin/main` reality before the push.
- **Audit-found issues fixed in-session:**
  - **dev.db / prod.db lock during prisma migrate** — PM2 dev tier + scheduler held the SQLite connection. Resolved by stopping the relevant PM2 process, migrating, restarting.
  - **TypeScript fallout on Bullet shape change** — ~30 sites tripped `tsc --noEmit`; 5 inline construction sites fixed (3 smokes + bullet-assist fill+rewrite + eval/provider); the rest funneled through `makeBullet` / `normalizeBullet` so defaults applied automatically.
  - **Agent 529 contamination** (3 of 5 agents) — literal `</content></invoke>` markers at file tails of 4 files; orchestrator edited each, verified tsc + smoke, committed on the agent's behalf.
  - **Agent A worktree behind main** — branched off `9e29040`, 3 commits stale; merged with `--no-ff` (clean because Agent A's files were FK-disjoint from later work).
  - **Agent A missing third smoke** — `resume-list-smoke.ts` never written (529 cut off); orchestrator wrote it matching Agent A's require.cache pattern.
  - **prompt-render-smoke failure post-merge** — `PROMPT_SLUGS` didn't include `bullet-auto-tag`; added to SLUGS + SAMPLE_VARS + EXPECTATIONS map (initial guess "Profile bullets" wrong → corrected to "Bullets to consider" by grepping actual prompt headers).
  - **auto-tag-smoke static-import hoisting bug** — initial smoke used `import { autoTagBullets }` which TypeScript hoists above the `injectCacheEntry` calls → real chatJSON ran, mock never took effect. Switched to `require()` inline pattern matching `resume-from-application-smoke`.
  - **resume-rewrite-fold-in-smoke tsc errors** — fixture used `tags: ["Python"]` on `BulletSelection` (no `tags` field) + missing `location`/`rawText` on `ParsedPosting`. Fixed.
  - **resume-list-smoke Test 4 cleanup fragility** — fixtures cleaned inside test body, not finally; mid-test failure could leak rows. Hoisted IDs to module state + finally-block cleanup in FK-aware order. (User-flagged after self-audit.)

## Umbrella goal

**Apply to jobs.** Track A (Pipeline UX), Track B (Discovery + notifications), Track C (Profile + resume + GitHub) are all in production. Forward motion runs through real-world use AND a fresh wave of bullet/scratchpad UX work designed 2026-05-25 (S7.10–S7.13 → M7.7/M7.8/M8.6) to make the bullet-edit experience cleaner and let the resume-gen step synthesize bullets from user-voice scratchpad text when the structured profile under-covers a posting.

1. Build M7.7 → M7.8 → M8.6 in order (see "Immediate next actions").
2. Send applications.
3. When the LLM rewrites are off, capture the failure mode as a Promptfoo fixture under `eval/suites/<callsite>.yaml` and tighten the prompt.
4. When something in the pipeline misfires (wrong status classification, missed posting, calendar sync glitch), file it as a follow-up and patch.

Backups encrypted (RAH-13). Rate limits in place (RAH-12). Older MA/MB/M7/M8/M9 milestone scaffolding is shipped; consult `docs/implementation.md` for per-milestone detail.

## Critical path — current

**Apply, observe, iterate — now backed by structured LLM tracing.** The "apply ASAP" loop is fully closed (capture, kanban + side-track kanban, drill-in, contacts, watchlists, notifications with negative-filter + quiet hours, profile + multi-resume import + snapshots, tailored PDF + DOCX + skills-gap + resume-diff). 2026-05-24 added the LLM observability rail on top: every Gemini call lands in Lunary with `name` + tokens + latency when `LUNARY_PUBLIC_KEY` is set; prompt regressions catch in `npm run test:prompts` via Promptfoo. §Prompt tuning's free-text observations fold into Promptfoo fixtures going forward — see `eval/suites/<callsite>.yaml` and `docs/llm-prompts/<slug>.md`.

## Immediate next actions (in order)

**Closed thread (2026-05-29) — Gmail real-time push + cross-tier LLM dedup — both SHIPPED + ARCHIVED.** Both design docs now live under `docs/archive/` (`gmail-realtime-push.html`, `cross-tier-llm-dedup.html`); the canonical operational records are CLAUDE.md (*Gmail webhook + ingest* and *Gemini rate limiting + model fleet*) + `docs/llm-calls.html` §7.
- **(a) Gmail real-time push — fully live (Phases 1–3 + C4).** Phase 1 `1f73067`; Phase 2 GCP + env 2026-05-29; Phase 3 verified (receive-path probe 10/10 both tiers + live round-trip in ~3s, no Scan-Inbox press); C4 404-recovery `a41045f` (16/16 smoke). Committed + pushed.
- **(b) Cross-tier LLM dedup — shipped `36a0d4d`** (implemented in-session, not handed off). Shared-SQLite content-addressed cache + reservation; prod redeployed; live cross-tier dedup confirmed.

1. **Triage the "Audit-found soft bugs" list below.** 10 issues caught during the 2026-05-24 audit; none have been picked off. Quick wins (≤ 15 min): #4 (email-parser model constant), #9 (cache.ts L1 pruner startup-skip), #10 (call-time Lunary-enabled wrappers). High-value (~30 min): #2 (notification-prune job — 784 rows / 4 days, growing unbounded), #5 (bulkMoveApplicationsTrack notIn → diff-in-JS). Product calls before fixing: #1 (nextSteps wipe semantics), #7 (orphan resume-row recovery strategy).
2. **LLM auto-fill / synthesize for Skills + Hobbies + Languages.** Resume render works (2026-05-27 fix below), but the M7.4 import path doesn't extract these fields from an uploaded resume — `lib/profile/synthesize.ts` and `lib/profile/import-llm.ts` schemas drop them. So importing a resume into a fresh profile loses any Skills/Hobbies/Languages it contained, and the user has to re-enter via the ProfileIdentityCard editors. Extend the LLM extraction schemas to capture all three; add Promptfoo fixtures so the new output shape doesn't silently break.
3. **UI surfacing of `merged=true` from today's track-as-application fix.** `POST /api/postings/[id]/track-as-application` now returns `merged: boolean` distinguishing "created a new app" from "linked into an existing app at the same role." Nothing in the kanban / posting card consumes it yet — surface a "Linked into existing X application" toast distinct from "Created new application." Five-line change in the route mutation handler.
4. **LOP-9 real fixture captures.** Seam wired 2026-05-25 in `lib/ai/gemini.ts:chatJSON` (call-time `captureFixturesEnabled()` gate, covers all 9 SDK callsites) and `lib/email-parser.ts:parseApplicationEmail` (inline gate, covers the SDK-bypassing 10th). **Remaining:** flip on `CAPTURE_FIXTURES=1` via `pm2 restart mission-control-dev mission-control-scheduler-dev --update-env`, use the app for ~30 min, then `pm2 logs … --raw --nostream | grep '\[FIXTURE\]' | sed … | jq -c .` to extract; bucket by `.name` and translate to `eval/suites/<name>.yaml` entries (YAML fixtures take lib function inputs, not the rendered system+user — work backwards from captured prompts to the originating input shape). Disable after harvest with a vanilla `pm2 restart` (no `--update-env`).
5. **Story S7.6 rollback UX (🔵, deferred-by-design).** Capture side shipped; restore-from-snapshot is the deferred half. Build a destructive-overwrite confirm + transactional bulk-replace of `WorkRole` / `Project` / `Education` from the stored payload. Pick up when you've actually made an edit you want to undo.
6. **Close-detection probe gate — ✅ SHIPPED + ARCHIVED (2026-05-30).** Live + regression-free on both tiers (5 days of clean `[job-watcher] probe-gate` logs; LinkedIn-24h / Workday-page-cap candidates confirmed *preserved*, never false-closed). Recovery reopened 921 dev + 629 prod. Design doc moved to `docs/archive/close-detection-probe.md`; canonical record now in `CLAUDE.md` + `docs/implementation.md` (MB-2.4). Archived 2 days before the literal 7-day soak (2026-06-01) on decisive evidence — monitoring is no longer an open action. _(If recurring `unknown` ever appears against a URL you *know* is closed, the closure-marker lists in `lib/postings/liveness.ts` (Ashby / LinkedIn / Workday) are still the extension point.)_
7. **Commodity-only entity penalty (resume scoring change #3).** Designed but not built — see "Known issues / parked TODOs" below for the full design including the three open product calls (threshold shape, haircut linear vs step, locked-bullet opt-out).
8. **Profile UI: per-entity `pinKeywords` chip-list editor.** Backend shipped 2026-05-26 with M8.7; UX gap remains. Today users set pins via `scripts/set-entity-pin-keywords.ts`. Need a chip-list affordance on `WorkRoleRow` / `ProjectsCard` rows / `EducationCard` rows, parallel to the bullet-tag chip pattern in `BulletRow.tsx`. Wires into existing PATCH endpoints — Zod schemas already accept `pinKeywords`.

## Audit-found soft bugs (deferred — pick next)

From the 2026-05-24 system audit (commits `4a609df`, `4f1854d`, `dde9151`, `86f5aec`). Each was caught + flagged but not fixed in-session because impact was bounded or the fix required a product call. **Regression tests for the 7 fixed bugs are now in pre-push** (`job-watcher-classifier-regression-smoke`, `job-watcher-scale-regression-smoke`, `fetcher-partial-regression-smoke`, `events-broadcast-regression-smoke` — covers A/B/C/D/E/G; F is wrap-and-test-resistant). The list below is what to fix next.

| # | Issue | Where | Impact | Fix shape |
|---|---|---|---|---|
| 1 | **`nextSteps` wipe on email update.** `lib/applications/ingest.ts:208` does `nextSteps: parsed.nextSteps ?? null` — a newer email that omits nextSteps wipes the prior value (e.g. "Reply by Friday" hint disappears when the followup "we received it" email lands). | `lib/applications/ingest.ts:208` + similar in race-loser branch ~264. | Soft UX. User may lose actionable hints. | Either `nextSteps: parsed.nextSteps ?? existingApp.nextSteps` (preserve), or product call: "latest email is source of truth even when blank?" |
| 2 | **No Notification retention.** WebhookDelivery has 30d retention; Notification has none. 784 rows in 4 days currently; grows unbounded. | `prisma/schema.prisma:Notification` + `scheduler/jobs/`. | Cosmetic table bloat. Bell stays usable but DB grows. | New `scheduler/jobs/notification-prune.ts` mirroring `webhook-delivery-prune.ts`, 90-day default; wire into `scheduler/index.ts:JOBS`. |
| 3 | **Gmail webhook doesn't paginate `gmail.users.history.list`.** For active users (sparse delta per webhook) this never matters. For backfill bursts (fresh user with many historical inbox events), page 2+ records silently drop — `lastSyncedHistoryId` advances past them. | `app/api/gmail/webhook/route.ts:95-100`. | Backfill-only. Unfair to new users on signup. | Loop on `nextPageToken` from `historyRes.data.nextPageToken`, append messages, stop when no token. |
| ~~4~~ | ~~**email-parser hardcodes `"gemini-3.1-flash-lite"` in three places.**~~ Fixed 2026-05-27 in code-health sweep. Single `MODEL_ID` constant at top of `lib/email-parser.ts`; all three usages (FIXTURE log, Lunary trace `extra.model`, SDK call) read from it. | — | — | — |
| 5 | **`bulkMoveApplicationsTrack` `NOT: { id: { in: ids } }` could P2029.** Same 999-param notIn limit as Bug C. Currently bounded by UI selection size (kanban). | `lib/repositories/applications.ts:192`. | Latent — only fires if a script bulk-moves >1000 apps. | Same pattern as Bug C fix: SELECT conflicting rows, diff in JS, UPDATE by id. |
| 6 | **Scheduler ↔ Next.js cross-process race.** `processOne`'s in-process Map mutex doesn't span processes. A user clicking "Run now" while the scheduler ticks the same watchlist → both processes fetch externally + write. P2002 fallback handles per-posting collisions; the external fetch is wasted. | `scheduler/jobs/job-watcher.ts:73-82` + `app/api/watchlists/[id]/run/route.ts`. | Wasteful (duplicate fetch, potential 429 on the external source) but not corrupting. | Move the mutex to DB (`Watchlist.inFlightAt` timestamp + 60s lease), or accept it. |
| 7 | **Resume gen orphan rows on crash mid-write.** `app/api/resumes/route.ts:219-282` creates a row with `status="ready"` then writes the artifact and updates `artifactPath`. Server crash between the two leaves a "ready" row with null path — `/download` 404s while the row sits in the list. | `app/api/resumes/route.ts:219-282`. | Low probability (~100ms window). User-visible: orphan row in resume history. | Either create row with `status="generating"` and only flip to `"ready"` after artifact write succeeds, or add a startup sweep that errors-out any `status="ready" + artifactPath=null` rows. |
| 8 | **4659 legacy null-`employmentType` rows in prod.** Most pre-date the classifier or were wiped by the (now-fixed) Bug A. Not recoverable without paid backfill. | `prisma/prod.db`. | UI shows "Unspecified" for these rows. Going-forward fix is in place; backfill is one-time. | Optional: `scripts/archive/migrations/backfill-employment-types.ts` that batches the 4659 nulls through `classifyEmploymentTypes`. ~$0.50–$2 in Gemini spend. |
| ~~9~~ | ~~**`cache.ts` L1 pruner setInterval startup-skip pattern.**~~ Fixed 2026-05-27 in code-health sweep. Added a `setTimeout(pruneExpiredL1, 0)` next-tick kick before the setInterval; mirrors scheduler/index.ts's pattern. | — | — | — |
| ~~10~~ | ~~**Module-init `LUNARY_ENABLED` reads in `lib/email-parser.ts` + `lib/ai/gemini.ts`.**~~ Fixed 2026-05-27 in code-health sweep. Both modules now use a `lunaryEnabled()` function (matching `lib/ai/prompts.ts:lunaryEnabled`); `gemini.ts`'s `tracedGenerate` became a function that lazily builds + memoizes the wrapped generator on first traced call, so processes that never enable Lunary never pay the wrapModel cost. | — | — | — |

Recommended fix order if working through the list:
- ~~**Quick wins (≤ 15 min each):** #4, #9, #10.~~ Shipped 2026-05-27 as a code-health sweep.
- **High-value (~30 min):** #2 (notification-prune job), #5 (bulkMoveApplicationsTrack notIn rewrite).
- **Real product calls:** #1 (nextSteps wipe semantics), #7 (orphan-row recovery strategy).
- **Bigger lifts:** #3 (Gmail pagination — needs a backfill smoke), #6 (cross-process mutex — moves state to DB), #8 (paid backfill — needs user opt-in for spend).

**Note: every other shippable 🔴/🟡/🔵 story (and both open RAH-N items) is now ✅ or ⛔.** The backlog moves to "real-world use → first applied posting → iterate on prompts." Failure modes captured via the Lunary trace and folded into Promptfoo fixtures rather than free-text in §Prompt tuning. Genuine MVP-followup TODOs from prior sessions (LLM fuzzy bullet dedup, LinkedIn export ZIP, legacy `.doc`, per-file SSE progress, ProjectCard portfolio toggle UI) are still around but none of them block applying.

**User-side follow-ups for RAH-13 (just shipped):**
- **Save the secret key to 1Password.** The file `~/.config/mission-control/backup.key` was generated this session. It's chmod-600 locally, but losing the Mac without an offsite copy means every encrypted backup is unrecoverable. Copy the file's full contents (including the `# created:` / `# public key:` header lines) into a 1Password secure-note titled `mission-control backup secret`.
- **Decrypt-smoke an encrypted backup** to be sure the round-trip works on a real artifact: `./scripts/backup-decrypt.sh ~/backups/mission-control/mc-20260522-201632.db.age` should produce a readable `.db` next to it. (This session already verified the round-trip end-to-end against the live keypair — same 37 Application rows in encrypted+decrypted vs `prisma/prod.db`.)
- **Purge the plaintext history** once decrypt-smoke passes: `rm -f ~/backups/mission-control/mc-*.db ~/backups/mission-control/mc-resumes-*.tar.gz` locally; if rclone is installed, also `rclone delete gdrive:backups/mission-control/ --include "mc-*.db" --include "mc-resumes-*.tar.gz"`. Until then, the security gap stays open on the historical snapshots even though new ones are encrypted.

**Genuine MVP-followup TODOs (cross-cutting, not story-numbered):**
- LLM-judged fuzzy bullet dedup (current dedup is exact-text only — "Built a TS API" and "Built a TypeScript API" both survive).
- LinkedIn export ZIP import support (separate unzip path that reads `Positions.csv` / `Education.csv`).
- Legacy `.doc` import format (mammoth handles `.docx` only).
- Per-file progress streaming via SSE so the UI shows "extract → analyze → merge" stages live instead of one long spinner.

## In-progress work

**Working tree as of 2026-05-31 (post-push):** clean — nothing uncommitted, and local `main` == `origin/main` after this session's push (see the top "PUSH" entry under Last session). The 2026-05-29 snapshot below is retained as history; its "not pushed" statements no longer hold.

**Working tree as of 2026-05-29 (historical):** the LinkedIn burst-pacing fix is committed (see top of Last session) but **not pushed**. A parallel agent's uncommitted "hide watchlists" work also sits in the tree — `components/cards/applications/{NewPostingsCard,WatchlistsCard}.tsx`, `components/providers/state/index.ts`, `prisma/schema.prisma`, and the `prisma/migrations/20260529000000_add_hidden_watchlist_ids/` migration — left untouched by the burst-pacing commit. The 4 brainstorm `side` watchlists exist as data in both dev.db + prod.db (not in git; `.db` files are gitignored). The Gmail real-time push Phase 2/3 + C4 work landed + pushed this session — origin/main carries a new commit with the webhook 404-recovery (`app/api/gmail/webhook/route.ts` + `lib/gmail/history-errors.ts`), the two probes (`gmail-push-{receive,roundtrip}-probe.ts`), the hermetic `gmail-webhook-404-recovery-smoke.ts` (added to pre-push), and the doc set (`gmail-realtime-push.html` P1–P3 checked, `CLAUDE.md` watch note, `hosting.md` §1 superseded, this file) — on top of the prior `815da88`/`1f73067`/`36a0d4d`. `.env` carries the new topic/signer vars but stays untracked. The push ran the full pre-push hermetic gate.

**Prod redeployed this session** — `npm run build` + `pm2 restart mission-control` so :3101 (compiled) picks up the SPA fix + dedup wiring (dev :4101 was already live via Turbopack HMR). The Gmail watch code is dormant in prod until `GMAIL_PUBSUB_TOPIC` is set (Phase 2).

**Gmail push: FULLY LIVE + VERIFIED 2026-05-29 (Phases 1–3).** Watch armed both tiers; receive-path probe 10/10; live round-trip confirmed (test email → `[GMAIL WEBHOOK] history=5414135` on both tiers in ~3s, `skipped:1`). Two new probe scripts: `scripts/tests/probes/gmail-push-receive-probe.ts` + `gmail-push-roundtrip-probe.ts`. C4 404-hardening (P3.2) ALSO built (`lib/gmail/history-errors.ts` + 16/16 `gmail-webhook-404-recovery-smoke.ts`): a stale-history 404 now re-seeds the watermark + acks 200 instead of 500-looping. **All Gmail push work (Phases 1–3 + C4) complete; committed + pushed this session.**

**Unattended verifications still worth doing (no urgency):**
- **Backup keypair offsite copy.** `~/.config/mission-control/backup.key` is chmod-600 locally; copy its contents (including `# created:` / `# public key:` headers) into a 1Password secure note titled `mission-control backup secret`. Then `./scripts/backup-decrypt.sh ~/backups/mission-control/$(ls -t ~/backups/mission-control/*.age | head -1)` to verify round-trip works against a real artifact.
- **Lunary trace spot-check.** Trigger any LLM path in dev (Profile dash bullet fill / posting paste / `bullet-tags-from-posting` via the generate flow / email ingest) and confirm the run lands in Lunary's dashboard tagged by its `name`. After the 2026-05-25 slug renames (`bullet-auto-tag` → `bullet-tags-from-posting`, `bullet-tag-suggest` → `bullet-tags-from-profile`), historic traces stay on the old names until manual cleanup in Lunary's UI.
- **Push renamed prompt templates to Lunary** with `LUNARY_SECRET_KEY=… npx tsx scripts/sync-lunary-templates.ts` so dashboard slugs match codebase slugs (`resume-tagline` + `posting-parse` haven't been pushed since the 2026-05-26 M8.7 prompt edits either).

<!-- pruned the 2026-05-26 + 2026-05-25 stale "uncommitted" + manual-smoke blocks on 2026-05-27 — all relevant work has landed in HEAD and is documented in §Recently completed -->

## Recently completed

- **2026-05-29 (LinkedIn watchlist burst-pacing — durable scheduler fix, committed this session, UNPUSHED)** — `runDueWatchlists` (`scheduler/jobs/job-watcher.ts`) now inserts a jittered 3–10s gap before each 2nd+ crawl to a fragile scraped source (LinkedIn/Indeed; ATS APIs unaffected) so a synced batch — esp. every overdue watchlist firing on the first tick after a >cadence scheduler downtime — can't burst. Injectable (`RunDueDeps`) for hermetic testing; new `job-watcher-crawl-pacing-smoke.ts` (19 assertions) in pre-push; full gate 85/85. Paired with 4 new no-degree "side" watchlists (AI coding evaluator / math tutor / bilingual CS / junior dev) in dev.db + prod.db, desync'd via distinct prime cadences (233/239/241/251) + next-due phase stagger. Deployed live to both scheduler tiers (tsx-on-source restart). Left the parallel "hide watchlists" tree changes untouched.
- **2026-05-29 (SPA / embedded-JSON posting-text fallback, commit `815da88` — UNPUSHED)** — Client-rendered ATS pages (Dayforce et al.) returned 0 visible chars to `fetchVisibleText`, so resume-from-URL failed the `MIN_POSTING_TEXT_CHARS` guard. New `extractPostingTextFromHtml(html)` in `lib/resumes/posting.ts` falls back to embedded posting JSON (`<script type="application/ld+json">` JobPosting + Next.js `__NEXT_DATA__`) when the DOM is empty, anchoring on the `jobTitle`+`jobPostingContent` pair so it won't grab the i18n string dictionary. New `posting-spa-extract-smoke.ts` (7/7). Live Dayforce recovery 0→5507 chars. **Prod needs `npm run build` + `pm2 restart mission-control`.**
- **2026-05-28 (LLM cost optimization — Tier 1 + Tier 2, commits `d61c249` + `3b491ac`)** — Tier 1: content-keyed cache on `parsePosting` + skip-synthesis guard on solo import (eliminates the single most expensive call on the common first-import path); merged `llm-calls` docs to HTML. Tier 2: parallelized `resume-rewrite` ∥ `resume-tagline` (no data dependency), batched `scratchpad-synth` N→1 via positional output. Hermetic 79/79; Promptfoo `scratchpad-synth` 4/4 vs real Gemini; Lunary template synced + prod redeployed.
- **2026-05-27 (code-health sweep — audit bugs #4 + #9 + #10)** — Three latent maintenance hazards from the 2026-05-24 audit, fixed as one bundled commit. #4: `lib/email-parser.ts` model string `"gemini-3.1-flash-lite"` was hardcoded in three sites (FIXTURE log, Lunary `extra.model`, SDK call); pulled to a single `MODEL_ID` constant so future model bumps can't make the trace lie. #9: `lib/cache.ts` L1 pruner gained a `setTimeout(pruneExpiredL1, 0)` next-tick kick before the 5min setInterval — mirrors scheduler/index.ts's pattern; early-expiring entries no longer survive a full interval beyond their TTL. #10: both `lib/email-parser.ts` and `lib/ai/gemini.ts` had module-init `LUNARY_ENABLED` reads; converted to call-time `lunaryEnabled()` functions matching `lib/ai/prompts.ts:lunaryEnabled`. `gemini.ts`'s `tracedGenerate` became a function that lazily builds + memoizes the wrapModel-wrapped generator on first traced call, so processes that never enable Lunary never pay the wrap cost. 74/74 green; no new smokes needed (covered by existing prompt-render + tagline-draft suites which exercise both code paths).
- **2026-05-27 (Languages + Hobbies always-show + extras side-by-side render)** — User noticed generated resumes never showed their Spanish/French/German fluency or Creative Film Writing hobby. Root cause: `selectProfileExtras` was posting-keyword-filtering ALL three extras sections (skills / languages / hobbies), so for an off-domain posting (security-guard role × tech profile) every section dropped to empty. M8.7's design intent was to tailor Skills to the posting (defensible for long lists) but the same filter on Languages/Hobbies was overreach — those are credential/personality signals that should always render. Fix: filter dropped for Languages and Hobbies (always shown); Skills filter unchanged. Companion template change: the three extras sections now render as a single `extras-row` flex block so a 3-skill list + 4-language list + 1-hobby line sit side-by-side instead of eating 3 × (heading + content) vertical lines. `flex-wrap` lets any oversized section overflow to a new row. New `profile-extras-filter-smoke.ts` (9 assertions) wired into pre-push. 74/74 green.
_(Earlier entries — 2026-05-26 Profile-dash collapse UI + M8.7 relevance-pipeline overhaul, and the 2026-05-27 cross-track / lockstep work — pruned per the 3–5-entry protocol; detail lives in the "Last session" history above, `docs/implementation.md` per-milestone sections, and `git log`.)_

## Known issues / parked TODOs

- **Manual UI smoke** is still nominally outstanding (eyeball the Profile dash, confirm cards render / drag-reorder works / Import + Generate cards look right, sanity-check `viewHue: 280`). Backend pipe is verified end-to-end so this is low-risk visual confirmation.
- **LLM fuzzy bullet dedup** — current merge dedup is exact-text only. "Built a TS API" vs "Built a TypeScript API" both survive. Add an LLM "are these the same accomplishment?" pass scoped to one parent entity when this becomes painful.
- **LinkedIn export ZIP** — separate unzip path reading `Positions.csv` / `Education.csv`; not wired yet.
- **Legacy `.doc`** — mammoth handles `.docx` only. Either skip or wire a converter.
- **`viewHue: 280`** for Profile dash is a placeholder — easy one-liner change in `components/providers/state/index.ts`.
- **Profile UI: per-entity `pinKeywords` chip-list editor.** 2026-05-26 backend shipped (schema migration `20260526220116_add_pin_keywords_to_entities`, Zod schemas, repository read/write, API routes, scorer keep-always + position-0 reorder, one-page pruner unremovable extension, hermetic select-smoke still 14/14). UX gap: no UI yet — users set pins via `scripts/set-entity-pin-keywords.ts` (list / set / clear). Need a chip-list affordance on WorkRoleRow / ProjectsCard rows / EducationCard rows, parallel to the bullet-tag chip pattern in BulletRow.tsx. Lives at the entity header (alongside title/company/dates). Wires into the existing PATCH endpoints — Zod schemas already accept `pinKeywords` on the request body. **Also: apply same migration to `prisma/prod.db`** via `DATABASE_URL=file:./prod.db npx prisma migrate deploy` (with prod tier paused) before this feature reaches prod resume gens.
- **Commodity-only entity penalty (resume scoring change #3).** Planned 2026-05-26. Sibling to the just-shipped keyword-importance weighting (`posting-parse` now emits `{keyword, importance}` per keyword; `scoreBullet` multiplies its base by `importance`). The penalty addresses the residual case where an entity racks up score on commodity keywords (`JavaScript`, `Web Applications`, `Full-stack Development` — importance 1–2) while another entity has fewer matches but on differentiator keywords (`Space Systems`, `ITAR` — importance 5). Today, DomeIQ (3 commodity matches on the top bullet) can still outscore Iris (2 differentiator matches per bullet) even with weighting, because count × small-weight > count × big-weight when the count gap is wide enough. Design: per-entity, compute `differentiatorShare = sum(weight ≥ 4 matches) / sum(all matches)`. If `differentiatorShare < 0.25` AND the posting has at least one keyword with `weight ≥ 4`, apply a haircut (multiplier in [0.5, 0.85] proportional to share). Open product calls: (a) is 0.25 the right threshold, or should it be the share of TOTAL posting weight (i.e. posting has 10 importance-5 points; entity hits 0 of them → big haircut, hits 5 of them → small haircut); (b) should the haircut be linear or step (e.g. 0.7 if below threshold, else 1.0); (c) do locked bullets opt out (they're user-pinned, shouldn't be score-suppressed). Implement in `lib/resumes/select.ts:scoreBullet` or a post-scoring pass in `selectFor`. Verify by re-running Rocket Lab generate — expectation: Iris ranks at or above DomeIQ.
